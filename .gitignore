node_modules
dist
.env
.env.local
*.local
.DS_Store

# Build output — Vercel builds from src/, never commit dist/ or copies of it
dist/
dist-ssr/
*.tsbuildinfo

# Claude Code worktrees — filesystem state, not for git
.claude/worktrees/

# Local DB snapshots from Supabase MCP exports — contain real user data, never commit
backups/
