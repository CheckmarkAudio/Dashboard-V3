# Project State — Checkmark Audio Dashboard

> **Single source of truth** for what's live, what's in flight, what's deferred.
> Read this BEFORE doing any work to understand current context.
> Updated each meaningful commit.
> For the broader mission, AI role lanes, safety laws, history protocol, and design memory, start with `docs/00_PROJECT_OS/README.md`.

---

## Snapshot

| | |
|--|--|
| **Live URL** | https://cm-audio.vercel.app (renamed 2026-07-11 from `dashboard-v3-dusky.vercel.app`; Vercel project itself is also named `cm-audio`, matching. The old `dashboard-v3-dusky.vercel.app` domain is intentionally still attached too — no downtime during the switch. A real custom domain, e.g. `app.checkmarkaudio.com`, is the next step once the director + Gavin pick a name) |
| **Hosting** | Vercel (auto-deploys from `main`) |
| **Database** | Supabase project `ncljfjdcyswoeitsooty` ("Checkmark Intern Manager") |
| **Latest commit (main)** | `54c1d81` (PR #287) — **DM dock: edit + delete own messages.** Hover-revealed pencil + trash on every own bubble in the floating DmDock chat windows (opened from the header Messages bell). Optimistic updates in `useChannelChat` (`deleteMessage` removes from local state immediately; `editMessage` applies the new text immediately) backed by Supabase writes; realtime UPDATE/DELETE events reconcile any drift. Inline edit: textarea replaces bubble content with Enter-to-save / Escape-to-cancel / X and ✓ buttons. Reuses the same `chat_messages` RLS policies added for the Forum in PR #220 (owner-update + owner-or-admin-delete) — no migration needed. `tsc -b` + `vite build` clean. **Preceded by:** `420e98d` (PR #286) — **always-visible inline trash on every member row.** `Trash2` icon on each roster row in both `TeamManager.tsx` (Members admin) and `AccountAccessPanel.tsx` (Settings → Account Access) — dimly visible at rest (`text-red-400/50`), brightens on hover, triggers a `ConfirmModal` "Are you sure?" before executing the delete. No horizontal scroll required. `c48b500` (PR #285) — **Self-serve member onboarding** — `admin-create-member` edge function calls `inviteUserByEmail` → member gets a Supabase invite email → clicks link → `RecoveryGate` intercepts `type=invite` → member sets their own password → status flips `pending→active` automatically. `TeamManager` wizard redesigned: Account Setup radio buttons removed, Step 4 replaced with a "Invite sent" success screen with numbered "what happens next" list, Pending filter tab + amber badge added, Re-send invite action added for pending rows. `e77bf21` (PR #284) — fix: drop `redirectTo` from `resetPasswordForEmail` to avoid Supabase allowlist error. `fe6c7ce` (PR #283) — fix(login): remove help modal, promote Forgot Password as the only recovery path. `01615b3` (PR #281) — fix DM attachment loading + drag-drop/paste + forgot-password flow improvements. `ecf86f9` (PR #279) — chore: regen DB types + sync PROJECT_STATE. |
| (prior) | `138920a` (PR #278) — **Feedback admin triage UI** (close of the "looks done but isn't" sweep). New **Feedback** section in Admin → Settings reads the `support_reports` rows the Feedback button now writes — filterable report list with severity badges, reporter, page-url chip, and an admin-gated status dropdown. Backed by FK `support_reports.reported_by → team_members(id)` + the `set_support_report_status()` SECURITY-DEFINER RPC. Preceded by `34f91e4` (#276, Analytics charts → real `sessions`+`assigned_tasks`) and `6ac99df` (#277, Feedback button → `support_reports` backend). `database.ts` types regenerated; security advisor reran clean (0 errors). |
| (prior) | `be80ee4` (PR #260) — **Calendar UI refresh — Izmahsa-inspired (5-PR sequence #253/#255/#258/#259/#260).** Bridget shared a "Calendar Dashboard by Izmahsa" reference and asked for a cleaner, team-oriented look. Shipped one PR at a time with live preview review on each. **(1) PR #253** (`bf8011c`) `perf(edge): cache Dropbox access token` — module-level token cache + in-flight dedupe in `upload-to-dropbox` so all four actions skip the ~500 ms OAuth refresh when an isolate is warm (deployed via Supabase CLI, v9+). **(2) PR #255** (`b68de46`) `MiniMonthPicker` added to the calendar left sidebar (below the existing Today/On-Shift `CalendarDayCard`, which stretches to fill while the picker anchors at the bottom). Also restructured the page header onto ONE row — title + member filter pills (first names + Filter icon, no "SHOW" label) + Schedule toggle + Request-schedule — and moved the week-nav (chevrons + "This Week" + date range) INTO the week-grid card header. **(3) PR #258** (`b0334b5`) session-type pastel colors on booking blocks via new `src/lib/calendar/sessionColors.ts` (engineering=mint, music_lesson=violet, consultation=sky, training=amber, education=rose, fallback slate) — replaces the old uniform gold `.calendar-booking-block`. **(4) PR #259** (`96c6d52`) member avatars on booking + schedule blocks via `memberByName` / `memberById` lookups. **(5) PR #260** (`be80ee4`) the big one — **per-member avatar-derived colors + the schedule-overlay redesign.** New `src/lib/calendar/extractAvatarColor.ts` samples each member's avatar in a 32×32 canvas (saturation-weighted average + HSL boost so murky photos snap to a vivid hue; CORS-tainted / blank fall back to a hashed palette), `memberColors.ts` returns CSS `rgba()` strings, `useTeamMemberColors.ts` resolves the whole team in parallel + caches by avatar URL (re-upload → fresh color). The schedule overlay went through many iterations with Bridget and landed on: **time-segment merge** (one wash block per stretch where the on-shift set is constant) + **start/end avatars only** (a member's continuous shift shows their face at arrival + departure, not repeated down the block) + **no segment seams** (dropped per-segment borders, bumped fill to /35 so hour-grid lines don't bleed through) + **hover-to-spotlight**: idle avatars are full-grayscale @ 0.85 opacity; hovering any avatar pops that member's icons to full color everywhere they appear AND repaints their segment blocks in the member's accent hue (/55), while non-matching segments fade to 0.18. Per Bridget the final result "isn't exactly what I'm looking for but it's the best so far" — shipped as the working baseline. `MemberAvatar` reused throughout; `firstName()` helper shared with the header pills. |
| (prior) | `787d564` (PR #252) — **Storage-cost wave + Media page upgrade + notification polish (7-PR batch).** The forum chat + Media page were each consuming Supabase storage faster than the 1 GB free tier could absorb (audio + photo uploads on track to fill the bucket in days). This wave routed video/audio off Supabase entirely and made the Media page a shared team library with proper thumbnails. **(1) PR #245** (`d7c7f77`) Client-side image compression before forum upload — canvas resize to 1920px max long edge + JPEG q=0.82 in the browser before hitting `forum-media`. Phone photos go ~4 MB → ~400 KB. Skips GIFs, sub-500 KB files, and decode failures (safe fallback to original). **(2) PR #247** (`70f280b`) Forum video + audio uploads route to Dropbox under `/forum/<channel>/<user>/<file>` via new `finalize-forum` edge-function action; chunked upload + share-link → raw URL transform (`?raw=1`) so `<video src>` / `<audio src>` stream straight from Dropbox CDN. Images stay on Supabase Storage. Zero schema change; chat attachment shape identical regardless of backend. **(3) PR #248** (`633eb94`) Media page becomes a **shared team library** — RLS migration `20260526120000_media_submissions_team_visibility.sql` replaces the (own + admin) SELECT policy with (any authenticated team_member). AddMedia.tsx drops the `.eq('member_id', memberId!)` filter that was overriding RLS; query joins `team_members` for submitter display name. Each row shows submitter + size + relative-time. Image rows render 48 px Dropbox-hosted thumbnails with click-to-lightbox modal (Escape + scrim-click close). **(4) PR #249** (`87090ad`) Whole image row clickable to open lightbox — `role="button"` + Enter/Space activation + `stopPropagation` on the "Open" link so it still escapes to Dropbox without firing the lightbox. **(5) PR #250** (`334fa72`) **Dropbox thumbnail API for ~115× bandwidth reduction** — new `thumbnail` action on the `upload-to-dropbox` edge function hits `files/get_thumbnail_v2` (w128h128 JPEG) and returns base64; `useDropboxThumbnail(fileId)` hook (React Query, `staleTime: Infinity`) replaces hot-linked full-size raw URLs. Media page transfer drops from ~36 MB → ~316 KB per open. Lightbox keeps the raw URL since you want real resolution when clicking. **(6) PR #251** (`6786ad2`) Notifications widget quick-reply shows forum link previews (Instagram-style OG card carried over from the chat bubble). **(7) PR #252** (`787d564`) Notification links open to the exact channel + message rather than the forum root. **Edge function `upload-to-dropbox`** is now at version 9 ACTIVE on prod Supabase (added thumbnail action via Supabase CLI). |
| (prior) | `14bde9d` (PR #242) — **Throttled pulse-and-lift on Overview Checklist + Checklist placed on member_overview (col 1, rs=1).** Cap-once-per-30-min `useThrottledPulse(key, intervalMinutes, enabled)` hook + `pulse-lift` CSS keyframes (1.8s ease-out-expo, translateY(-4px) + scale(1.008) + soft gold shadow peak at 35%). Wired into `TeamChecklistWidget` gated by `useLocation().pathname === '/'` so the animation only fires on Overview (not on `/daily`). LAYOUT_VERSION 38 → 39 dropped saved member_overview layouts so every user picks up the new Checklist placement automatically. **Closed out the entire Task Tweaks batch (PRs #238, #241, #243) + the full Member Work Scheduler series (PRs #227–#232, #234) + forum polish (#233, #239, #240) + Studio Hours of Operation (#237) + quick-keys renumber (#236) + drag-overlay portal fix (#243).** Net: scheduler end-to-end live across Members admin · Calendar overlay · Overview widget · Profile page · admin Hub widget · CalendarDayCard shifts strip. |
| (prior) | `200fd8c` (PR #225) — **Forum: drag-anywhere · Cmd+V paste · parallel multi-file uploads (PR B of the Discord-style overhaul).** Full-page drop zone in `Content.tsx` (dragHasFiles via `dataTransfer.types`, dragDepth counter to prevent flicker, translucent overlay); Cmd+V paste handler reads `clipboardData.items` and uploads any image; `<input multiple>` on every MediaPicker file input + `Promise.all` for parallel uploads so a fast image lands before a slow video. New `src/lib/forum/upload.ts` shared util consolidates picker + drag + paste into one MIME/size/Storage code path. Pending image attachments render as 64×64 thumbnail tiles. Per user direction: "Discord is the golden standard, and we want fast submissions, nothing that takes a while to load." |
| (prior) | `7e3f639` (PR #220) — **Forum: edit + delete your own messages (admin can delete any).** Hover-revealed `…` menu on every bubble; Edit (own only) swaps in an inline textarea with ⌘/Ctrl+Enter save + Esc cancel; on save bumps a new `edited_at` column + shows "(edited)" badge with precise-time tooltip. Delete uses a two-tap rose "Delete?" pill so misclicks don't nuke anything. Realtime sub on `chat_messages` extended from INSERT-only to also catch UPDATE + DELETE so other clients see the change live within a beat. Schema migration `20260520210000_chat_messages_edit_delete.sql` adds `edited_at timestamptz NULL` + two new RLS policies (`chat_messages_owner_update`: sender-only; `chat_messages_owner_or_admin_delete`: sender OR `is_team_admin()`). Migration applied to prod via MCP at PR open so the writes worked end-to-end the moment the frontend deployed. Per user direction: "we need to be able to edit and or to delete our messages if we need to." |
| (older — kept for context) | **Task widget tweaks (3-pack): My Tasks reorder · sitewide Priority filter pill · admin reassign in Edit Tasks.** Three coordinated tweaks to the task widgets across `/daily` + Overview, picked by the user as the next polish bundle after the Calendar delete work shipped. **(1) My Tasks reorder** — `MyTasksCard` now wraps its Active task list in a dnd-kit `DndContext` + `SortableContext` (vertical strategy); each row picks up a small grip handle on the left edge (hover-revealed so it doesn't compete visually with the existing checkbox + title). Drop fires a debounced (500 ms) DB write to a new `team_members.preferences.my_tasks_order` jsonb subkey via the existing `setUserPreference` helper — same pattern as `useWorkspaceLayout`. Order follows the user across devices/browsers + survives reload. Sort logic: saved order wins for known IDs; brand-new assignments (not yet in saved order) bubble to the TOP so the user notices them, then settle into a preferred spot after their first drag. Pending mid-flight tasks (transfer/delete/edit awaiting admin) stay in the dimmer non-draggable Pending section below the divider — they can't be reordered while in-flight. Filter changes (priority pill / source pill / completed eye) don't lose the user's order: dropped subset gets spliced back into the canonical saved order at the same positions. **(2) Priority filter pill** — new shared `<PriorityToggle>` in `src/components/tasks/shared.tsx` (Flame icon + count suffix) drops into the toolbar of all three task widgets: `MyTasksCard` (drives the `team_tasks` widget on Overview + `/daily`) and `AssignmentBoardBody` (drives `studio_tasks` + `team_board` widgets on `/daily`). Reads the existing `is_required` boolean — no new column; the inline gold Flame icon that's been on priority rows since PR #94 is the same field. Per-session state at the call site (no persistence — matches the show-completed eye's behavior so flipping it on doesn't surprise on next visit). Stacks cleanly with the existing source filter (Assigned vs Self) on MyTasksCard. Count suffix reflects "priority within the current view" so it always tells the truth about what toggling it on will narrow to. **(3) Admin reassign** — `AdminEditTasksModal`'s inline edit form now exposes an **Assigned to** dropdown picker right under Title. Pulls active team members (sorted alphabetically by display_name + position), defaults to the task's current assignee, supports an Unassigned option that clears the column. Studio-scope tasks (assigned_to NULL by definition) render a small "assignee not applicable" hint instead so admins don't think the field is broken. RPC already supported `p_assigned_to` / `p_clear_assigned_to` (added 2026-05-07 per the comment in `adminUpdateAssignedTask`) — this PR is UI-only on top of an already-wired backend. If the current assignee is inactive/missing from the active list, they surface as a fallback option labeled "(inactive)" so the admin sees what they're changing from. No DB migration. Recent stack: `73f9a3d` Calendar delete + scope picker (PR #204); `df9336e` Media nav rename; `(PR #201)` Bookings full-table export; `8d9c786` Workspace inline expand (PR #200); `33a2a58` Activity inline expand (PR #199); `0128c5f` Activity widget exports (PR #198). |
| **Currently active** | **Archived members hidden site-wide + Settings → Archive (branch `feat/archived-members-hidden`).** Follow-up to the lifecycle work: archived members (`status='inactive'`) were still cluttering team bubbles, overview/hub widgets, and pickers because the shared roster query didn't filter them. Fix: **`fetchTeamMembers()` now excludes `status='inactive'`** at the source — one change hides archived members from every consumer of the shared `teamMemberKeys.list()` cache (task pickers, calendar cards, message dialogs, overview + hub widgets, checklists) and is a no-op for the handful that already hand-filtered `!== 'inactive'`. Pending (mid-onboarding) members stay visible. Added `fetchAllTeamMembers()` / `fetchArchivedTeamMembers()` for admin surfaces. New **`ArchivedMembersPanel`** wired into **Settings → Archive** (new left-rail section, Archive icon): lists archived members (grayscale avatar), **Restore** (unarchive → back on roster + login re-enabled) and **Delete forever** (two-tap; pre-checks history via `inspectMember`, blocked with a clear message when linked records exist). Historical attribution is untouched — name-by-id lookups (`.in('id', ids)`) still resolve archived members; only roster-style lists filter them. `admin-remove-member` is at **v5** (deployed) from the prior hotfix. TeamManager roster keeps its own direct query + status filter (admin management surface). `tsc -b`+`vite build` clean. <br><br>**Prior — Member lifecycle reliability — MERGED (PRs #289 `056fefa` + #291 `3626bc6`).** The permanent fix for "A user with this email address has already been registered". Root causes found by full sweep on 2026-07-08: **(1)** every delete path (`TeamManager.handleDelete`, `AccountAccessPanel.confirmDelete`) removed only the `team_members` row, stranding the `auth.users` login so that email could never be re-added — two orphaned auth accounts existed in prod (Tony Rivara's from 2026-07-08 + one from April), both purged, DB verified 0 orphans in either direction; **(2)** PR #285 rewrote `admin-create-member` around `inviteUserByEmail` but the function was **never deployed** — prod ran April's v15, so the UI promised invite emails nothing ever sent, and duplicate emails surfaced as the raw GoTrue 422; **(3)** no custom SMTP is configured, so invite/reset email cannot deliver reliably regardless (decision #7 already knew this; #285 re-bet on it anyway). Fix shipped + **both functions deployed via MCP**: **`admin-create-member` v16** — back to the proven `createUser(email_confirm)` + `generateLink(recovery)` pattern; always returns a guaranteed `setup_link`; self-heals every prior state for an email (orphaned login → adopt; archived member → reactivate + unban + fresh link; active → clear 409); **`admin-remove-member` v4** — Codex's #254 function finally adopted into the repo + wired to the UI, with a new `unarchive` mode (archive = inactive + login ban; unarchive = active + unban; permanent_delete = auth **and** profile, only when zero linked history so attribution survives). UI: new shared **`RemoveMemberModal`** (big Archive vs Delete-forever buttons, live history counts) replaces both raw deletes; create-success screen shows **`SetupLinkReveal`** (copy + Gmail buttons); roster menu gains **"New setup link"** (pending) / **"Password reset link"** (active); Activate/Deactivate now genuinely enable/disable login. Honest copy replaces "invite email sent automatically". **Notes:** Supabase-dashboard user deletes fail by design for members with history (`team_members` FKs are NO ACTION) — use in-app Archive; supersedes PR #254 (close it); "Forgot password?" on the login page still depends on built-in SMTP → configure a custom SMTP provider (Resend/Gmail app password) in Supabase Auth settings to make member-facing email reliable; until then admins hand out reset links from the roster menu. <br><br>**Prior — Member UX hardening wave — COMPLETE + MERGED (PRs #281 → #287).** Five PRs closing out the self-serve onboarding + delete-member + DM edit/delete backlog. **(1) PR #283** `fe6c7ce` fix(login): Removed the modal-based help sidebar from the login page and promoted "Forgot Password" as the sole recovery path — clean, one-path UX. **(2) PR #284** `e77bf21` fix(auth): Dropped `redirectTo` from `resetPasswordForEmail` — Supabase was rejecting the URL because it wasn't in the allowlist; dropping it lets Supabase use its project-default redirect which is already allowed. **(3) PR #285** `c48b500` Self-serve member onboarding: `admin-create-member` edge function now calls `inviteUserByEmail`; invited members receive a Supabase email, click the link, `RecoveryGate` intercepts `type=invite` and shows "Set your password", then auto-flips `pending→active`. `TeamManager` wizard redesigned (no more radio buttons / temp-password mode) — clean "Invite sent" success screen, Pending filter tab + amber badge, Re-send invite action. **(4) PR #286** `420e98d` Always-visible trash bin on member rows in both `TeamManager` roster and `AccountAccessPanel` (Settings → Account Access) — `ConfirmModal` "Are you sure?" gate, no horizontal scroll required. **(5) PR #287** `54c1d81` DM dock edit + delete: pencil + trash on own messages in all DmDock floating windows (the "other chat interface on the header"), optimistic updates, inline textarea edit mode. **Genuinely next:** SMTP fix (Gmail App Password needed in Supabase SMTP settings), referral tracking (Retention sibling), session "completed" status (Production undercount), role-change→Education hook, delete dead `buildMemberFlywheelChartData`, settable per-stage goals + `member_kpis` target overlays. **Open PR to watch:** #254 (Codex member-removal-safety) marked CONFLICTING — needs rebase before it can merge. **Prior sweep (complete):** "Looks done but isn't" (PRs #276 → #279): Analytics charts wired to real data, Feedback button backend, Feedback admin triage UI, type regen + security advisor 0 errors. <br><br>**Prior — Feedback button backend (PR #277, MERGED).** The bottom-left Feedback button (`TroubleshootingButton.tsx`) used to fake-submit (300ms timeout + toast, no persistence). Now it writes for real. Migration `20260607120000_support_reports.sql` (applied to prod via MCP, validated by rollback): `support_reports` table (description, what_tried, severity Low/Medium/High/Critical, page_url, user_agent, status open/in_progress/resolved/dismissed, team_id, reported_by) + team-scoped SELECT RLS so admins can triage; **`submit_support_report()`** SECURITY-DEFINER RPC is the only write path. Frontend: `src/lib/queries/supportReports.ts` (`submitSupportReport`) + button handler now inserts (capturing current page path + user-agent) with real error handling. `tsc -b`+`vite build` clean. (Type regen deferred — no typed reader of `support_reports` yet; admin triage UI is a later follow-up.) **Just shipped (PR 1 of 2): Analytics charts wired to real data (PR #276)** — Bookings/Monthly Trend/Bookings Trend now read the real `sessions` table, Tasks split KPI(tagged) vs Other(untagged) from real `assigned_tasks`; removed empty placeholder consts + mock `bookings`. <br><br>**Prior active — Review-received capture (Retention epic, phase A) — MERGED (PR #275).** First real Retention signal. Migration `20260530190000_client_reviews.sql` (applied to prod via MCP, validated by rollback): `client_reviews` table (rating 1-5, source manual/google/other, body, client_id, team_id) + team-scoped SELECT RLS; **`log_client_review()`** SECURITY-DEFINER RPC is the only write path and **atomically inserts the review AND emits a `retention` flywheel event** (`source_type: 'review'`, metadata rating/client/source). Frontend: `src/lib/queries/reviews.ts` (logClientReview + fetchReviewStats); ClientsPanel gets a **"Log review"** action (star-rating modal + source + note) and a per-client **★ avg · count badge**; `describeFlywheelEvent` learns the `review` source ("logged a 5★ review"); types regenerated. So logging a review now lights up the Retention stage across all the flywheel surfaces. `tsc -b`+`vite build` clean. **Phase B (later):** team Reviews tile on Analytics, per-client review history, `review_requested_at` ask→received conversion. Google auto-import = phase C. **Recently shipped + merged:** Flywheel demo-preview + Analytics polish (PR #273), DM media attachments (#274), Hub widget→ledger (#272), KPIs panel→ledger (#271), charts→ledger (#270), emit coverage incl. task-by-tag (#269), canonical rename + `growth`→`retention` (#268), Phase 2 activity feed (#267), ledger re-key (#266), Flywheel Phase 1 (#203), Messenger chat dock (#264), Direct Messages (#263), Feedback button (#265). **Next epics:** referral tracking, session "completed" status, contracts/invoicing, LMS/ticketing. Plus small cleanups (role-change→education hook; delete dead `buildMemberFlywheelChartData`; `member_kpis` numeric target overlays). **Before that: Calendar refresh shipped + merged (PRs #253/#255/#258/#259/#260).** The 5-PR calendar sequence (token cache → mini-month + header restructure → session-type colors → member avatars → per-member avatar-derived colors + hover-spotlight schedule overlay) is all on `main` and live. Bridget's verdict on the final schedule-overlay treatment: "isn't exactly what I'm looking for but it's the best so far" — so it's the working baseline; a future revisit may rethink the overlay if she lands on a clearer mental model. **Open PRs NOT from this session (left untouched):** `#254` Codex member-removal-safety (their active branch — Claude stays out of `TeamManager.tsx` / account-access delete UI / member-archive migrations until it merges); plus older/stale: `#211` recurring-task one-active, `#153`/`#108` docs, `#111` security tooling, `#110` book-button pills row, `#91` social links. **On deck / queued** (carried): (a) **Dropbox thumbnail batching** if Media list-load still feels sluggish post-token-cache (`files/get_thumbnail_batch`, up to 25 thumbs/call); (b) **Calendar overlay revisit** if Bridget forms a clearer target — the current hover-spotlight is a baseline, not a final; (c) **Google Calendar Phase 2A** — reconnect Google + "Push pending bookings"; (d) **Flywheel event ledger Phase 1** (PR #203); (e) **Sessions polish #2 redo**; (f) **Codex AI titling/search** lives on `codex/ai-titling-search` — separate worktree, Claude doesn't touch. |
| **Prior active (scheduler kickoff)** | **Member Work Scheduler — PR 1 of 4 in flight on `claude/member-work-scheduler`.** User scoped a four-PR sequence: (PR1) data tables + admin Work Scheduler in Members left rail; (PR2) calendar overlay with sage/teal translucent blocks behind bookings; (PR3) Overview `<MyScheduleWidget />` with my-vs-team toggle + member "Request schedule block" flow (admin approves/denies like task requests); (PR4) weekly schedule section on member Profile page. This PR1 ships: migration `20260523120000_team_work_scheduler.sql` creating `team_schedule_recurring` (weekday + start/end time + effective window) and `team_schedule_blocks` (status pending/approved/denied, one-off + member-proposed) with RLS (admin write anything, members SELECT all + INSERT pending for self + DELETE own pending only — withdraw flow); `src/lib/schedule/` (expand.ts pure expansion fn + useTeamSchedule hook with realtime sub + mutations.ts admin/member CRUD); `src/components/admin/WorkScheduler.tsx` slotted into TeamManager left rail between Roster and Clock Data (Pending requests panel only when blocks exist · Recurring weekly hours table with Tue–Sat default weekday picker · One-off blocks list scoped to the visible week with week-navigator). Studio work week locked as Tue–Sat; weekend rows allowed for cover staff. Member request flow lands in PR3.  **Prior round — Forum is feature-complete (PRs #209, #218–#226).** Today's wave shipped: edit/delete messages, admin create-channel, comprehensive media overhaul (linkify pasted URLs · audio uploads · video codec-failure fallback · Instagram-style OG previews via new `unfurl-link` edge function + `chat_link_previews` cache · Discord-style optimistic sends + drag/paste/parallel uploads · Spotify/SoundCloud/Bandcamp inline player embeds). Plus a parallel push on **media uploads** — Dropbox cap went 50MB → 150MB single-shot (PR #216) → **10GB chunked sessions** (PR #217, browser drives `upload_session/start|append_v2` directly so file bytes never proxy through the edge function). Plus **Tasks widget** maintenance: drag widgets across carousel pages now works (PR #213); new **Checklist** widget for team maintenance items (PR #212) + admin Manage Checklist surface on Assign page (PR #215); recurring tasks now stay as one active instance with "Yesterday" overdue badge (PR #211 — Apple-Reminders model). Plus QoL: refresh-stays-put (PR #208), stale-chunk auto-recover (PR #210), session-memory widget expansion (PR #206). **On deck / queued**: (a) **Member Work Scheduler PRs 2–4** (calendar overlay → Overview widget + request flow → profile section); (b) **Google Calendar Phase 2A** — user reconnects Google + clicks "Push pending bookings"; (c) **Flywheel event ledger Phase 1** (PR #203 still open) — `flywheel_events` append-only table + `emit_flywheel_event` RPC; (d) **Sessions polish #2 redo** — earlier preview (PR #207, scratched) restructured `/sessions` with left-rail Bookings · Calendar · Clients tabs; user rejected the layout but the underlying need (Calendar view on Booking page) still stands — explore a lighter pattern (e.g. a "View calendar" toggle inside Bookings instead of a full restructure). |
| **Current planning overlay** | **Project OS + worker-obvious web polish foundation (in progress).** User clarified that Tasks is only the first tactical proof point, not the core vision. The broad goal is a beautiful, refined, easy, stable, worker-magnetic Checkmark operating system with a safe, reliable, documented backend and traceable AI/human history. New Project OS front door: `docs/00_PROJECT_OS/README.md` plus vision, language, laws, roles, history/learning, design, and checkpoint ledger docs. Existing master phase order remains `docs/pwa/APP_BUILD_ROADMAP.md`; Accountant stays deferred until auth/daily clarity is handled unless user overrides. Website polish sources: `docs/AI_CODERS_READ_THIS_FIRST.md`, `docs/pwa/WEB_INTERFACE_POLISH_ROADMAP.md`, `docs/ux/WORKER_OBVIOUSNESS_AUDIT.md`, `docs/ux/TASKS_PAGE_REDESIGN_PLAN.md`, `docs/ux/MESSAGES_DISCOVERY_PLAN.md`, `docs/ux/SCHEDULE_UX_REDESIGN_PLAN.md`. First implementation slice: `/daily` now opens on My Tasks and places Checklist, Team Tasks, and Studio Tasks behind a sidebar/pane system in `src/pages/DailyChecklist.tsx`. Open validation: Gavin/worker should test whether My Tasks is obvious and whether secondary task panes are understandable; vacation/time-off language/data model still needs director decision before backend work. **CODEX:** authored. |
| **Prior active (forum round)** | **Forum is feature-complete for this round (PRs #209, #218–#220, plus #202 chat color from earlier).** Today's wave shipped: edit/delete messages, admin create-channel, comprehensive media overhaul (linkify pasted URLs · audio uploads · video codec-failure fallback · Instagram-style OG previews via new `unfurl-link` edge function + `chat_link_previews` cache). Plus a parallel push on **media uploads** — Dropbox cap went 50MB → 150MB single-shot (PR #216) → **10GB chunked sessions** (PR #217, browser drives `upload_session/start|append_v2` directly so file bytes never proxy through the edge function). Plus **Tasks widget** maintenance: drag widgets across carousel pages now works (PR #213 — measuring strategy + widget-preferred collision detection); new **Checklist** widget for team maintenance items (PR #212) + admin Manage Checklist surface on Assign page (PR #215); recurring tasks now stay as one active instance with "Yesterday" overdue badge (PR #211 — Apple-Reminders model). Plus QoL: refresh-stays-put (PR #208), stale-chunk auto-recover (PR #210), session-memory widget expansion (PR #206). **On deck / queued**: (a) **Google Calendar Phase 2A** — user reconnects Google + clicks "Push pending bookings"; (b) **Flywheel event ledger Phase 1** (PR #203 still open) — `flywheel_events` append-only table + `emit_flywheel_event` RPC; (c) **Sessions polish #2 redo** — earlier preview (PR #207, scratched) restructured `/sessions` with left-rail Bookings · Calendar · Clients tabs; user rejected the layout but the underlying need (Calendar view on Booking page) still stands — explore a lighter pattern (e.g. a "View calendar" toggle inside Bookings instead of a full restructure). |
| **Prior active** | Task widget tweaks 3-pack (PR #205) — My Tasks DB-persisted drag-reorder + sitewide Priority filter pill + admin reassign in `AdminEditTasksModal`. Calendar delete bookings + recurring scope picker (PR #204) — Sessions polish #1; admin-only Delete pill in `BookingDetailModal` footer + right-click context menu on week-grid blocks; Google-Calendar-style "Just this event" vs "This and all future events" scope picker for recurring rows; new `admin_delete_future_sessions` RPC nulls out template's `recurrence_spec` to stop the cron from spawning fresh children of a dead series. Sessions/Bookings full-table CSV/PDF export on `/sessions` (PR #201) — closed out the export pipeline at 9 admin surfaces via new `src/lib/columns/adminSessionColumns.tsx`. Light-mode revamp — soft borders + grey body / white cards + fintech wash + bright yellow gold token + contrast scan. PR #102 (`26225d8`) Studio +Add modal + recurrence + Members-pane chrome parity + `/daily` widget sectioning + modal close-on-mouseup fix. PR #101 (`0d550f6`) Studio tab on Assign + `studio_space` room sections. PR #100 (`b31f4b7`) uniform pending symbols + green approve buttons + clean diff container. PR #99 (`1ebaad3`) sitewide orange-for-edit + clickable admin queue rows + pending-row opacity fix. PR #98 (`23d5cce`) RequestDetailModal + member cancel + cancelled status. PR #97 (`08dc7c3`) member request-to-edit flow. PR #96 (`d5a541d`) Templates as inline sidebar tab. PR #94 (`f717c1e`) removed the empty "Settings for Tasks" button. PR #90 (`740667d`) Assign delete reliability. PR #89 (`8dfa922`) sticky header fix (`overflow: clip`). PR #88 (`4be99e3`) MyTasksCard pending section + divider. PR #87 (`a5a9fe2`) realtime sync for task surfaces. PR #86 (`13aaaea`) `FloatingDetailModal` portals to `document.body`. PR #85 (`3fffc6f`) unified member request actions — Delete + Transfer composer pattern. PR #84 (`56693de`) admin-delete UX polish. PR #82 (`52954df`) admin direct-delete on `/admin/templates`. |
| **Security advisor (last explicitly repo-verified)** | **0 ERRORS** (rerun 2026-06-07 after the feedback/triage RPCs). 130 lints total, all WARN/INFO: 100 WARN `authenticated_security_definer_function_executable` + 22 WARN `anon_security_definer_function_executable` (both by-design — every SECURITY DEFINER RPC keeps its default PUBLIC grant, so `anon`+`authenticated` can call it; each function self-gates internally via `auth.uid()` / `is_team_admin()`), 3 WARN `function_search_path_mutable` (NOT our new functions — all of ours pin `search_path=public`), 2 WARN `public_bucket_allows_listing`, 1 WARN `auth_leaked_password_protection` (Pro-plan paywall), 2 INFO `rls_enabled_no_policy`. Our new `submit_support_report` / `set_support_report_status` show only the executable-by-role WARNs. Rerun Advisor after any new RPC surface before quoting a fresh count. |

---

## Architecture (where things live)

| Area | Implementation |
|--|--|
| **Hosting** | Vercel; `vercel.json` at repo root; SPA rewrites + smart cache headers |
| **Browser security headers** | Added 2026-05-01 in a Codex session: CSP, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, and restrictive `Permissions-Policy` on all routes. |
| **Auth** | Supabase Auth, `flowType: 'implicit'`, `lock` disabled in `src/lib/supabase.ts` |
| **Recovery flow** | Inline `<script>` in `src/index.html` detects `#type=recovery` BEFORE supabase-js init; `src/components/auth/RecoveryGate.tsx` intercepts every route when flag is set |
| **Owner protection** | 3 layers: DB triggers (`protect_owner_update`, `protect_owner_delete`), `OWNER_EMAIL` constant in `src/domain/permissions/index.ts`, email-first `getAppRole(email)` |
| **Permission model** | `owner` / `admin` / `member` roles; `team_members.role` is `'admin' \| 'intern'` in DB (intern = member-tier) |
| **Account creation** | `admin-create-member` edge function (proper GoTrue admin API) called from `TeamManager.tsx`; default UI path now follows with `admin-generate-setup-link` so admins can copy/send a setup link directly |
| **Account access UI** | `src/components/admin/AccountAccessPanel.tsx` — owner-only role toggle + direct setup-link generation for existing members; avoids relying on Supabase SMTP delivery |
| **Tables** | Renamed to `team_*` with `intern_*` compat views (Phase B done; Approach A column rename deferred) |
| **Code splitting** | Per-route `React.lazy()` in `src/features/{member,admin}/routes.tsx`; `<Suspense>` in `Layout.tsx` |
| **Theme** | `light` \| `dark` \| `system`; `src/contexts/ThemeContext.tsx`; localStorage-only (DB persistence deferred) |
| **Design tokens** | `src/index.css` `@theme` block — brand gold scale 50-950, neutrals, semantic status, flywheel stages, animations. **This is canonical** — matches the v1.0 PDF design system. |

---

## Vision & origin

**Business**: Checkmark Audio — a music studio in Albuquerque (recording, mixing, education, artist development).

**Why this dashboard exists**: streamline ops for a small growing team. Increase productivity, workflow, and quality of services delivered. The team includes ADHD employees, so the UX needs to be engageable, clickable, and satisfying — not bureaucratic.

**The flywheel model** (the core conceptual framework):
The business runs on a 5-stage flywheel — **Discovery → Workflow → Production → Education → Retention** (the canonical model since the 2026-05 re-key; "growth" was renamed to "retention"). Stage labels: Discovery & Media · Workflow & Admin · Production & Completion · Education & Community · Retention & Advocacy. The single source of truth is `src/lib/flywheel/stages.ts` (`FLYWHEEL_STAGES` + hex colors + `normalizeLegacyStage` mapping any old keys like deliver→production, capture→workflow, share/attract→discovery, book→workflow). Each stage feeds the next; operations track health across all five and surface where momentum is breaking.

**The three core entities** that feed flywheel KPIs:
- **Bookings** — calendar appointments (creating one emits a Workflow-stage event)
- **Tasks** — team activities to complete (completing one emits an event to the task's *tagged* stage via `category`)
- **Sessions** — completing the actual booked work (emits toward Production)

**The flywheel is now live, not placeholder.** The append-only `flywheel_events` ledger (`record_flywheel_event` / `get_flywheel_stage_summary` SECURITY-DEFINER RPCs, `emitFlywheelEvent()` wrapper) backs every KPI/chart surface — Analytics, the KPIs panel, Team Activity, and the Hub widget all read it. Emit hooks span all five stages (tasks, sessions, clients, media uploads, leads, pipeline, forum posts, education students, reviews). A zero-DB-cost demo (`?flywheel-demo=1`) renders a populated preview without writing to prod.

**Earlier evolution** (briefly):
- Codex did Phase 1 architecture work (permissions, route split, workspace foundation, blueprint at `docs/REBUILD-BLUEPRINT.md`).
- Code-quality and architectural critiques drove subsequent reform: real-data migration off mock contexts, admin/member symmetry, responsive widgets, theme toggle.
- An auth-bug saga (PKCE recovery race, supabase-js lock contention, GoTrue raw-SQL incident with Bridget) drove the auth hardening you see today.
- The intern→team rename Phase B closed the biggest naming-debt item with zero downtime.
- Vercel migration replaced GitHub Pages, eliminating the "hard refresh after every deploy" friction.

---

## Key architectural decisions (the reasoning, not just the what)

These are the load-bearing decisions. If you're considering reversing one, read the rationale first.

**1. Vercel over GitHub Pages**
- *Why:* edge CDN, automatic preview URLs per branch, smart cache invalidation, no manual "hard refresh" needed, ~30s deploys.
- *Trade-off:* Vercel free tier is technically "personal use" per ToS — for a small low-traffic internal tool, enforcement is reputation-based and we accept that risk. Pro is $20/mo if ever needed.
- *Date:* 2026-04-16 — commits `5be53d9` (prep) + `3ca605d` (migration).

**2. Compat views for table renames (Stripe/Shopify pattern)**
- *Why:* rename DDL is atomic; old code keeps working transparently through views; code sweep can happen at any pace; trivially reversible (just drop the views).
- *Trade-off:* views add ~1KB metadata total, zero runtime cost (Postgres inlines simple `SELECT *` views at plan time).
- *Date:* 2026-04-17 — migration `intern_to_team_rename_phase_b`. See `docs/intern-rename-resume/02-strategy.md`.

**3. 3-layer owner protection**
- *Why:* defense-in-depth — a UI bug OR DB drift OR code refactor must not be able to lock the primary admin out of their own app.
- *Layers:* (1) DB triggers `protect_owner_update`/`protect_owner_delete` coerce any change back; (2) `OWNER_EMAIL` constant hardcoded in `src/domain/permissions/index.ts`; (3) `getAppRole(email)` returns `'owner'` based on email match alone, before ever reading the profile.
- *Date:* 2026-04-16 — built reactively after Bridget's login race exposed the need.

**4. Implicit auth flow over PKCE**
- *Why:* PKCE encodes password-recovery URLs as `?code=XXX` query params. The entire `RecoveryGate` + inline-script detection layer was built for the `#type=recovery&access_token=...` hash format. PKCE would require rebuilding every detection layer.
- *Trade-off:* implicit is marginally less secure than PKCE for SPAs, but for an admin-provisioned-accounts model on a known domain, implicit is fine.
- *Date:* 2026-04-16 — commit `50b852a` ("I believe in you Claude").

**5. Code splitting via React.lazy + Suspense**
- *Why:* pre-split bundle was 1.1MB. Admin pages — especially `BusinessHealth.tsx` which pulls in the 308KB recharts library — should not load for member-only employees.
- *Trade-off:* per-route lazy loading adds a brief loading spinner on first navigation; Suspense boundary in `Layout.tsx` makes the loading state consistent across routes.
- *Date:* 2026-04-17 — commit `ba76dfb`.

**6. Disable `navigator.locks` in supabase-js**
- *Why:* stuck-state lock errors (`"Lock was released because another request stole it"`) cannot be retried away — the lock is held until the other tab releases it. Multi-tab is rare in this app's actual usage.
- *Trade-off:* if two tabs ever refresh tokens simultaneously, both refresh — Supabase's server tolerates this (last write wins, session stays valid).
- *Date:* 2026-04-16 — in `src/lib/supabase.ts`.

**7. Direct setup links over SMTP-dependent email delivery**
- *Why:* recovery/setup links are still the professional UX, but Supabase SMTP delivery proved unreliable for non-owner employee addresses. The app now asks Supabase Auth Admin API to generate the link and shows it to the owner/admin to send directly.
- *Trade-off:* admins copy/send the link manually until a real SMTP provider is configured. This avoids plaintext passwords as the default while keeping temp passwords available as a fallback for in-person handoff.
- *Date:* 2026-05-14.

**8. Approach B for rename (tables only, defer columns)**
- *Why:* simple `SELECT *` compat views (~80 lines of SQL) instead of column-aliasing views (~200 lines). Half the code-sweep surface. Lower typo risk per migration attempt.
- *Trade-off:* column names like `intern_id` remain inconsistent with the renamed `team_members` table for now. Cleanup is a future focused session (Approach A — see `docs/intern-rename-resume/`).
- *Date:* 2026-04-17.

---

## Timeline

| Date | Commit / PR | Notes |
|--|--|--|
| 2026-05-30 | (PR pending) `feat/review-capture` | **Review-received capture (Retention epic, phase A).** `CLAUDE:` first real Retention emit source. Migration `20260530190000_client_reviews.sql` (prod via MCP, rollback-validated): `client_reviews` (rating 1-5 CHECK, source manual/google/other, body, nullable client_id FK, team_id) + team-scoped SELECT RLS; `log_client_review(p_client_id, p_rating, p_source, p_body)` SECURITY DEFINER RPC = only write path, atomically inserts the review + a `retention`/`review` flywheel_events row (metadata rating/client_id/source). Client: `src/lib/queries/reviews.ts` (`logClientReview`, `fetchReviewStats` → per-client count+avg, team rollup); `ClientsPanel` "Log review" action → star-rating modal (1-5 + source + optional note) + per-client ★avg·count badge on the name cell; `describeFlywheelEvent` handles `review`; `database.ts` regenerated. tsc -b + vite build clean. Phase B (later): Analytics reviews tile, per-client history, ask→received conversion. |
| 2026-05-30 | (PR #273) `feat/flywheel-demo-preview` | **Flywheel demo-preview mode + Analytics polish (zero-DB-cost populated view).** `CLAUDE:` Bridget wanted to see the KPIs/charts "busy" without polluting the shared prod DB. Client-side demo: `?flywheel-demo=1` (persists per session; `=0` clears; captured in `main.tsx` before the auth redirect can strip it) short-circuits `fetchFlywheelStageSummary`/`fetchFlywheelActivity` + the BusinessHealth placeholder charts to `src/lib/flywheel/demo.ts` — synthetic per-stage counts (busy-studio base, window-scaled, 0.28× per-member) + activity feed + monthly trend / tasks-by-employee / studio buckets + varied per-stage completion rates. Floating "DEMO DATA" badge; gated so real users never see it; **no DB/RPC writes**. Bundled real Analytics polish (applies in live mode too): Flywheel Activity chart redesign (on-bar numbers, wider bars, translucent goal track, nested panel, fixed tooltip), new **Stage Health** card (normalized completion rate + Leading/Needs), chart recolor (emerald/blue/violet off the murky gold/amber), color-coded summary cards (KPI Health R→Y→G; Best/Needs tinted to stage color). `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #274 `feat/dm-attachments` | **DM media attachments — dock at parity with the Forum.** `CLAUDE:` the floating chat dock was text-only for v1; per user it now supports image/video/audio. DMs live in the same `chat_messages` table (which already has the `attachments` jsonb), so this reuses the forum pieces: `useChannelChat` selects + sends `attachments` (`send(text, attachments)`, attachment-only allowed); `DmChatWindow` mounts the forum `MediaPicker` above the composer + renders `AttachmentDisplay` in bubbles; upload via the existing `uploadForumFile` path (channel-keyed, works for DM channel_ids). **No DB change.** The Forum's in-page DM view (`?dm=`) already had attachments (it reuses the full forum pane). `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #272 `feat/flywheel-hub-widget` | **Flywheel — Hub widget converted to the ledger (last surface).** `CLAUDE:` the admin Dashboard/Hub `AdminFlywheelWidget` still ran on `member_kpis` defs + task aggregation. Rewrote it to read `get_flywheel_stage_summary` (all-time team counts): header = total events + active-stages/5; one bar per stage, length relative to the busiest; empty/loading/error states. Dropped the KPI-def + team-tasks queries and the now-unused imports (`useMemo`, `useAuth`, `normalizeLegacyStage`, `fetchTeamAssignedTasks`, `kpiKeys`/`fetchKPIDefinitions`/`fetchKPIEntries`, `StageKey`). No DB change (reuses the PR2 RPC). With this, **every flywheel surface is ledger-driven**. `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #271 `feat/flywheel-kpi-ledger` | **Flywheel PR3 — "Flywheel KPIs" panel ledger-derived; time toggle made real.** `CLAUDE:` the BusinessHealth "Flywheel KPIs" drill-down showed task-based % and its total/year/month/week/day toggle didn't filter anything. Now `timeFilter` computes a since/until window → `fetchFlywheelStageSummary(since, until)` (reuses PR2's `get_flywheel_stage_summary` RPC — **no DB change**). Stage cards show real event counts for the window; the selected-stage detail shows the count + the stage's share of total flywheel activity that period. Kept the task drilldown + descriptive targets; removed the dead `stats` var. **Completes the ledger-wiring arc** — activity feed + Flywheel Activity chart + member Flywheel Today widget + Flywheel KPIs panel all read real `flywheel_events`. `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #270 `feat/flywheel-aggregation` | **Flywheel PR2 — charts wired to the live ledger.** `CLAUDE:` new RPC `get_flywheel_stage_summary(p_since, p_until, p_member)` (migration `20260530180000_flywheel_stage_summary_rpc.sql`, applied to prod via MCP, validated by rollback) — per-stage `flywheel_events` counts, `team_id = get_my_team_id()` scoped, zero-filled to all 5 stages via a VALUES left join. Frontend: `fetchFlywheelStageSummary()` + `flywheelKeys.summary()` in `flywheelEvents.ts`; **BusinessHealth "Flywheel Activity"** bar chart now shows real events per stage over the page date range (replaced the done/open task-category stacked bars) with loading + empty states; member Overview **"Flywheel Today"** widget (`FlywheelSummaryWidget`) reads the caller's own ledger counts (replaced `buildMemberFlywheelChartData` proxies; also fixed an axis showing raw lowercase keys instead of labels). `database.ts` regenerated for the new RPC. `buildMemberFlywheelChartData` now unused (left for a cleanup PR). `tsc -b` + `vite build` clean. Next: PR3 (KPI % from the ledger). |
| 2026-05-30 | PR #269 `feat/flywheel-emit-coverage` | **Flywheel emit coverage across all 5 stages.** `CLAUDE:` Bridget gave a detailed per-stage taxonomy; most examples are *tasks*, so the keystone change is **`completeAssignedTask` now emits to the task's tagged `flywheel_stage`** (via `normalizeLegacyStage(category)`) instead of hardcoded `production` — untagged tasks emit nothing. Added hooks (additive, no DB change): `team_leads` insert→discovery, `artist_pipeline` insert→discovery, `team_maintenance_toggle` (check=true)→workflow, `deliverable_submissions` insert→production, `education_students` insert→education, and a **repeat_booking→retention** branch in `CreateBookingModal` (client with prior sessions; first session still →workflow `client_converted`). Reclassified `AddMedia` upload discovery→**workflow** (Dropbox pipeline, per Bridget). Extended `FlywheelSourceType` (lead/pipeline/deliverable/checklist/education_student) + `describeFlywheelEvent` copy. Deferred: member role/title-change→education (member-update path needs locating) and forum-post→education (emitting on every chat message = noise; Education is covered by education-tagged tasks + student enrollment). `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #268 `feat/flywheel-canonical-rename` | **Flywheel PR1a — canonical stage module + system-wide rename.** `CLAUDE:` Bridget showed the Assign picker + admin Flywheel Activity chart + Flywheel KPIs panel still saying Deliver/Capture/Share/Attract/Book — because the five stages were defined independently in 4+ modules (formAtoms `'Deliver'…`, tasks/shared `'deliver'…`, types/index, domain/memberOverview) plus per-component copies in AdminFlywheelWidget, BusinessHealth, KPIDashboard, MemberKPIPanel, WeeklyReviewForm. Introduced **`src/lib/flywheel/stages.ts`** as the single source of truth (keys discovery/workflow/production/education/**retention** + labels/colors/hex + `normalizeLegacyStage()`), and re-pointed every definition + consumer at it (the dupes now re-export for API stability). The 5th stage is **retention** (not "growth") — retention is the action that fuels growth (recurring clients, 5★ reviews, re-engaging past clients); migration `20260530170000_flywheel_growth_to_retention.sql` swaps the ledger CHECK + RPC. Data migrated on prod via MCP (two migrations, validated by rollback): `assigned_tasks`/`task_requests`/`task_template_items.category` and `member_kpis.flywheel_stage` re-keyed (Deliver→production, Capture→workflow, Share/Attract→discovery, Book→workflow; free-text like "Brand Knowledge"/"Training" left alone). Bookings count toward `workflow` on BusinessHealth (interim). Note: `weekly_admin_reviews.flywheel_scores` jsonb (historical) still uses old keys — deferred. `tsc -b` + `vite build` clean. Follow-ups: PR1b dedicated `flywheel_stage` column; PR2 ledger-driven charts; PR3 ledger-derived KPIs. |
| 2026-05-30 | PR #267 `feat/flywheel-phase2-lean` | **Flywheel Phase 2 (lean) — Team Activity widget reads the live ledger.** `CLAUDE:` "go with lean" → the leanest cut of Phase 2 (make the ledger visible) rather than the full BusinessHealth rebuild. `TeamActivityWidget` (Overview) dropped its hardcoded `MOCK_ACTIVITY` and now renders real `flywheel_events`. Added to `flywheelEvents.ts`: `fetchFlywheelActivity(limit)` — a direct client query (RLS team-scopes it; `member:team_members!flywheel_events_member_id_fkey(display_name)` embed for the actor) — plus `describeFlywheelEvent()` which maps (source_type, metadata) → a human sentence (task→"completed X", client→"added a new client", media_upload→"uploaded X", session→"booked a … session"/"converted a new client"). Stage pills + `STAGE_LABELS`/`STAGE_STYLES` updated to the new five (discovery=cyan, workflow=orange, production=blue, education=violet, growth=emerald). 60s poll; loading/error/empty states. **No migration, no RPC, no type change.** Deeper aggregation charts (BusinessHealth, AdminFlywheelWidget) deferred. `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #266 `feat/flywheel-rekey` | **Flywheel re-keyed to the refined 5-stage platform model (Phase 1.5).** `CLAUDE:` Bridget delivered a researched "big five" that fits the studio's growth loop better than the original deliver/capture/share/attract/book. New canonical stages (ledger `stage` keys): **discovery** · **workflow** · **production** · **education** · **growth**. Migration `20260530140000_flywheel_rekey_five_stage.sql` (applied to prod via MCP, validated via rollback harness — new stage accepted, old key rejected, team resolves; 0 rows to remap) swaps the CHECK constraint + `record_flywheel_event` IN-list; includes a defensive old→new remap UPDATE. Client: `FlywheelStage` union updated + 5 emit hooks re-pointed — `completeAssignedTask`→`production`, `CreateBookingModal` booking_created + client_converted→`workflow` (distinguished by `metadata.milestone`), `createClient`→`discovery`, `AddMedia` upload→`discovery` (content asset per Bridget). `education` + `growth` emit nothing yet — they need net-new sources (LMS/workshop/ticketing; CRM follow-ups/review-gen/referral codes/loyalty). Stub Flywheel activity widget on Overview still shows old labels — deferred to **Phase 2** (aggregation RPCs + wire BusinessHealth/AdminFlywheelWidget to the live ledger). `tsc -b` + `vite build` clean. |
| 2026-05-30 | PR #203 (rebased) `claude/flywheel-events-phase-1` | **Flywheel event ledger Phase 1 — unblocked + rebased onto main.** `CLAUDE:` the PR had sat ~59 commits behind main and was flagged CONFLICTING at every session start. Rebased onto current main; only 3 conflicts (docs ×2 + `AddMedia.tsx`). `flywheel_events` table + `record_flywheel_event(stage, source_type, source_id, member_id, metadata, occurred_at)` SECURITY-DEFINER RPC (resolves `team_id` via `get_my_team_id()`, append-only — no INSERT/UPDATE/DELETE policy) were already live on prod from PR-open, so **no DB apply / type regen needed** (types already present from the DM-PR regen). Client side: new `src/lib/queries/flywheelEvents.ts` `emitFlywheelEvent()` (fire-and-forget; swallows errors so an emit failure never regresses the user's action) + 5 emit hooks wired — **Deliver** (`completeAssignedTask`, on `isCompleted`), **Book** + **Capture** (`CreateBookingModal` create path: every new session = Book; client's first-ever session = Capture, via a count check), **Attract** (`createClient`), **Share** (`AddMedia` upload success — re-applied onto the current Dropbox-chunked path during the rebase). Phase 2 (aggregation RPCs + swapping BusinessHealth / AdminFlywheelWidget off their stub arrays onto this table) is still pending. `tsc -b` + `vite build` clean. |
| 2026-05-30 | (PR pending) `feat/dm-dock` | **Messenger-style persistent chat dock.** `CLAUDE:` per user — "I want the DM I'm actively messaging to be with me as I go page to page… message back and forth while on other pages, similar to FB Messenger." Built a global floating dock mounted in `Layout` (outside the routed `<Outlet/>`) so open conversations survive navigation. **No DB change** — pure frontend on the merged DM schema. New files: `DmDockContext.tsx` (open-thread list + minimized map, capped to 3 windows, mirrored to sessionStorage so reloads keep your chats), `useChannelChat.ts` (per-channel load + realtime INSERT/UPDATE/DELETE + optimistic text send — lean v1, no attachments/unfurl), `DmDock.tsx` (bottom-right expanded chat windows + collapsed avatar "heads" with unread badge). Header `MessagesBell` thread-click + `NewMessageDialog` create now call `openThread()` (dock) instead of navigating to `/content?dm=`. Dock windows mark-read on open + invalidate `dmKeys` so the bell badge clears. `tsc -b` + `vite build` clean. |
| 2026-05-30 | `f3996a7` (PR #263) `feat/direct-messages` | **Direct Messages — 1:1 + group DMs (header bell + Forum DM area).** `CLAUDE:` per user — "make a direct messaging system… accessible on the header and a DM area in the forums page." Modeled a DM as a **private channel** to reuse the entire existing chat stack (messages, read-tracking, attachments, edit/delete, realtime) rather than a parallel system. Migration `20260530120000_dm_channels.sql` (applied to prod via MCP at PR open): `chat_channels.kind` (`public`\|`dm`\|`group`); new `chat_channel_members` table (RLS-enabled); recursion-safe `is_channel_member()` / `is_private_channel()` SECURITY DEFINER helpers; RLS rewritten so private channels + their messages + member rows are visible **only to members** while public channels keep prior behavior; RPCs `find_or_create_dm(other)` (idempotent 1:1 dedup), `create_group_dm(members[], title)`, `get_dm_threads()` (threads + co-members + last message + unread); existing `get_channel_notifications()` / `mark_all_channels_read()` re-scoped to `kind='public'` so DMs don't double-count in the notifications bell; `chat_channels` + `chat_channel_members` added to the realtime publication. Frontend: `src/lib/queries/dms.ts` + `useDmThreads`/`useDmUnreadCount`; header `MessagesBell` (speech-bubble icon + unread badge, mirrors `NotificationsBell`) just left of the notifications bell in `Layout`; `NewMessageDialog` (teammate multi-select → 1:1 or group); Forum (`Content.tsx`) gains a **Direct Messages** sidebar section + `?dm=<channel_id>` deep-link that hydrates the existing message pane (public channel list filtered to `kind='public'`). RLS + RPCs validated pre-apply via rollback-transaction harness (member-only visibility, dedup, no recursion, public-channel regression — all green). Types regenerated from live schema (also closed a large stale-types drift gap). `tsc -b` + `vite build` clean. |
| 2026-05-20 | `7e3f639` (PR #220) | **Forum: edit + delete your own messages (admin can delete any).** `CLAUDE:` per user — "we need to be able to edit and or to delete our messages if we need to". Pre-PR `chat_messages` was INSERT-only + SELECT-only. New migration `20260520210000_chat_messages_edit_delete.sql` (applied to prod via MCP at PR open): ADDs `edited_at timestamptz NULL`; new policy `chat_messages_owner_update` (sender = auth.uid()); new policy `chat_messages_owner_or_admin_delete` (sender OR `is_team_admin()`). `ChatBubble` grows a hover-revealed `…` actions menu — Edit (own) + Delete (own + admin). Edit swaps the bubble for an inline textarea (⌘/Ctrl+Enter saves, Esc cancels), updates content + bumps edited_at; "(edited)" badge appears in the timestamp row with a precise-time tooltip. Delete uses a two-tap rose "Delete?" inline confirm + 4s auto-reset. Realtime sub on chat_messages extended from INSERT-only to also catch UPDATE + DELETE so other clients see the change live. RLS is the real authorization gate; UI hide/show is UX polish. |
| 2026-05-20 | `70e8b8d` (PR #219) | **Forum: admin can create new # channels from the sidebar.** `CLAUDE:` per user — "I will need to be able to add # channels into the forum. admin should be able to do this". Small `+` button next to the "Channels" header in `/content`, admin-only via `useAuth().isAdmin`. Clicks open a modal: name input + optional description. Auto-slugifies Discord-style (lowercase + alphanumerics + hyphens; trims leading `#`, drops apostrophes, 60 char cap). Live preview "Will appear as #music-production" + client-side uniqueness check against existing channels list. INSERT goes direct via supabase-js — `admins_can_insert_channels` RLS policy (added in 20260501000000) gates writes. Friendly error mapping (23505 → "already exists", 42501 → "Only admins can create channels"). On success: refreshes list + activates the new channel. No DB migration (both `chat_channels.name` and `.slug` already UNIQUE). |
| 2026-05-20 | `2dbef07` (PR #218) | **Forum: comprehensive media overhaul — linkify · audio · video fix · OG previews.** `CLAUDE:` addresses three user complaints + the Instagram-style preview ask in one PR. **(1) Linkify** — new `src/lib/forum/linkify.ts` (regex URL tokenizer, trims trailing punctuation) + `<LinkifiedText>` component. Replaces inert `{message.content}` so URLs render as gold clickable `<a target="_blank">`. **(2) Audio uploads** — added `audio/*` MIME types to `forum-media` bucket (migration `20260520190000`); extended `FORUM_AUDIO_MIME` allow-list + `FORUM_AUDIO_ACCEPT` for the picker; `MediaPicker` gets a Music-icon `+Audio` option; `AttachmentDisplay` renders `<audio controls>` with filename + download. Follow-up (migration `20260520195000`) bumped the bucket + client allow-list with legacy MIME aliases (audio/mp3, audio/x-m4a, audio/aac, audio/flac, audio/x-flac) AND switched all three picker `accept` attrs (image/video/audio) from strict MIME-only lists to wildcard + extensions (`audio/*,.mp3,.wav,…`) — fixes user report "my mp3s are all greyed out when I try to select tham" caused by macOS Finder's MIME database not tagging .mp3 as `audio/mpeg`. **(3) Video fallback** — extracted `<VideoAttachment>` listens for the native `error` event (browser fires this on codec mismatch — .MOV on Chrome, HEVC anywhere) and swaps to a "Browser can't play — Download" card instead of leaving a broken player visible. **(4) Link unfurl** — new `unfurl-link` edge function (verify_jwt:true, 8s timeout, 1.5MB body cap, SSRF guard against private IPs, User-Agent that passes typical bot filters, hand-rolled OG/Twitter/standard meta extractor). New `chat_link_previews` cache table (migration `20260520200000`, 7-day TTL, keyed by URL). `ChatAttachment.preview = {title, description, image, site_name}` field added. `MediaPicker.submitLink` calls `unfurlLink()` after add and patches the attachment; pending strip shows a "Fetching preview…" pill while in flight. `AttachmentDisplay.LinkPreviewCard` renders Instagram-style hero image + title + description + site name; falls back to compact icon card if no preview. YouTube/Vimeo/Loom still get iframe embeds (unchanged). |
| 2026-05-20 | `622a773` (PR #217) | **Media: chunked Dropbox uploads up to 10GB (direct browser → Dropbox).** `CLAUDE:` per user wanted "what is standard for dropbox" — settled on 10GB (Dropbox's mobile-app per-file ceiling) as the realistic browser limit before tabs get unstable. Rebuild architecture: edge function `upload-to-dropbox` reshaped to two actions — `action:'token'` returns a 4hr Dropbox access token to the browser; `action:'finalize'` (after browser drives the upload) commits the session, creates share link, inserts `media_submissions` row with the canonical server-computed path (browser can't tamper). New `src/lib/dropboxUpload.ts` client uploader uses `upload_session/start` + `append_v2` in 32MB chunks (Dropbox-required multiple of 4MB), 2 retries per chunk with backoff, AbortController cancellation, lazy `file.slice()` so browser memory stays low. `AddMedia.tsx` rewired around the new uploader with a real progress bar. Server-side `MAX_FINALIZE_BYTES` defense-in-depth cap also at 10GB so a malicious caller can't craft a row claiming size > 10GB. Edge function `upload-to-dropbox` v7 deployed via MCP. |
| 2026-05-20 | `9ddc56c` (PR #216) | **Media: bump Dropbox upload cap 50MB → 150MB (single-shot).** Brief intermediate step before #217's chunked rewrite. Bumped `MAX_FILE_BYTES` in the edge function + `AddMedia.tsx` to 150MB (Dropbox `/2/files/upload` single-shot endpoint hard limit). Edge function v5 deployed via MCP. Superseded the same day by #217's chunked-session architecture. |
| 2026-05-20 | `0fc80a6` (PR #215) | **Assign: Checklist tab in the Assign-page left rail.** `CLAUDE:` per user — "we need to add a 'checklist' section in the side menu on the assign page that links to the checklist widget". Refactored `AdminManageChecklistModal` body into a reusable `<TeamMaintenanceManager>` component so the same management UI renders inside the modal AND inline in the new sidebar tab. Both share `maintenanceKeys.list()` React Query cache + realtime sub on `TeamChecklistWidget`, so admin edits propagate live everywhere. `AssignAdmin` left rail gains a `ClipboardList`-icon Checklist tab between Studio and Templates; URL hash extended to support `#checklist`. View state union grows to `'member' | 'templates' | 'studio' | 'checklist'`. No DB/backend changes. |
| 2026-05-20 | `6c273dd` (PR #214) | **Checklist: fix `column "auth_user_id" does not exist` bug.** `CLAUDE:` user hit this on the +Add form. This schema has no `auth_user_id` column on `team_members` — `team_members.id` IS the `auth.users.id` directly. Fixed the lookup in `team_maintenance_toggle()` + `admin_team_maintenance_create()` RPCs to match on `id = auth.uid()`. Hotfix applied to prod via MCP at PR open; follow-up migration `20260520120000_team_maintenance_fix_member_lookup.sql` + in-place edit on the original migration captures the canonical RPC bodies. |
| 2026-05-20 | `4375de6` (PR #213) | **Workspace: cross-page widget drag finally works.** `CLAUDE:` four-iteration fix to user report "we need to be able to move widget carosel page to carosel page" + "its still not setting in where I dragged it". (1) Widened EdgeSensors from 64→112px + added chevron telegraph hints during drag so the user can see WHERE the drop zone is. (2) `handleDragEnd` learned to handle `drop-on-edge` — swaps with the LAST visible widget so the user lands on the new page they auto-paged to (previous code returned early on edge-id, leaving the widget snapped back). (3) Added `MeasuringStrategy.Always` to DndContext so droppable rects re-measure every frame instead of being cached at drag-start (cache was stale after carousel translated to new page). (4) Custom collision detection that prefers SortableWidget hits over EdgeSensor hits — the widened (112px) edge was stealing collisions from real target widgets near the edge. The combined fix gives the user: drag widget → hold near right edge → carousel pages over → drop on target widget on new page → widget lands. |
| 2026-05-20 | `467e49f` (PR #212) | **Tasks page: new team Maintenance Checklist widget.** `CLAUDE:` per user — "a 'Check List' This will be different from Tasks in that it is a list to ensure by the end of the day / week / month that these tasks are taken care of. It is a maintenance list... visible to the team for everyone to partake in ensuring the checklist gets looked at". Distinct from `assigned_tasks` semantically (tasks = "do this work"; checks = "verify this is still in good shape"). New tables `team_maintenance_items` (admin-curated list with cadence ∈ daily/weekly/monthly) + `team_maintenance_completions` (append-only log keyed by `period_key`). Period-bucket helper `team_maintenance_period_key()` returns `YYYY-MM-DD` (daily) / `YYYY-Www` (weekly ISO) / `YYYY-MM` (monthly) in Denver tz — no cron needed, "is this checked for current period?" computed per-fetch. RLS: SELECT for team; INSERT/UPDATE/DELETE items admin-only; INSERT/DELETE completions any team member. New `<TeamChecklistWidget>` on /daily (col 1 rs2, lands 2nd in carousel after My Tasks). Toggle is idempotent via `UNIQUE(item_id, period_key)`. New `<AdminManageChecklistModal>` opens from a +Checklist tile added to the admin Hub `AdminAssignWidget`. Also swapped Studio ↔ Team Tasks col positions per user direction. Both tables added to `supabase_realtime`. |
| 2026-05-20 | `f97580f` (PR #211) | **Recurring tasks: one active instance + "Yesterday" overdue badge (Apple Reminders model).** `CLAUDE:` per user — "recurring tasks to not keep repopulating, they should show a notifier underneath it of of it being a missed task. when the task is completed for the designated day, thats when it will populate as a recorded completed task". Migration `20260519180000_recurring_tasks_one_active.sql` (applied to prod via MCP): `spawn_recurring_task_instances()` learned to SKIP a template when it already has an active (uncompleted) child instance; new trigger `bump_recurrence_template_on_complete` bumps the parent's `recurrence_last_spawned_at` to now() on each child completion so the cadence math restarts from the completion moment (daily → next tomorrow; weekly → +7d; monthly → +1mo). New shared `formatOverdueLabel()` helper returns "Yesterday" / "N days ago" / past short date for any due_date in the past. MyTasksCard + AssignmentBoardBody rows prepend a rose-dot + label to the meta line when the task is incomplete + overdue. |
| 2026-05-19 | `b868952` (PR #206) | **Workspace: remember widget expansion across navigations for the session.** `CLAUDE:` per user — "we need to have the website keep memory of what how their widgets have been expanded and keep it that way for the duration of being logged in til log out". New `src/hooks/useSessionExpand.ts` — useState-shaped hook backed by sessionStorage, persists across page navigations + reloads, auto-clears on tab close. Keys namespaced per `(scope, userId)`. Wired into `WorkspacePanel` + `MemberActivitySection`. `AuthContext.signOut` calls `clearSessionExpandState()` for clean reset on logout. |
| 2026-05-19 | `56839b5` (PR #208) | **Routing: refresh stays on current page instead of bouncing to Overview.** `CLAUDE:` per user — "when we refresh the webpage can it refresh back to the page we are currently on instead of back to dashboard?". Root cause: `AuthContext` defers profile fetch via setTimeout(0), so `setLoading(false)` lands BEFORE profile arrives. During that ~10ms window appRole defaults to 'member', which fails capability checks on inner `<ProtectedRoute requiredCapabilities={['view_admin_app']}>` wrappers → `<Navigate to={overview} />` fires. Fix: ProtectedRoute keeps the spinner up while profile is still resolving AND a capability check is required. Owner special-cased (resolved from email alone). Member routes unchanged. |
| 2026-05-19 | `3f882b0` (PR #210) | **Errors: auto-recover from stale-chunk errors after a deploy.** `CLAUDE:` user hit this on PR #209 preview — every Vercel deploy replaces hashed chunk filenames; an already-loaded tab's cached index.html references chunks that no longer exist, lazy import 404s, ErrorBoundary shows "page crashed" until hard-refresh. Fix: ErrorBoundary detects chunk-load failure by message keywords ("Failed to fetch dynamically imported module", "Loading chunk", "ChunkLoadError", etc) and triggers one `window.location.reload()` to pull the fresh bundle. Guarded by 30s timestamp in sessionStorage so a persistent failure can't infinite-loop. |
| 2026-05-19 | `84461e7` (PR #209) | **Notifications: pill-ify the Open-in-forum CTA + drop buried text link.** `CLAUDE:` per user — "lets remove the tiny buried text link too, and can we try to make the new link a bit more obvious". Promoted the Open-in-forum control from a twin circular icon button to a pill with `ExternalLink` icon + "Open" label. Stronger hover (bg-violet-500/30 + text-white + 3px shadow ring). Dropped the redundant tiny "Open #channel" text-link inside the expanded reply form. |
| 2026-05-18 | `dfe34db` (PR #202) | **Forum: drop 'rose' from chat color palette + pin owner to gold.** Per user — "I am not wanting red as a user color option for the forum because it looks negative or alarming. Can we change the user color of checkmark to gold?". |
| 2026-05-18 | `(hotfix)` | **Richard's email typo fix — direct SQL via MCP, no PR.** Auth user had `richard.checkmark@gmail.com` (no "b") but team_members profile + screen showed `richardb.checkmark@gmail.com`. Updated `auth.users.email` + `auth.identities.identity_data.email` + `provider_id` to match the profile, reset password to a known temp (`Checkmark2026!`), kept `requires_password_change=true` so first login forces a fresh password. |
| 2026-05-17 | `d635b22` (PR #205) | **Task widget tweaks (3-pack): My Tasks reorder · sitewide Priority filter pill · admin reassign in Edit Tasks.** `CLAUDE:` per user direction — "Tasks tweaks: (1) make tasks re-arrangable under 'my tasks' widget, have it remember how you rearranged it for future sessions. (2) add 'priority' task tab under 'Studio tasks', 'my tasks' & Team Tasks where priority tasks will be filtered into there. (3) make Users changeable to already created tasks on admin side." User clarified (1) should persist via DB (cross-device), and (2) should reuse the existing `is_required` "priority" flag rather than introducing a new column. **(1) My Tasks reorder** — `MyTasksCard` wraps its Active task list in dnd-kit `DndContext` + `SortableContext` (verticalListSortingStrategy, PointerSensor with `distance: 5` so click-to-open-detail still fires on quick taps, KeyboardSensor for a11y). New `SortableAssignedTaskRow` wrapper exposes a small `<GripVertical>` handle pinned to the left edge of each row (opacity-0 → opacity-100 on `group-hover/sortable`, also visible when keyboard-focused). Drop fires `handleDragEnd` which computes the new visible-subset order via `arrayMove` then splices it back into the canonical saved order at the original visible positions — tasks that are filtered out (priority pill / source pill / completed eye off) keep their relative positions so toggling the filter back on reveals them in the same spot. Persisted via debounced (500 ms) write to a new `team_members.preferences.my_tasks_order` jsonb subkey using the existing `setUserPreference` helper from `src/lib/preferences.ts` (same dual-write pattern as `useWorkspaceLayout`). Initial hydrate reads from `profile.preferences.my_tasks_order` on mount + re-hydrates if the profile id flips. Sort logic in `openTasks` memo: saved order wins for known IDs; brand-new assignments not yet in saved order bubble to the TOP (high visibility for new work), then settle into a preferred spot after the user's first drag. Pending mid-flight tasks (transfer/delete/edit awaiting admin) stay below the divider in the dimmer non-draggable Pending section. No DB migration — the `preferences` column already exists from `20260513140000_team_members_preferences_column.sql`. **(2) Priority filter pill** — new shared `<PriorityToggle>` component in `src/components/tasks/shared.tsx` (gold pill with `<Flame>` icon + tabular count suffix; same visual rhythm as the existing `<SourcePill>` so all the filter affordances read as one family). Drops into the toolbar of three widgets: `MyTasksCard` (drives `team_tasks` widget on Overview + `/daily`) + `AssignmentBoardBody` (drives both `studio_tasks` + `team_board` widgets on `/daily`). Reads existing `is_required` boolean — no new column; the inline gold Flame icon that's been on priority rows since PR #94 reads the same field, so per-user instruction it's "switch on the priority tab to filter." Per-session state at the call site (no DB persistence — matches the show-completed eye's behavior so flipping it on doesn't surprise the user on next visit). Stacks cleanly with the existing source filter (Assigned vs Self) on MyTasksCard. Count suffix is computed against the current-view subset so it always reflects "priority within the current view" not the raw archive — admins can see at a glance whether toggling it on will narrow the list to zero rows. **(3) Admin reassign** — `AdminEditTasksModal`'s inline edit form now exposes an **Assigned to** dropdown picker right under Title. Parent modal loads team members via `fetchTeamMembers()` (filtered to active, sorted alphabetically by display_name) and threads them through as a new `assignableMembers` prop on `EditableRow`. State seeded from `task.assigned_to`, with an empty "— Unassigned —" option that clears the column (rare but valid for parking a task back in the unassigned pool, supported by the RPC's `p_clear_assigned_to`). Save handler dispatches `payload.assigned_to = nextAssignee` or `payload.clearAssignedTo = true` based on the picker's value. Studio-scope tasks render a small "assignee not applicable (any team member can complete)" hint instead — the server validation raises on studio rows so the UI matches. If the current assignee is missing from the active member list (deactivated mid-task), they surface as a fallback option labeled "(inactive)" so the admin sees what they're changing from. The RPC already supported `p_assigned_to` since 2026-05-07 — this PR is UI-only on top of an already-wired backend. No DB migration. `npm run build` verified — bundle delta minimal (dnd-kit was already loaded by `WorkspacePanel` so no new chunk). |
| 2026-05-17 | `73f9a3d` (PR #204) | **Calendar delete bookings + recurring scope picker (Sessions polish #1, admin-only).** `CLAUDE:` per user direction — "(1) we need to be able to delete Calendar bookings on calendar view. both on the modal and by right clicking there should be an option to delete. We need to also have the question ask 'this and all future events?' or 'this event only' for recurring events that are cancelled." Two new admin surfaces wired up on `/calendar`: (a) Delete pill in `BookingDetailModal` footer — admin-only via `useAuth().isAdmin`, sits to the LEFT of Close/Edit (`mr-auto`) per sitewide convention that destructive actions live on the far left so the gold primary action stays rightmost + most prominent; (b) right-click on any week-grid booking block opens a contextual menu (View details / Edit booking / Delete booking…). Both routes invoke the new `src/components/calendar/DeleteBookingDialog.tsx` which auto-detects recurrence shape on mount via the new `fetchSessionRecurrenceShape(sessionId)` helper (returns `{isTemplate, isChild}` from a `sessions` lookup of `recurrence_spec` + `recurrence_parent_id`; null on cross-team/deleted rows treated as non-recurring). Single bookings get a Cancel · Delete confirm; recurring bookings get a Google-Calendar-style scope picker with two `ScopeOption` radio buttons — "Just this event" (calls existing `adminDeleteSession`) vs "This and all future events" (calls new `adminDeleteFutureSessions`). **New migration `20260517230000_admin_delete_recurring_sessions.sql`** creates `admin_delete_future_sessions(p_session_id uuid, p_cancel_note text DEFAULT NULL)` RPC, applied to prod via MCP. Semantics: resolves `template_id` (self when `recurrence_spec IS NOT NULL`, else `recurrence_parent_id`), uses the clicked row's `session_date` as the starting cutoff, deletes every row in the series with `(id = template_id OR recurrence_parent_id = template_id) AND session_date >= starting_date`, AND explicitly `UPDATE sessions SET recurrence_spec = NULL WHERE id = template_id` first so a race against `spawn_recurring_session_instances()` between the DELETE and the next cron tick can't spawn fresh children of a dead series. Returns `jsonb` with `{deleted_count, deleted_google_event_ids[], cancel_note}` — googleIds stripped of NULLs in SQL so the client doesn't have to filter. Function is `SECURITY DEFINER` + `is_team_admin()` gated + team-scoped (cross-team uuid raises "session not found" instead of leaking). Degenerates safely on non-recurring rows (skips the template-nulling path, just deletes the single row, same return shape) so the client can always call this function without sniffing the recurrence shape first. **New `adminDeleteFutureSessions()` client helper** in `src/lib/queries/adminSessions.ts` mirrors the existing `adminDeleteSession()` API surface; sweeps each returned `google_event_id` through `deleteSessionEventFromGoogleCalendar()` in a best-effort loop and aggregates errors into a single `syncWarning` string ("3 Google Calendar events failed to delete. First error: ...") so the modal shows ONE banner instead of a stack of toasts when a long series has multiple Google events. **Calendar page wiring**: `bookingLabel(b)` helper formats a human label for the delete prompt ("Bridget · Tue May 20 · 2:00 PM"); `deleteTarget` + `contextMenu` state lives at the page level so a single `DeleteBookingDialog` instance handles deletes from both surfaces; `onDelete` on `BookingDetailModal` closes the detail modal + opens the delete dialog so the two never visually overlap; context menu closes on outside-click + Escape via `useEffect`. Success path: parent invalidates calendar queries via existing refresh hooks + shows a success toast (`"Deleted N booking(s)"` + sync-warning subtitle if present). `npm run build` verified clean. **Queued (Sessions polish #2)**: add Calendar view to the booking page by restructuring `/sessions` around a Settings/Assign-style left-side rail with Bookings · Calendar · Clients tabs — user wants a preview first before deciding whether to fully merge Calendar into the Bookings menu item. |
| 2026-05-17 | `df9336e` | **Nav rename — "Add Media" → "Media".** `CLAUDE:` per user direction — "lets change 'Add Media' to 'Media' on the header bar." Three coordinated string updates: (a) `src/components/nav/TopNavBar.tsx` tab label, (b) `src/pages/AddMedia.tsx` page H1 title, (c) `<title>` updated by the page-title hook so the browser tab also reads "Media". Route path stays `/add-media` to avoid breaking deep-links/back-button history; only the user-facing chrome changed. Direct-to-main per visual-tweak convention. `npm run build` verified. |
| 2026-05-17 | `(PR #201)` | **Sessions/Bookings full-table CSV/PDF export on `/sessions` page (admin-only).** `CLAUDE:` ninth admin export surface — completes the export pipeline. New `src/lib/columns/adminSessionColumns.tsx` over the full `AdminSession` shape: Date, Start Time, End Time, Duration (minutes), Duration, Client, Type, Room, Status, Assigned To, Notes, Google Synced, Created At. Deliberately distinct from `sessionColumns.tsx` (which is the narrower `RecentSessionInfo` shape used by the Activity widget) — different shapes, different consumers, intentionally separate modules. `/sessions` page gets an admin-only `useQuery` for `fetchAllSessions({ includePast: true })` (the `admin_list_all_sessions` RPC, member-inaccessible — gated via `enabled: isAdmin === true` so members never trigger it; cached separately from the member-side `loadSessionsWindow` query so neither clobbers the other). `<ExportButtons>` drops into the existing Bookings-only sub-row immediately before the Manage Bookings + Book a Session buttons, only when isAdmin. Filename `bookings-<yyyy-mm-dd>.csv`; PDF title "Bookings". Smart formatters: `session_date` parsed with `T00:00:00` suffix to avoid evening-Mountain-Time UTC drift (per `c6c5bf6`'s lesson); `start_time`/`end_time` rendered as 12-hour clock; Duration as raw minutes + humanized "Xh Ym"; Status / session_type capitalized as enum → human label; Google Synced as Yes/No. Single query call, no separate edge function — just consumes the existing admin RPC that was already in place. `npm run build` verified. |
| 2026-05-17 | `8d9c786` (PR #200) | **Inline 2× widget expansion extended to Overview + Hub (`WorkspacePanel`) + shared `packPagedWidgets` helper.** `CLAUDE:` per user direction — "I love what you did, lets do it to overview and to dashboard (hub) too, I want to see how it goes." (1) Extracted the carousel slot-packing algorithm from PR #199's inline implementation into a new shared module `src/lib/carousel/packPagedWidgets.ts` — generic over the widget-id type, supports variable page sizes (1/2/3 widgets per page depending on viewport for WorkspacePanel; fixed 2 for MemberActivitySection). Exports `packPagedWidgets()`, `packedTotalPages()`, `packedPageForWidget()` helpers. (2) `WorkspacePanel` rebuilt around `packedLayout = packPagedWidgets(orderedIds, weightFn, pageSize)` — same spacer-placeholder technique keeps the carousel page-snap clean when an expanded widget would otherwise straddle a page boundary. Expanded widget width = `calc(2 * normalWidth + PAGE_GAP_PX)` so it spans exactly the visual footprint of two normal widgets sitting side by side. Uses the existing `DashboardWidgetFrame.isExpanded` prop (added in PR #199) so the chevron icon swaps Maximize2 ↔ Minimize2 on Overview + Hub too. (3) Edge cases: auto-collapse on drag-start (a 2-slot widget mid-drag would orphan its spacer placeholder); auto-collapse on viewport-driven pageSize change (a 2-slot widget doesn't make sense on a 1-up phone page); auto-collapse when the currently expanded widget gets hidden via Workspace Controls; expand chevron is suppressed entirely on phone (pageSize=1) since "expand to 2 slots" has nothing to expand into. (4) Same smooth `flex-basis 320ms cubic-bezier(0.16, 1, 0.3, 1)` width transition + matching carousel translate easing — expand grow + page-scroll land together as one motion (raised from the previous 280ms to 320ms in WorkspacePanel for parity with PR #199). (5) Same subtle gold-tinged ring + soft drop-shadow on the expanded widget. (6) `FloatingDetailModal` import + usage dropped from `WorkspacePanel`. (7) `MemberActivitySection` refactored to consume the shared helper too — zero behavior change there, just DRY cleanup so any future tweak to the algorithm propagates to both surfaces. LEARNING_LOG updated with two new entries: "Extracting a reusable algorithm into a shared module" + a "degenerate page size" edge-case note appended to the page-packing entry. `npm run build` verified. |
| 2026-05-17 | `33a2a58` (PR #199) | **Inline 2× expand on the Member Activity widget carousel — replaces FloatingDetailModal with a fluid inline expansion.** `CLAUDE:` per user direction — "instead of having it open a modal, can we make that extender make the widget the size of itself x2? so when you extend it, it will widen up and push the other widgets over on the carosel. can you try that out and plan it so that it is fluid, fun to use and intuitive?" Added optional `isExpanded?: boolean` prop to `DashboardWidgetFrame` (chevron swaps Maximize2 ↔ Minimize2, aria-label flips Expand ↔ Collapse). Rebuilt `MemberActivitySection.ActivityCarousel` around a `packWidgets(order, expandedId)` slot-packing algorithm with invisible spacer placeholders to keep the carousel page-snap clean when an expanded widget would otherwise span a page boundary. Smooth 320ms flex-basis transition + matching translate easing, subtle gold ring + drop-shadow on the expanded widget. Auto-navigate to the expanded widget's page; auto-collapse on member change + drag-start. PR #200 generalized the algorithm into a shared `packPagedWidgets` helper and extended it to Overview + Hub. |
| 2026-05-17 | `0128c5f` (PR #198) | **Member Activity widgets (Session History · Task Completion · Clock Data) now ship CSV/PDF exports.** `CLAUDE:` per user direction — "we need to make csv file production possible for the session history and task completion widgets that are on the activity page under members, using the same coding structure, and also if you can put the pdf and csv link to the previous clock in data spreadsheet we worked on earlier into the clock in widget on the member activity page as well." (1) Extracted the existing inline `clockColumns` array + `parseClockNotes` + helpers from `ClockDataSection.tsx` into new `src/lib/columns/clockColumns.tsx` — both the main Members > Clock Data tab AND the Activity-page Clock widget now consume it, so renaming a header in `clockColumns.tsx` propagates to both surfaces AND the CSV/PDF from both surfaces in lockstep. (2) New `src/lib/columns/sessionColumns.tsx` over `RecentSessionInfo` shape (Date, Start Time, Title, Room, Status). (3) New `src/lib/columns/memberTaskCompletionColumns.tsx` over `RecentTaskInfo` shape (Title, Completed At, Completed (relative)) — deliberately distinct from `taskExportColumns` since widget data is a narrow summary not the full `AssignedTask`. (4) `MemberActivitySection.tsx` now renders `<ExportButtons>` as a small right-aligned strip at the top of each of the three widget bodies (Session History, Task Completion, Clock Data); filename + PDF title both include the selected member's name via a new `exportNames()` helper. `WidgetBody` dispatcher signature changed from `memberId: string` to `member: TeamMember` so each body has access to display_name for the export filename. Eight admin export surfaces now live total: Members, Clock Data tab, AssignAdmin per-member tasks, Studio Tasks, BusinessHealth all-team history, plus the three new activity widgets. `npm run build` verified — initial bundle unchanged (jsPDF already code-split). |
| 2026-05-17 | `eb97208` (PR #197) | **Tasks descriptor refactor — `TaskRow` + `StudioTaskRow` display cells now driven by shared `taskExportColumns` via new `<TaskDisplayCells>` helper. Full drift-proofing across visible task rows AND CSV/PDF.** `CLAUDE:` resolved user's question "did we drift-proof the tasks pages the same as Members?" The honest pre-PR answer was "row content was duplicated — three task surfaces shared one CSV column array, but the visible task rows had their own hardcoded JSX." User pushed for the proper fix even though the original recommendation was "leave as-is, PR scope is large." This PR ships it. (1) Renamed `src/lib/columns/taskColumns.ts` → `.tsx` to allow JSX in `render` functions. (2) Added `render` to the three display-cell descriptors (`title`, `due_date`, `recurrence`) alongside their existing `exportValue` functions; the other descriptors stay export-only (description, scope, studio_space, assigned_to, category, required, completed, completed_at, created_at, source) because those fields don't show in the visible row. (3) New `src/components/tasks/TaskDisplayCells.tsx` iterates `visibleColumns(taskExportColumns)` and renders the display cells inline; null returns from individual descriptors (e.g. `render` returns null when `t.due_date` is missing) collapse cleanly so layout stays tight. (4) `TaskRow` in AssignAdmin + `StudioTaskRow` in StudioTasksPane both now use `<TaskDisplayCells task={task} />` for the visible display half; both keep their hardcoded interactive controls (completion checkbox, edit/delete buttons, confirm-delete pill) because those carry row-level state (`selectMode`, `isSelected`, `confirmDelete`, mutation callbacks) that doesn't belong in descriptors. **The "narrow descriptor + fat interactive shell" pattern**: descriptors own data display + export; row components own interaction. Documented as a new entry in `docs/LEARNING_LOG.md`. **Acceptable UX side-effect**: `TaskRow` in AssignAdmin previously didn't show the recurrence pill (StudioTaskRow did); now both show it when present — same data was always in the CSV, so visible parity is the win. **Effect proven**: rename "Title" → "Task Name" once in `taskColumns.tsx` and the visible task rows in BOTH AssignAdmin AND StudioTasksPane update, AND the CSV column header updates, AND the BusinessHealth all-team CSV header updates. Single source of truth across five surfaces. `npm run build` verified — bundle unchanged. |
| 2026-05-17 | `55ce0e6` (PR #196) | **BusinessHealth all-team task history export — date range + presets + Refresh all wired live.** `CLAUDE:` replaced the page's original stub "Export PDF" + "Print" buttons (placeholders since the page was first built) with a real `<ExportButtons>` fed by the shared `taskExportColumns` from PR #195. Bonus wires that had been stubs until this PR: (a) **preset pills** (Today / Week / Month / Quarter / Year / All Time) now set `from`/`to` via a new `presetToRange()` helper using Denver-local date math (Monday-start weeks to match studio cadence; `localDateKey()` for evening-timezone safety per `c6c5bf6`'s lesson); (b) **Refresh button** now invalidates the `team-assigned-tasks` query so a stale cache doesn't hide recent completions; (c) **date range filter** now drives the export — a task qualifies if EITHER its `completed_at` OR its `created_at` falls in the range (gives "task activity in this window" rather than just "created in window," which matches retrospective-report intent). "Showing N of M team tasks · range from → to" strip sits above ExportButtons for immediate visibility into how the filter is biting. Filename + PDF title incorporate the active range (`all-team-tasks-2026-05-01-to-2026-05-31.csv`, or `all-team-tasks-all-time.csv` for unfiltered). `npm run build` verified — bundle delta ~90 bytes. |
| 2026-05-17 | `b051c65` (PR #195) | **Tasks export — per-member + Studio Tasks panes wired via shared `taskExportColumns`.** `CLAUDE:` added `src/lib/columns/taskColumns.ts` exporting a shared `TableColumn<AssignedTask>[]` covering Title, Description, Scope, Studio Space, Assigned To, Category, Due Date, Required, Completed, Completed At, Created At, Source, Recurrence. Today every descriptor is export-only (no `render`) — AssignAdmin's per-member task pane and `StudioTasksPane` both use rich custom row components (`TaskRow`, `StudioTaskRow`) rather than a basic `<table>`, so the visible UI and the export schema intentionally differ. The shared module proves the `TableColumn<T>` pattern scales across multiple surfaces that want the same export schema — when a future surface adds a structured table view, each descriptor just gets a `render` function and the same array drives both surfaces (the canonical drift-proof pattern). Wired on (a) `AssignAdmin` per-member task pane — `<ExportButtons>` sits next to the Add Task button, filename + PDF title include the selected member's name (`tasks-Bridget Reinhard-2026-05-17.csv`), exports the currently-loaded tasks array which mirrors what the admin sees; and (b) `StudioTasksPane` — `<ExportButtons>` in the action bar, `studio-tasks` filename. Helpful column transforms: ISO timestamps formatted to readable strings for Completed At / Created At; booleans render as Yes/No; recurrence_spec renders as `weekly` / `every 2 daily`; enum-like fields (Source, Scope) get human labels. Recent stack: `8886610` add LEARNING_LOG.md; `2ead2eb` ExportButtons + drift-proof TableColumn<T> (PR #194); `17aec00` Calendar reconnect guard; `a4fd2dd` Calendar outbound recovery; `c6c5bf6` Calendar local-date key fix. `npm run build` verified — initial bundle unchanged (jsPDF already code-split). |
| 2026-05-17 | `8886610` | **Added `docs/LEARNING_LOG.md`** as user-facing personal dev-concepts reference. `CLAUDE:` user asked for a running record of development terminology and methodology they pick up while working on Dashboard-V3. Each entry follows a consistent shape — TL;DR, when encountered, why it matters, mental model. Seeded with today's PR #194 lessons: the GitHub Desktop loop they just executed end-to-end (fetch → switch → edit → save → diff → commit → push), Git concepts (branches, PRs, merge conflicts, worktrees, detached HEAD, squash merge), Vercel concepts (production vs preview), architecture concepts (drift-proofing, DRY, schema migration, parse-on-display), and code concepts (code splitting, lazy import, bundle size, CSV format, build, TypeScript strict mode). Doc includes "Adding new entries" convention at the bottom so future sessions know the user can say "add this to my learning log" and Claude will append at the top following the same shape. Direct-to-main per docs-only convention. |
| 2026-05-16 | `2ead2eb` (PR #194) | **Reusable `<ExportButtons />` (CSV + PDF) + drift-proof `TableColumn<T>` descriptors + wired on Members + Clock Data.** `CLAUDE:` added `src/components/ui/ExportButtons.tsx` as the canonical export-row pattern for admin tables. CSV path: synchronous, dependency-free, RFC 4180 escaping, UTF-8 BOM so Excel auto-detects encoding, CRLF line endings. PDF path: lazy-imports `jspdf` (4.2.1) + `jspdf-autotable` (5.0.7) on first click — bundle stays out of the initial chunk (~390KB jsPDF + 31KB autotable + 202KB html2canvas all code-split, only loaded by admins who actually click Export PDF). Branded header (gold underline + brand-gold fill on table head), page-number footer, auto-orientation landscape letter. **Drift-proof column descriptors**: new `src/components/ui/tableColumn.ts` defines `TableColumn<T> = { key, header, render?, exportValue?, thClassName?, tdClassName?, pdfWidth? }` with helpers `visibleColumns()` + `toExportColumns()`. Every admin table now declares ONE column array — visible `<thead>`/`<td>` cells AND the `<ExportButtons>` columns are both derived from it, so renaming a header or changing a cell transformation in the array updates BOTH surfaces in lockstep. Three column shapes supported: both surfaces (render + exportValue), visible-only (render only — e.g. combined Member avatar+name+email cell, Actions menu), export-only (exportValue only — e.g. Name, Email, Phone broken out as separate CSV columns even though the visible table collapses them). Wired on (a) `TeamManager` Members roster — exports the currently-filtered rows so search + position + status filters all flow through; visible Member cell + export-only Name/Email/Phone columns; and (b) `ClockDataSection` Shift history — visible humanized Duration + export-only raw `Duration (minutes)` for payroll formulas; respects the member-filter dropdown. Also includes a `parseClockNotes()` helper that splits the single flat `notes` column (stored as `"Went well: ... \n\nTo improve: ..."` by `SelfReportModal`) into two display columns (`Went Well`, `To Improve`) — read-only at render/export time, zero DB migration, with a legacy-format fallback that keeps unrecognized notes visible in the Went Well column. **Drift-proofing was end-to-end-proven by the user**: their own two GitHub Desktop commits (`3ec4fb5` clock name change, `0214ff4` Clock In capitalization fix) renamed the two header strings in `ClockDataSection.tsx`, and a fresh CSV download from the Vercel preview reflected the new names exactly — single source of truth verified. Component API: `<ExportButtons<T> filename columns={ExportColumn<T>[]} rows={T[]} title? pdfSubtitle? formats?={['csv','pdf']} disabled? />` — re-exported from `src/components/ui/index.ts` alongside `TableColumn`, `visibleColumns`, `toExportColumns`. Disabled state when rows=0 surfaces a tooltip. Follow-up PRs wire Sessions/Bookings (needs admin-table surface), Tasks history (AssignAdmin/BusinessHealth), and Forum activity (post-`chat_*` schema) — all using the same `TableColumn<T>` pattern. **Deferred per user**: KPI graph export — "not yet completed enough to define parameters." `npm run build` verified — TeamManager bundle grew ~230 bytes from the refactor (rounding noise). |
| 2026-05-16 | `17aec00` | **Calendar reconnect guard.** `CODEX:` user tried "Push pending bookings" while the stored Google token was already showing "Token has been expired or revoked." The button correctly failed server-side, but the UI showed a generic edge-function toast and did not guide the owner to reconnect first. Fix: `src/lib/googleCalendar.ts` now uses `extractEdgeFunctionError()` so function JSON errors surface directly; Settings → Database detects expired/revoked/`invalid_grant` token messages, shows a Reconnect Google Calendar call-to-action, and disables Push/Pull until reconnect. `npm run build` verified. |
| 2026-05-16 | `a4fd2dd` | **Calendar outbound recovery.** `CODEX:` investigated why the new Bridget May 16 booking did not appear in Apple Calendar. Production row showed `google_sync_status='error'`, `google_sync_error='Token has been expired or revoked.'`, and `google_event_id IS NULL`, so Apple had nothing to mirror from Google. Added Settings → Database outbound health (`outbound_pending_count`, `outbound_last_error`) and a "Push pending bookings" action that calls new `google-calendar-sync` action `retry_pending_sessions`. The action is admin/owner-only, scoped by `team_id`, and only upserts non-cancelled sessions from Denver-local today forward whose Google event is missing or errored. It does not mutate Checkmark booking content or delete sessions. `SUPABASE-DEPLOYED:` deployed `google-calendar-auth` and `google-calendar-sync` to project `ncljfjdcyswoeitsooty`. `npm run build` verified; local `deno check` unavailable on this machine. |
| 2026-05-16 | `c6c5bf6` | **Calendar local-date key fix.** `CODEX:` fixed the week-view booking alignment bug where the visible day label could be one day behind the booking data in US evening timezones. Root cause: `getWeekDays()` kept the current evening clock time while generating each local day, then used `toISOString().split('T')[0]` for the internal key; in Mountain time, May 15 at night becomes `2026-05-16` UTC, so the May 16 booking rendered under the May 15 label. Fix: use shared `localDateKey()` for Calendar page keys, CalendarDayCard today/week/shift helpers, and CreateBookingModal's today default. `npm run build` verified. |
| 2026-05-15 | `d6210b4` | **Calendar Phase 2A team-scope hardening.** `CODEX:` audited the existing manual inbound-sync path and found the main Phase 2 UI/edge-function slice already exists. Hardened `google-calendar-sync` so helper `setSessionSyncState()` and the catch-path error persistence require both `session_id` and `team_id`, preventing even status/error writes from crossing team boundaries under service-role execution. `SUPABASE-DEPLOYED:` deployed `google-calendar-sync` to project `ncljfjdcyswoeitsooty`. `npm run build` verified. |
| 2026-05-14 | `e93f3b3` | **Credential hygiene.** `CODEX:` identified `checkmarkaudio-agent-936c9de96144.json` as a Google service account key export for `checkmark-media-uploader@checkmarkaudio-agent.iam.gserviceaccount.com`. It was never committed and is not referenced by tracked code. Added `.gitignore` rule `checkmarkaudio-agent-*.json`; app should use Supabase secret `GOOGLE_SERVICE_ACCOUNT_JSON`, not repo-stored key files. |
| 2026-05-14 | `000d1d4` / `372b8fd` / `d142a3f` | **Member password/setup flow stabilized.** `CODEX:` normalized blank Supabase Auth token fields, fixed temp-password fallback identity writes, and separated the two password-change flows: `RecoveryGate` owns setup/recovery links and clears `requires_password_change`; `ForcePasswordChangeModal` only handles temp-password/admin-created first login. This prevents the double consecutive password reset modal. `npm run build` verified. |
| 2026-05-14 | PR #178 closed | **Archived stale SMTP reset PR.** `CODEX:` closed PR #178 (`feat/email-password-reset`) as stale because it was built around Supabase SMTP delivery and touched `Login`, `AccountAccessPanel`, and `TeamManager` with an older flow. Do not merge/reopen without rebasing against current main and re-reviewing `RecoveryGate`, `ForcePasswordChangeModal`, setup-link generation, and temp-password fallback. |
| 2026-05-15 | PR #192 (`f2a7747`) | **Backfill `task_request_cancelled` enum value on prod — cancel-task-request button fix.** User reported clicking CANCEL? on a pending task request errored with `invalid input value for enum assignment_notification_type: "task_request_cancelled"`. Root cause: PR #98's migration `20260503020000_task_request_cancel_and_status_cancelled.sql` was marked MCP-applied at the time but the `ALTER TYPE … ADD VALUE` portion silently didn't land in prod — Postgres can't ADD an enum value AND use it (in the `cancel_my_task_request` RPC body) in the same transaction. The CHECK constraint + RPC body landed; the enum value didn't. Verified 2026-05-15: prod's `assignment_notification_type` enum had 12 values, none of them `task_request_cancelled`. Fix: isolated single-statement migration that just runs the `ADD VALUE` (IF NOT EXISTS, idempotent). Applied directly to prod via MCP at PR open so the cancel button worked immediately; the file lives in the repo so fresh deploys pick it up too. No code changes needed — `cancel_my_task_request` already referenced the value, just needed the enum on prod to recognize it. **`CLAUDE:`** authored. |
| 2026-05-14 | PR #191 (`5065e23`) | **Docs catch-up + Studio Tasks nested-panel chrome + members-list-only accordion fold.** Three contained changes in one PR, no auth/onboarding overlap (the parallel session was working on member-access stabilization in parallel). **(1) Docs**: PROJECT_STATE "Latest commit" + "Currently active" cells refreshed (was stuck on PR #152 calendar work) + Timeline entries for PRs #179, #182, #185, #190; SESSION_CONTEXT got the Codex → Claude SMTP-onboarding handoff entry (later marked deferred by the parallel session). **(2) Studio Tasks pane chrome**: the members tab in AssignAdmin already used inset-panel + divide-y divide-theme + flat-row pattern (PR #176); the Studio Tasks pane was the holdout still using `ul.space-y-0.5` + per-row `rounded-xl`. Refit: each room section's task list wrapped in `inset-panel`, `ul` switched to `divide-y divide-theme`, `StudioTaskRow` drops its own `rounded-xl` chrome (hover + select tints still apply). Same pattern as MyTasksCard, Activity widgets, NotificationsPanel. Honors user direction "lets nest the tasks like we nested tasks in widgets to keep things consistent." **(3) Members-list-only accordion fold**: PR #176's rail-wide collapse (260px → 64px shrinking the whole sidebar to icons) was too aggressive — the user only wanted the member NAMES to fold while Studio / Templates / Other tabs stay full-size. Refactor: sidebar stays at 260px always; Members section header has a ChevronUp/Down toggle that hides only the `<ul>` of names; all `railCollapsed` conditionals stripped from the Studio/Templates/Other tabs (drop the icon-only branches). Main-pane prev/next chevrons stay tied to the same state so admins can step through members when the list is folded. State + preference key renamed: `railCollapsed` → `membersListCollapsed`, `assign_rail_collapsed` → `assign_members_list_collapsed` (old key still read once for back-compat). **`CLAUDE:`** authored. |
| 2026-05-14 | PR #190 (`b79a483`) | **Add Media — drag-drop uploads to Dropbox per-member folders.** New top-nav tab (FolderUp icon) for all logged-in members → drag/drop to a branded dropzone → POSTed to new `upload-to-dropbox` edge function (OAuth refresh-token flow, Basic auth on `/oauth2/token`, multipart upload to `/2/files/upload`, best-effort share-link creation). Per-member subfolders auto-created at `/Apps/CHECKMARK MEDIA/<member>/<timestamp>_<filename>`. New `media_submissions` table (RLS: own-only for members, all for admins, owner-only delete) backs a per-member submission history table on the page with "Open in Dropbox" deep-links. **Pivoted from Google Drive mid-implementation** — Google Drive service-account uploads to personal Drive accounts deprecated in late 2024 ("Service Accounts do not have storage quota"); both replacements (Shared Drives, OAuth domain-wide delegation) require Workspace which we don't have. Owner has paid Dropbox; refresh tokens don't expire. Edge fn deployed as v1. Secrets: DROPBOX_APP_KEY / DROPBOX_APP_SECRET / DROPBOX_REFRESH_TOKEN. **Bug fix in same PR**: upload state hung at "Uploading…" forever because the useEffect's `cancelled` flag flipped before the response landed (effect re-ran on every `pending` change, fired its own cleanup); replaced with a `useRef` gate so each in-flight upload owns its state lifetime. **`CLAUDE:`** authored. |
| 2026-05-14 | PR #185 (`3ef0651`) | **Editable emails — Settings + Team Manager.** New owner-only `admin-update-email` edge function updates BOTH `auth.users.email` (login identity, via service-role admin API with `email_confirm: true`) and `team_members.email` (profile mirror) in one call. Two surfaces wired: (a) Settings → Account Access shows a hover-revealed pencil next to each email → inline editor (Enter saves, Escape cancels, ✓/X buttons), with optimistic update + rollback on error; (b) Team Manager → Edit Member dropped the `disabled` lock on the email field; submit handler routes through the edge function before the rest of the profile update so a dupe/invalid email aborts cleanly. **3 iterations to land**: v1 returned 404 for profile-only members (no auth.users row) → v2 added profile-only fallback; v2 returned 502 from `listUsers()` ("Database error finding users" — known GoTrue bug) → v3 swapped to direct SQL probes via `admin.schema('auth').from('users')` with try/catch fallback. New `extractEdgeFunctionError()` helper in `src/lib/edgeFunctionError.ts` reads `FunctionsHttpError.context` so the UI surfaces our `{ ok: false, error }` JSON instead of supabase-js's generic "non-2xx status code" string. **`CLAUDE:`** authored. |
| 2026-05-14 | PR #182 (`d3c59b1`) | **Activity widgets adopt canonical chrome.** Refit of the Members → Activity tab to match the rest of the site exactly. Each widget now uses: (a) `inset-panel` body chrome with the standard widget-frame title bar (drag grip · bold title · expand chevron), (b) `divide-y divide-theme` needle-thin row dividers (NotificationsPanel pattern), (c) colored circle icon chips per category (violet=sessions, gold=tasks, emerald=shifts), bold title + muted meta + tabular date pill on the right. Drag-reorder via dnd-kit (PointerSensor distance: 6, horizontalListSortingStrategy, swap on drop, ghost in DragOverlay — exact wiring lifted from WorkspacePanel); session-only state since this is transient drill-down, not a customizable dashboard. Click-the-title expands into a FloatingDetailModal at maxWidth 720. Carousel pages 2-at-a-time at 696px height (matches Overview's rowSpan: 2 widgets). Right-pane outer card chrome dropped when activeSection === 'activity' so widget-card chrome on each tile is the only border the eye sees (no nested cards). **Earlier passes in same session** (later replaced by canonical refit): #179 added the Activity left-rail section as a single full-pane block, then refit through several iterations on user feedback ("widgets nested" → un-nest; "make them this long" → 696px; "give them titles" → drop hideTitle). **`CLAUDE:`** authored. |
| 2026-05-14 | PR #179 (`9c39cb6`) | **Activity left-rail section in Members admin.** Originally shipped as a per-row drawer (clicking a member row in TeamManager opened a slide-over with sessions + tasks + clock history); user wanted it parked in nav instead, so refactored into a third left-rail section alongside Roster + Clock Data. Member-picker dropdown defaults to first active member alphabetically so picking Activity shows useful content immediately. Three configurable-limit query hooks added to `lib/queries/memberProfile.ts` (`useMemberAdminSessions`, `useMemberAdminCompletedTasks`, `useMemberClockEntries`) so the section can pull deeper history (20 rows) without clobbering the profile sidebar's hard-5 cap cache. **`CLAUDE:`** authored. |
| 2026-05-14 | Setup-link rescue in flight | **Employee login unblocker.** `CODEX:` added `admin-generate-setup-link`, deployed it to Supabase project `ncljfjdcyswoeitsooty`, and rewired Add Member / Account Access toward copyable setup links instead of SMTP-dependent reset email or default temp-password handoff. Verified with `npm run build`. |
| 2026-05-14 | Auth safety fix in flight | **Production login lockdown follow-up.** `CODEX:` tightened preview auto-login so it now requires both a Vercel branch-preview hostname and `VITE_DEPLOY_ENV === 'preview'` from the Vercel build environment. Production builds still strip `VITE_PREVIEW_LOGIN_*` credentials, and now the runtime path also refuses auto-login if those vars drift into the wrong scope. Verified with `npm run build` and `VERCEL_ENV=production npm run build`. After merge/deploy, return to Google Calendar Phase 2 inbound sync work. |
| 2026-05-14 | Calendar Phase 2 edge functions deployed | **Two-way calendar-sync beta hardening.** `CODEX:` updated `google-calendar-auth` / `google-calendar-sync` so owners as well as admins can manage calendar sync, removed an invalid Google Calendar extended-property list filter, preserved external title edits as `client_name`, and persisted inbound sync failures to Settings-visible connection fields. `REMOTE-SCHEMA-VERIFIED:` required Phase 2 columns exist on production despite migration-history drift. `SUPABASE-DEPLOYED:` both edge functions deployed to project `ncljfjdcyswoeitsooty`. |
| 2026-05-07 | PR #152 merged | **Google Calendar Phase 1 sync is live on production.** `CODEX:` built and patched the Google Calendar integration, including `google_event_id` booking mapping, outbound sync hooks, admin Settings UI, Phase 1 docs/runbooks, and OAuth callback fixes. `MANUAL-GOOGLE-CLOUD:` user created the OAuth client, rotated the exposed client secret, and confirmed `checkmarkaudio@gmail.com` as the connected Google account. `MANUAL-SUPABASE:` user applied the calendar SQL manually in the Supabase SQL Editor due migration-history drift, then updated secrets and redeployed `google-calendar-auth`. `LIVE-VERIFIED:` production Settings now shows `Google Calendar Sync`, connected account `checkmarkaudio@gmail.com`, calendar target `primary`, and a Disconnect button. Phase 1 operating rule locked: `Checkmark -> Google -> Apple`; no inbound Apple/Google edits yet. |
| 2026-05-07 | Phase 2 branch in progress | **Phase 2 safe-slice implementation started on a branch only.** `CODEX:` added a manual admin-triggered inbound sync path for **already-linked** Google events only, plus new audit/sync metadata, branch-only UI for `Pull inbound changes`, and docs/risk notes that explicitly preserve the live Phase 1 outbound contract. This beta is intentionally additive and is **not deployed to production yet**. |
| 2026-05-07 | Docs refresh | **Phase 2 planning opened.** Added/updated: `docs/google-calendar-phase1-ops-runbook.md`, `docs/google-calendar-phase2-sync-spec.md`, `docs/google-calendar-sync-setup.md`, and catch-up notes in state/context docs so future sessions know that outbound sync is live and that the next safe milestone is inbound sync for already-linked events only. |

| Date | Commit | What landed |
|--|--|--|
| 2026-05-06 | (in flight) | **Flat yellow buttons + transparent social icons — light-mode polish round.** Two follow-ups on PR #103. **(1) Flat bright yellow buttons.** Every `bg-gradient-to-b from-gold to-gold-muted` (12 sites: Layout Clock In, Sessions Book a Session + Add Client, Dashboard Book a Session, MultiTaskCreateModal Send, AddFromTemplateModal Add, AdminEditTasksModal Save changes, AdminEditSessionsModal Save changes, TaskReassignRequestModal Approve, AssignedTaskBoards Request to take, shared.tsx SubmitBar) flattened to `bg-gold hover:bg-gold-muted`. The darker-bottom gradient was dulling the bright yellow `#FFD60A` introduced in PR #103 rev5; flat reads punchier and matches the fintech reference. Marigold-tinted shadows (`rgba(214,170,55,...)`) replaced with neutral `rgba(0,0,0,0.08)` for a clean drop. **(2) Transparent social media icons.** `MemberHighlights.SocialStat` flipped from a solid 40×40 yellow tile + black glyph to TRANSPARENT background with the brand glyph rendered in `text-text` (deep contrast in both themes). The previous yellow square's hard edges cut into surrounding text per user report; transparent background + dark glyph integrates cleanly with the count text beside it. Hover surface stays clickable via a `-mx-2 -my-1 px-2 py-1` negative-margin/padding trick that extends the hit area past the visible glyph. The `SOCIAL_CHANNELS` hardcoded counts (Instagram 1240, TikTok 560, YouTube 95) preserved verbatim from PR #79 — no rework on the manual count entry. **`CLAUDE:`** authored. |
| 2026-05-04 | `cbbb4e2` (PR #103) | **Light-mode revamp — soft borders + grey body / white cards + fintech background wash + bright yellow gold token + contrast scan.** User reported the rev17 light mode was "horrible" — six iterations to land a clean fintech-style theme. **rev1**: borders `#1c1c22` black ink → `#e5e5e7` soft grey; body slightly warmer; cards lifted lighter; widget-card shadow bumped. **rev2**: flipped to grey body + WHITE cards per user reference image (Apple-tone Toggle demo), text bumped to pure black. **rev3**: added a multi-tonal background wash (warm cream/peach top-left → near-white center → cool grey bottom-right) on `body`. **rev4**: moved the wash from `body` onto `.dashboard-shell` so it shows up INSIDE the rounded page area (was hidden behind shell's flat bg). **rev5**: swapped `--color-gold` from marigold `#f0a623` to bright pure yellow `#FFD60A` — cascades to every `bg-gold` use across the app (buttons, accent badges, social tiles, etc.); rev10 token-split preserved (`text-gold` override stays at deep amber `#7a510f` for readable text on white). **rev6**: wash spread bumped from 36% radius to 75-90% ellipse so it actually covers the whole page area (not tiny corner patches); `MemberHighlights` social-media tiles flipped from black-bg/light-glyph to gold-bg/black-glyph by default (matches the fintech reference's bright-yellow CTA treatment). **Contrast scan** added between rev2 and rev3: closed the gaps in PR #79's rev17 accent-shade overrides — Tailwind's `/N` opacity modifier compiles a separate class (e.g., `.text-amber-300/80` ≠ `.text-amber-300`), so each opacity variant in actual codebase use now has its own selector → 700-tier in light mode. Also added overrides for missing tiers (`text-amber-100/500`, `text-orange-*` — entire family — `text-cyan-200/500`, `text-red-600`, `text-sky-500`) and all `text-gold/40-90` opacity variants. Dark mode untouched (its own gold + violet gradient stays). **Out of scope (deferred follow-up)**: more granular polish on social media tiles + any remaining muddy/orange CTAs the user calls out next round. **`CLAUDE:`** authored. |
| 2026-05-03 | `26225d8` (PR #102) | **Studio +Add modal + recurrence picker + full Members-pane chrome parity.** Iteration on PR #101's Studio tab UX based on user feedback (multi-pass: first the modal swap + recurrence; then full chrome parity with the Members pane — Save as Template, Select-mode bulk delete, Templates ▾ dropdown). **(1) Single top +Add button consistent with Members.** Removed the per-section inline +Add composer from `<StudioTasksPane />`; replaced with a single "+Add task" button in a title row beneath the action bar that opens the existing `MultiTaskCreateModal` pre-toggled to Studio scope (`initialScope='studio'`). Mirrors how the Members tab uses the same modal. **(2) StudioSpacePicker pills in studio mode.** `MultiTaskCreateModal`'s `<DraftRowCard />` swaps `FlywheelStagePicker` for a new `<StudioSpacePicker />` when `scope === 'studio'`. Pills: `[All]` `[Control Room]` `[Studio A]` `[Studio B]`. `[All]` maps to `studio_space=NULL` (the catch-all bucket; lands in the StudioTasksPane "(no space set)" section) — a single task can only be in one physical room, so `[All]` = "no specific room." Member rows still see the FlywheelStagePicker (unchanged). **(3) RecurrencePicker pills (studio scope only).** New `<RecurrencePicker />` in studio mode: `[None]` `[Daily]` `[Weekly]` `[Monthly]`. Saves as `recurrence_spec = {frequency, interval: 1}`. Member rows don't see this picker yet (column accepts member rows too — UI extension is a 1-line change later). **Backend** (`MCP-APPLIED:` migration `studio_task_recurrence_spec`, captured at `supabase/migrations/20260503040000_studio_task_recurrence_spec.sql`): ALTER TABLE `assigned_tasks` ADD COLUMN `recurrence_spec jsonb` NULL with shape CHECK enforcing `{frequency: 'daily'|'weekly'|'monthly', ...}`; `assign_custom_tasks_to_members` reads `recurrence_spec` from each task element on both branches; `get_studio_assigned_tasks` JSON projection adds `recurrence_spec`. `ADVISOR-VERIFIED:` 0 ERRORS, 75 WARN (unchanged — both RPCs replaced in-place with same signatures, no overload pollution). **Frontend types** ([assignments.ts](src/types/assignments.ts)): `AssignedTask.recurrence_spec?` field + new `AssignedTaskRecurrenceSpec` type (mirrors `task_requests.RecurrenceSpec` shape so the future scheduler can read both tables uniformly). [queries/assignments.ts](src/lib/queries/assignments.ts): `CustomTaskDraft.recurrence_spec?` field + normalizer extension. **UI**: `<MultiTaskCreateModal />` extended with `initialStudioSpace?` prop (currently passed as `null` from StudioTasksPane — the modal opens scope-toggled to Studio with no pre-selected room; the admin picks per row). Studio task rows in `StudioTasksPane` show a small gold `🔁 weekly` badge when recurrence is set. **(4) Full Members-pane chrome parity.** Action bar at the top: `Save as Template · Select` on the left, `Templates ▾` on the right. Title row underneath: `Studio Tasks` + `N tasks · M complete` + `Add Task` button — matches the Members pane's "All Tasks for {Member}" row exactly. **(5) `/daily` Studio Tasks widget now sections by room.** `StudioAssignedTasksCard` renders visible rows under per-room headers (Control Room · Studio A · Studio B) plus a top "(no space set)" backfill bucket when present. Empty sections are HIDDEN here (different from the admin pane) — the widget is space-constrained. Empty-state copy updated: was "Studio tasks need the scope migration before shared tasks can flow here" (stale message from before PR #101's migration shipped), now "Add room-tagged tasks from Assign → Studio." **(6) Modal click-target bug fix.** `FloatingDetailModal` was closing mid-edit when admin started a text-selection drag inside an input and released the mouse on the backdrop. Fixed by tracking `mouseDown` target — now only closes if BOTH `mouseDown` AND the click happened on the backdrop itself. Pattern applies to ANY modal using FloatingDetailModal (TaskDetailModal, MultiTaskCreateModal, RequestDetailModal, etc.) — single fix cascades across the whole modal system. **Save as Template** wires to a new local `<SaveAsStudioTemplateModal />` that calls `createTaskTemplate` + loops `addTaskTemplateItem` over the current studio tasks (template is scope-agnostic; can be re-applied back to studio or to any member). **Select** mode toggles row checkboxes from gold (completion) to rose (selection); the action bar morphs to `N selected · Select all · Delete · Cancel` with a two-step inline confirm on Delete (mirrors PR #84's pattern). Per-row Edit + Trash hide in select mode. **Templates ▾** dropdown lists the team's templates; clicking one fetches its items via `fetchTaskTemplateDetail` and pre-loads them as drafts in `MultiTaskCreateModal` (initialScope='studio') so the admin can review/edit (especially `studio_space`, which the template doesn't carry) before sending. New `initialDrafts?: CustomTaskDraft[]` prop on `MultiTaskCreateModal` powers the pre-load. **Out of scope (deferred follow-up)**: recurrence auto-recreate engine — today the spec is captured + displayed but no scheduler fires new task rows on the cadence; that lands in its own PR. **`CLAUDE:`** authored. |
| 2026-05-03 | `0d550f6` (PR #101) | **Studio tab on Assign + `studio_space` room sections.** New admin surface for managing studio-scope tasks by physical room, sitting alongside the Members + Templates tabs in the Assign page sidebar. **Backend** (`MCP-APPLIED:` migration `studio_space_for_studio_tasks` + paired `studio_space_drop_old_admin_update_overload`, captured at `supabase/migrations/20260503030000_studio_space_for_studio_tasks.sql`): (1) ALTER TABLE `assigned_tasks` ADD COLUMN `studio_space text` NULL. (2) CHECK constraint `assigned_tasks_studio_space_check` enforces it's NULL OR (`scope='studio'` AND `studio_space ∈ {'Control Room', 'Studio A', 'Studio B'}`) — guards against member tasks accidentally inheriting a room tag. (3) `assign_custom_tasks_to_members` extended: studio-scope branch reads `studio_space` from each task element via `NULLIF(t->>'studio_space','')`. (4) `get_studio_assigned_tasks` JSON projection adds `studio_space` and the ORDER BY now sorts by `studio_space NULLS FIRST` so the admin pane can group rows in stable order without an extra client-side sort. (5) `admin_update_assigned_task` adds `p_studio_space text DEFAULT NULL` + `p_clear_studio_space boolean DEFAULT false`; defensive raise if `p_studio_space` is set on a non-studio task. Old 8-param overload DROPPED in the paired cleanup migration so PostgREST never disambiguates (same pattern as PR #97's `approve_task_request` consolidation). `ADVISOR-VERIFIED:` 0 ERRORS, 75 WARN (+1 by-design from the new RPC signature). **Frontend types** ([adminTasks.ts](src/lib/queries/adminTasks.ts) + [assignments.ts](src/lib/queries/assignments.ts) + [types/assignments.ts](src/types/assignments.ts)): new `StudioSpace` type (`'Control Room' \| 'Studio A' \| 'Studio B'`) + `STUDIO_SPACES` const; `AssignedTask.studio_space?` field added to type + normalizer; `AdminUpdateTaskPayload` + `CustomTaskDraft` interfaces extended with `studio_space` / `clearStudioSpace`. **UI** — new self-contained component [`StudioTasksPane.tsx`](src/pages/admin/StudioTasksPane.tsx): fetches ALL studio-scope tasks via `fetchStudioAssignedTasks`, groups them by space into 3+1 sections (Control Room · Studio A · Studio B + optional "(no space set)" backfill section that appears at the TOP only when there are unbacked rows). Each section has a per-room +Add task inline composer (single-line title, ⌘/Ctrl+Enter to send, auto-attaches the section's room tag) and renders rows with completion checkbox + edit pencil + trash with two-step inline confirm (mirrors PR #84's pattern). Realtime subscription on `assigned_tasks` filtered to `scope=eq.studio` so cross-session edits land instantly. Local `<StudioTaskEditModal />` reuses the SingleTaskEditModal shape but adds a Room picker that shows the three studio spaces + "(no space set)" option. **AssignAdmin** ([AssignAdmin.tsx](src/pages/admin/AssignAdmin.tsx)): `view` state extended to `'member' \| 'templates' \| 'studio'`; URL hash mirrors the selection (`#studio` joins `#templates` and `#<memberId>`); new Studio sidebar tab between Members and Templates with the same chrome pattern (Building2 icon, gold-ring active state, ChevronRight indicator, "Open" / "Tasks by room" subtitle). **Out of scope (deferred)**: bringing `studio_space` sectioning to the `/daily` Studio Tasks widget (`AssignedTaskBoards.tsx`) — admin-create surface ships first, the consumer view follows once we know the column populates cleanly in production. Existing studio tasks created before this PR have `studio_space=NULL` and surface in the "(no space set)" section for backfill. **`CLAUDE:`** authored. |
| 2026-05-03 | `b31f4b7` (PR #100) | **Uniform pending symbols + green approve buttons + clean edit-diff container.** Tightening visual consistency across the request triad surfaces. Three changes: (1) **Sitewide kind icons** — admin queue + `RequestDetailModal` icon containers now use Minus / Edit2 / Plus to match the member-side pending-row badges (was Trash2 / Edit2 / UserCircle2). (2) **Unified approve buttons** — both `PendingTaskRequestsWidget` and `RequestDetailModal` now render every kind's approve trigger + confirm as the same emerald outline `Check Approve…` / `Check Confirm approve` (was kind-tinted: rose `Approve delete…`, orange `Apply edit…`, emerald `Approve…`). Approve is a positive/affirmative action regardless of what's being approved; the kind context is already conveyed by the request row's badge above. (3) **Clean edit-diff container** — the `EditDiff` (admin queue) and `EditDiffBlock` (modal) atoms drop their orange `bg-orange-500/[0.08] ring-1 ring-orange-500/25` chrome so the title-change row reads as flat text. Field labels (`Title:`, `Description:`, `Stage:`, `Due:`) and the Current → Proposed arrow stay orange as the kind signal. **Out of scope**: warning copy color (e.g. "the task will be deleted permanently") stays kind-coded for safety; CSP fixes + stale-bundle auto-reload (filed as a follow-up chip). **`CLAUDE:`** authored. |
| 2026-05-03 | `1ebaad3` (PR #99) | **Sitewide orange-for-edit + clickable admin queue rows + pending-row opacity fix.** Three follow-ups stitched after PR #98 sanity check. **(1)** The whole admin request row body in `PendingTaskRequestsWidget` is now a single `role="button"` container instead of just the title text — fixes a regression where `kind='create'` rows couldn't open the modal because the title `<button>` shrunk to 0px width inside a flex-wrap parent (badge label "New task" wider than "Edit"/"Delete"). Bigger click target as a side benefit. Inline approve/decline buttons sit in a separate row, unaffected. **(2)** Pending-row opacity moved off the row container onto the title text — `opacity-60` was being inherited multiplicatively by the badges, dimming them below the admin queue's badge brightness. Replaced with `text-text-muted` on the title + slightly lighter row bg (`bg-white/[0.025]` vs the active `bg-white/[0.018]`); badges now render at full saturation, matching the admin queue's visual weight. **(3)** Orange (was gold) is now the canonical edit color across all four request-triad surfaces: `PendingTaskRequestsWidget` (icon container, badge, EditDiff atom, approve confirm copy + buttons), `AdminApprovalLogWidget` ("Edited" tag + icon), `RequestDetailModal` (kind header chrome, EditDiffBlock atom, approve buttons, kind-aware confirm container bg), and `TaskDetailModal` (member-side Edit pill + composer tone — `ComposerShell` extended with `'orange'` tone alongside the existing `'gold'` and `'rose'`). The recurrence pill on `kind='create'` rows keeps its gold styling — unrelated to edit. **`CLAUDE:`** authored. |
| 2026-05-03 | `23d5cce` (PR #98) | **Request inspection modal + member cancel — polish round on the request triad (PR #97).** Two user-requested follow-ups stitched into one PR. **(1) Admin click-into-request modal.** New [`RequestDetailModal.tsx`](src/components/admin/assign/RequestDetailModal.tsx) opens when an admin clicks any pending request title in [PendingTaskRequestsWidget.tsx](src/components/admin/assign/PendingTaskRequestsWidget.tsx). Reuses `FloatingDetailModal` shell (same portal-to-document.body fix from PR #86 so it centers correctly inside transformed/backdrop-filtered ancestors). Body is kind-aware: kind='create' shows due / stage / recurrence / required as a meta grid; kind='edit' shows a larger `EditDiffBlock` Current → Proposed table (one row per changed field); kind='delete' shows reason or "No reason provided" italic. Common across kinds: requester name + when, kind badge (rose/gold/amber), and a description/reason section. Footer has Decline + Approve actions with their own two-step inline confirm (decline takes an optional note; approve shows the FlywheelStagePicker only on create). Modal owns its own approve / reject mutations + cache invalidation so it stays self-contained — could be opened from a different surface (notifications panel, member's history view) without changing parent state. The inline Approve/Decline buttons on the row stay too: one-click approval from the queue is faster, the modal is the "more details" path. **(2) Member cancel for pending task_requests.** New `cancel_my_task_request(p_request_id)` RPC + `'cancelled'` status on `task_requests_status_check`. RPC is caller-only (`requester_id = auth.uid()` enforced); requires `status='pending'`; sets `status='cancelled'` + `reviewer_id`/`reviewed_at` so the row appears in the resolution log. Notifies all team admins so a vanishing pending row isn't surprising. EXECUTE locked to authenticated. `admin_recent_approvals` extended to include `'cancelled'` rows in the WHERE clause + the JSON projection so the existing AdminApprovalLogWidget surfaces them. **MCP-APPLIED**: migration `task_request_cancel_and_status_cancelled` (captured at `supabase/migrations/20260503020000_task_request_cancel_and_status_cancelled.sql`). `ADVISOR-VERIFIED:` 0 ERRORS, 74 WARN (was 73 — +1 expected by-design from the new SECURITY DEFINER RPC). **Frontend cancel UX** mirrors PR #84's bulk-confirm pattern: hover-X button on each pending row → first click morphs to "Cancel request? [Cancel] [Keep]" inline pill → confirm fires `cancelMyTaskRequest`. Auto-resets after 4s. Wired in two places: (a) [MyTasksCard.tsx](src/components/tasks/MyTasksCard.tsx) `AssignedTaskRow` for `kind='delete'`/`'edit'` rows (carries the `requestId` via the extended `PendingMeta`); (b) `PendingCreateRequestRow` for `kind='create'` (the in-flight new-task request rows). Cancel button is HIDDEN for `kind='transfer'` rows since transfers live in the sibling `task_reassign_requests` table — a separate cancel flow ships with the next round. **AdminApprovalLogWidget** gains a "Cancelled" variant: muted Ban icon + uppercase "Cancelled" tag + 70% opacity, distinct from rejected (rose X) and approved-* (kind-specific). `RecentApprovalRow.status` type extended to `'approved' | 'rejected' | 'cancelled'`. **Verified**: type-check + build clean (2.74s); local preview only the expected no-auth Supabase fetch errors. **Out of scope**: cancel for `task_reassign_requests` (transfer kind); admin-side undo for an approved request (out of scope by design — once approved, the action is committed). **`CLAUDE:`** authored. |
| 2026-05-03 | `08dc7c3` (PR #97) | **Member request-to-edit flow — closes the Delete + Transfer + Edit triad.** Server already had `task_requests.kind='edit'` reserved (PR #83 schema) but `approve_task_request` raised `'unsupported'`; this PR fills it in end-to-end. **Backend** (`MCP-APPLIED:` migration `task_edit_request_rpcs`, captured at `supabase/migrations/20260503010000_task_edit_request_rpcs.sql`): (1) ALTER TABLE `task_requests` ADD COLUMN `proposed jsonb` + new CHECK enforcing `proposed IS NOT NULL` ⟺ `kind='edit'`. (2) New `submit_task_edit_request(p_task_id, p_proposed jsonb, p_reason text DEFAULT NULL)` RPC — SECURITY DEFINER, member-only (no admin gate), team-scoped lookup, `scope='member'` + `assigned_to=caller` required; filters proposed to allowed keys (`title`, `description`, `category`, `due_date`); per-key type validation (title cannot be cleared; due_date must be parseable YYYY-MM-DD); inserts `kind='edit'` row + notifies all team admins via `task_request_submitted`. EXECUTE locked to `authenticated`. (3) `approve_task_request` extended with `'edit'` branch: applies COALESCE/CASE updates to target task — absent key = unchanged, explicit null/empty = clear (description/category/due_date only). Defensive: raises if `target_task_id` was already deleted (P0002). Notification body kind-aware ("Your edit request was approved"). Returns `{updated_count}` alongside existing fields. (4) `get_pending_task_requests` + `get_my_task_requests` extended to surface `proposed`; pending also returns a `current` snapshot of the target's editable fields so the admin queue renders a Current → Proposed diff without a second round-trip per row. `ADVISOR-VERIFIED:` 0 ERRORS, 73 WARN (was 72 — one expected by-design `authenticated_security_definer_function_executable` from the new SECURITY DEFINER RPC). **Frontend helper** ([taskRequests.ts](src/lib/queries/taskRequests.ts)): new `ProposedTaskEdit` + `CurrentTaskSnapshot` types; `PendingTaskRequest` + `MyTaskRequest` interfaces gain `proposed`/`current`; new `submitTaskEditRequest(taskId, proposed, reason?)` helper. **UI**: (a) [TaskDetailModal.tsx](src/components/tasks/TaskDetailModal.tsx) — third gold pill `✎ Edit` (between Delete + Transfer); composer pre-fills current values in 4 inputs (title / description textarea / due date / flywheel stage select) + optional reason; client-side diff sends only changed fields; Send disabled when nothing changed; ⌘/Ctrl+Enter shortcut wired. State machine extended to `compose: null | 'transfer' | 'delete' | 'edit'`. (b) [PendingTaskRequestsWidget.tsx](src/components/admin/assign/PendingTaskRequestsWidget.tsx) — gold Edit2 icon + "Edit" badge on `kind='edit'` rows; new `<EditDiff />` atom renders a tight Current → Proposed diff block per changed field; approve confirm shows a gold "Apply edit…" CTA + descriptive line; toast copy is kind-aware (`Edit applied — "X" updated for Y.`). (c) [AdminApprovalLogWidget.tsx](src/components/admin/assign/AdminApprovalLogWidget.tsx) — gold Edit2 icon + "Edited" tag on approved-edit rows, distinct from approved-create (emerald check), approved-delete (rose Trash2), and rejected (rose X). (d) [MyTasksCard.tsx](src/components/tasks/MyTasksCard.tsx) — `PendingKind` type extended to `'transfer' | 'delete' | 'edit'`; pendingByTask now picks up edit-kind rows; pending row shows gold "Awaiting admin to edit" badge. **Verified**: type-check + build clean (3.12s); local preview no React errors (only the expected no-auth Supabase fetch errors). **`CLAUDE:`** authored. |
| 2026-05-03 | `d5a541d` (PR #96) | **Templates as inline sidebar tab — Assign page UX rework #2.** After scratching the 3-column rework (PR #95, closed unmerged), user redirected: Templates should behave like a member tab — clicking it swaps the main pane in place instead of routing to `/admin/template-library`. **Frontend** ([TemplateLibrary.tsx](src/pages/admin/TemplateLibrary.tsx)): new optional `embedded?: boolean` prop. When set, drops the full-page chrome (`PageHeader`, `useDocumentTitle`-style effect, `max-w-[1400px] mx-auto` wrapper) for a slim inline header bar (Layers icon + "Templates" + count badge + filled-tonal "+ New Template" on the right, separated by a hairline border-b); tightens the card grid from `grid-cols-2 md:grid-cols-3 lg:grid-cols-4` → `grid-cols-2 lg:grid-cols-3` so cards don't squish in the narrower Assign main pane. Same React Query cache key (`taskTemplateKeys.library`), same modals — embedded + standalone mounts stay in sync. **AssignAdmin** ([AssignAdmin.tsx](src/pages/admin/AssignAdmin.tsx)): replaces the Templates `<Link>` with a tab button. New `view: 'member' \| 'templates'` state; URL hash mirrors the selection (`#templates` or `#<memberId>`) so refresh + deep links stay put. New `selectMember(id)` helper auto-flips view back to `'member'` when a member tab is clicked. Templates-tab active state matches the member tabs visually (gold ring + chevron + bold label). Standalone `/admin/template-library` route still works for deep links. **Verified**: build clean (2.69s); preview at 1440×900 — Templates tab swaps main pane in place, gold ring on active, URL hash updates to `#templates`, standalone route still loads with own document title + PageHeader. **`CLAUDE:`** authored. |
| 2026-05-02 | `f717c1e` (PR #94) | **Removed empty "Settings for Tasks" button on the Assign page.** User: "we need to get rid of the empty 'settings for tasks' button next to 'save as templates'. just delete it." Single-file change in [AssignAdmin.tsx](src/pages/admin/AssignAdmin.tsx) — the placeholder button had no handler and no follow-up scope. **`CLAUDE:`** authored. |
| 2026-05-03 | `740667d` (PR #90) | **Assign delete reliability — Codex `codex/assign-delete-debug` branch.** Closes the user-reported "Assign tasks won't actually delete" bug by hardening backend + frontend against `assigned_tasks.team_id` drift. **Backend** (`MCP-APPLIED:` 3 migrations applied to production via `mcp__apply_migration` and verified before merge): (1) `assigned_tasks_team_id_guard.sql` — backfills NULL `team_id` from assignee or assignment-batch admin; adds `BEFORE INSERT OR UPDATE OF assigned_to, recipient_assignment_id, team_id` trigger calling `fill_assigned_task_team_id()` (SECURITY DEFINER, REVOKED from PUBLIC/anon/authenticated so only the trigger subsystem invokes it); fallback chain: assignee.team_id → batch_admin.team_id → `get_my_team_id()`; trigger short-circuits when `NEW.team_id IS NOT NULL`. (2) `admin_delete_assigned_tasks_return_ids.sql` — RPC now returns `{deleted_count, deleted_ids}`; adds NULL-team rescue branch (deletes legacy NULL-team rows when team ownership is provable via assignee or assignment-batch admin); team isolation preserved (cross-team rows with non-NULL team_id remain unreachable). (3) `cleanup_stuck_assign_test_tasks.sql` — one-time DELETE of three specific stuck test rows by title+due_date. **Frontend** ([adminTasks.ts](src/lib/queries/adminTasks.ts)): helper returns `{deleted_count, deleted_ids}` with safe defaults (`deleted_ids = []`) so the UI degrades gracefully if the new RPC shape isn't live. **UI** ([AssignAdmin.tsx](src/pages/admin/AssignAdmin.tsx)): mutation now passes `{requestedIds, result}`; optimistic remove only fires for confirmed-deleted ids; toast logic distinguishes 0 / partial / full deletes (no fake success); select mode resets only on full success. **`ADVISOR-VERIFIED:`** 0 ERRORS post-apply. **All 7 `INSERT INTO public.assigned_tasks` paths reviewed** — 6 don't set team_id explicitly (now covered by the trigger); 1 already does (`assign_rpcs_set_team_id`, unchanged behavior). Reviewed by **`CLAUDE:`** before merge with full edge-case scan and updated PR description to document the MCP application + verification. **Operational caveat for future sessions: Vercel green ≠ Supabase migrations applied. Apply migrations via MCP (or surface as a manual ops step in the PR description) before merging any PR with new migration files.** Authored by **Codex** (`codex/assign-delete-debug`); reviewed + applied + verified by **Claude**. |
| 2026-05-02 | `8dfa922` (PR #89) | **Sticky header fix — `.dashboard-shell` `overflow: hidden` → `overflow: clip`.** User reported the top header no longer floats and gets lost when scrolling. Diagnosis: `.dashboard-shell` has had `overflow: hidden` since 2026-04-18 (commit `ec4bbda4`). That puts the sticky `<header>` inside the shell's scroll context — but the shell itself never actually scrolls (it just clips), so the header has nothing to stick to and scrolls away with the page body. The bug has technically existed since April 18, but only became visible NOW because the pending section in PR #88 made the page tall enough to actually scroll on shorter viewports. Fix: changed to `overflow: clip` — same visual rounded-corner clipping but does NOT establish a scroll context, so the sticky header propagates through to the document scroll container as intended. Browser support: Chrome 90+ / Firefox 81+ / Safari 16+ (≥99% of users). **Verified in preview** at 1280×600: header at y=21 before scroll, sticks at y=0 after scrolling 491px down; `getComputedStyle(.dashboard-shell).overflow === 'clip'`. **`CLAUDE:`** authored. |
| 2026-05-02 | `4be99e3` (PR #88) | **MyTasksCard pending section — visual identifier for in-flight delete / transfer / new-task requests.** User: "we need those list items to have some form of visual identifier that they are pending deletion, and pending transfer ... a line divider within all of the my tasks widgets where below the visual partition, the pending transfer and pending delete and pending new task are placed into that lower divider area. have them be a bit dimmer in light value and look like a pending asset would look in a good website." **Backend** (`MCP-APPLIED:` migration `reassign_request_direction_aware_queries`, captured at `supabase/migrations/20260502200000_reassign_request_direction_aware_queries.sql`): (1) Replaced `get_my_incoming_reassign_requests` with a direction-aware filter — was returning my outgoing transfer requests too (because the existing WHERE clause keyed only on `current_assignee_id = caller`, which is the correct decider for TAKE direction but the WRONG party for TRANSFER direction). New filter returns rows where caller is the DECIDER: `(direction='take' AND current_assignee_id=caller) OR (direction='transfer' AND requester_id=caller)`. JSON now includes `direction` + `other_party_id` / `other_party_name` so the modal can render appropriate copy. Legacy fields preserved for backwards-compat. (2) Added `get_my_outgoing_pending_reassign_requests()` for the symmetric case — rows where the caller initiated. EXECUTE locked to authenticated. **Frontend helper** ([taskReassign.ts](src/lib/queries/taskReassign.ts)): `IncomingReassignRequest` interface gains optional `direction` + `other_party_*` fields; new `OutgoingReassignRequest` interface; new `fetchMyOutgoingPendingReassignRequests()` helper; new `taskReassignKeys.outgoing()` cache key. **UI** ([MyTasksCard.tsx](src/components/tasks/MyTasksCard.tsx)): full restructure of the body. Computes a `pendingByTask` Map<taskId, {kind, otherPartyName}> from the two request queries. Splits visible tasks into `activeVisibleTasks` (rendered above) and `pendingVisibleTasks` (rendered below the divider, dimmer at opacity-60 + status badge replacing the role/name metadata line). New PENDING eyebrow divider (Hourglass + amber/80 caps + thin hairline) shows only when there's anything below. Pending NEW task requests (kind='create' + status='pending') render as a dedicated `<PendingCreateRequestRow />` row variant since there's no materialized assigned_task underlying them yet (Plus icon for pending, Hourglass + rose tint for rejected with reviewer note inline). Pending tasks suppress the Submit-Completed queue toggle (clicking the checkbox no-ops) since toggling complete on a mid-flight task would race with the admin/peer's decision; click body still opens the detail modal. The expandable strip + chip-toggle from PR #16 retired in favor of the always-visible divider section. Realtime subscription extended: now listens to `task_requests` + `task_reassign_requests` events too, so the divider section auto-promotes tasks back to active when the admin declines a delete or a peer declines a transfer (PR #87 enabled the realtime publication). **Verified**: `npm run build` clean (2.56s); 0 advisor ERRORS; pre-existing Supabase auth errors only in dev preview console. **Out of scope**: cancellation of pending requests from the row itself (likely follow-up — add a small X button on hover that calls a new `cancel_*` RPC); same pending treatment on Studio + Team Boards (those aren't "your" tasks personally, so the pending-state semantic doesn't quite fit). **`CLAUDE:`** authored. |
| 2026-05-02 | `a5a9fe2` (PR #87) | **Realtime sync for task surfaces — admin/member views update live.** User reported a real bug doing a sanity check after PR #85: admin deleted a task from `/admin/templates` in one browser; the member's My Tasks widget in another browser still listed it; clicking → opening the detail modal → submitting "Ask admin to delete" returned `task not found` from the server. Root cause: the request-flow tables (`assigned_tasks`, `assignment_notifications`, `task_requests`, `task_reassign_requests`) were NOT in the `supabase_realtime` publication, so the existing `postgres_changes` subscription in MyTasksCard never received events from cross-session writes. Fix: (1) `MCP-APPLIED:` migration `realtime_task_sync` — `ALTER TABLE ... REPLICA IDENTITY FULL` on all 4 tables (so DELETE events carry full row data — required for client-side filters like `assigned_to=eq.<user-id>` to match) + `ALTER PUBLICATION supabase_realtime ADD TABLE` for each (idempotent via DO-block guards). (2) Wired missing realtime subscriptions on `AssignedTaskBoards.tsx` (Studio + Team Boards on `/daily` were polling-only at 60s), `AssignAdmin.tsx` (the per-member task list refetched only on mutation; now syncs when the member completes/edits/deletes from elsewhere), and `PendingTaskRequestsWidget.tsx` (admin queue now pops new member requests instantly instead of waiting for the 60s refetch). (3) Defensive `handleStaleTaskError` in TaskDetailModal — when the server returns "task not found" (rare cache-race window between admin direct-delete and a member's request-delete submission), surfaces a friendly `"This task was already removed by an admin."` toast + invalidates local caches + closes the modal instead of the raw error. **Verified**: `npm run build` clean (2.62s); 0 advisor ERRORS; SQL `\\dRp+` confirms `assigned_tasks` / `assignment_notifications` / `task_requests` / `task_reassign_requests` all show `rep_id=full` and `in_pub=true`. **Out of scope**: `sessions` realtime (booking-board live updates are Lean 5 territory; deferred). **`CLAUDE:`** authored. |
| 2026-05-02 | `3fffc6f` (PR #85, supersedes #83) | **Member request actions — unified delete + transfer + shared compact composer pattern.** Combines what was originally split across PR #83 (request-to-delete) and PR #85 (request-to-transfer) into one PR after user feedback that the two flows should look + feel identical and the original transfer composer pushed Send below the modal viewport. **Entry points** in TaskDetailModal footer are now small **PILL BUTTONS** (icon + label): rose `🗑 Delete` (request admin delete) + gold `⇄ Transfer` (hand off to a teammate). Both shown only on the user's own active member-scope tasks. **Two-click flow** for both: (1) pill click opens an inline composer in the footer; (2) Send commits. **Shared `<ComposerShell />`** atom keeps both flows visually consistent — single-line header (icon + label), body slot for the caller's inputs, action row with Cancel + filled tonal Send (gold for transfer, rose for delete) + ⌘/Ctrl+Enter / Esc keyboard hint inline. Tight vertical rhythm (px-4 py-2.5, space-y-1.5) so the composer fits inside `FloatingDetailModal`'s `max-h-[90dvh]` (was 85vh — switched to dynamic-viewport-height for proper anchoring on browsers with chrome) even on small viewports. **Single state machine** (`compose: null | 'transfer' | 'delete'`) prevents both composers from rendering side-by-side; opening one auto-closes the other. **Schemas + RPCs unchanged** from the originals — `task_requests.kind` + `target_task_id` (PR #83 schema) and `task_reassign_requests.direction` (PR #85 schema) ship together; their RPCs (`submit_task_delete_request`, `submit_task_transfer_request`, dispatching `approve_task_request` + `approve_task_reassignment` + `decline_task_reassignment`) ship together. `MCP-APPLIED:` to staging via separate migrations under `20260502140000_*` (delete) + `20260502160000_*` (transfer); `ADVISOR-VERIFIED:` 0 ERRORS post-merge. **PR #83 closed as superseded** — its branch lives on origin for archive. **`CLAUDE:`** authored. |
| 2026-05-02 | `56693de` (PR #84) | **AssignAdmin admin-delete UX polish — fix doubling-checkbox in select mode + per-row inline confirm.** Two follow-up fixes to PR #82 ([AssignAdmin.tsx](src/pages/admin/AssignAdmin.tsx)) after the user tested on production. (1) **"Doubling box issue"**: in select mode every row was rendering BOTH the rose select-checkbox AND the gold complete-toggle side by side. Fix: in select mode the row now shows ONLY the rose select-checkbox (the gold complete-toggle is hidden). One mode → one checkbox per row. (2) **Per-row Trash inline confirm**: replaced the native `window.confirm(...)` browser dialog with the same inline two-step "Confirm delete" pill the bulk Delete uses; auto-resets after 4s; auto-clears if admin enters bulk select mode mid-confirm. **`CLAUDE:`** authored. |
| 2026-05-02 | (PR #83, superseded by #85) | **Member request-to-delete flow — second half of the locked admin-immediate / member-request model** (closed; merged into PR #85 with the unified composer pattern). |
| 2026-05-02 | `52954df` (PR #82) | **Admin direct-delete for assigned tasks — per-row Trash + bulk Select/Delete bar on `/admin/templates`.** User pivoted off Lean 4 (Calendar polish) to ship this "asap" — the new Assign page had Edit per-row but no way to delete a task at all, only mark complete (which left rows in the data and skewed per-member counts). **Backend** (`MCP-APPLIED:` migration `admin_delete_assigned_tasks`, captured in repo at `supabase/migrations/20260502120000_admin_delete_assigned_tasks.sql`): SECURITY DEFINER RPC `admin_delete_assigned_tasks(p_task_ids uuid[])` returns `{ deleted_count }`. Admin-guarded via `is_team_admin()`, team-scoped via `team_id = get_my_team_id()`, search_path locked to public. Single bulk RPC takes a uuid[] so per-row delete (one id) and bulk select-and-delete (N ids) hit the same code path. EXECUTE locked to `authenticated` only. **`ADVISOR-VERIFIED:`** 0 ERRORS. **Frontend** ([adminTasks.ts](src/lib/queries/adminTasks.ts)): new `adminDeleteAssignedTasks(taskIds: string[])` helper. **UI** ([AssignAdmin.tsx](src/pages/admin/AssignAdmin.tsx)): per-row hover-only Trash icon + new "Select" toggle button in the top action bar; selecting morphs the top bar to `N selected · Select all/Clear all · Delete · Cancel` with a two-step confirm on Delete; bulk action scoped to the currently-viewed member only. **`CLAUDE:`** authored. PR #84 followed up with the doubling-box + inline-confirm UX polish. |
| 2026-05-02 | `f81ff07` (PR #81) | **Lean 3 — widget grid carousel containment.** Closes the user-reported "widgets slip way down the page" funkiness by replacing the 3-column CSS grid with a single-row horizontal carousel. **Layout**: phone (<640px) shows 1 widget per page; tablet (640-1024px) shows 2; desktop (≥1024px) shows 3 — same look as today on desktop, no visual regression at the live viewport. **Navigation**: when widgets exceed the page size, ChevronLeft / ChevronRight buttons appear at the row edges (vertically centered, dim when at boundary) plus a row of page dots below (long gold = current, dim short = others). **Drag**: drag-to-swap is the only widget reorder gesture; no vertical drag, no falling-down-the-page. Drop on another widget = swap order; drop on edge sensor or empty space = no-op (snap back). **Auto-advance**: while dragging, holding the widget against the row's left or right edge for 600ms pages the carousel in that direction (re-arms on success → continuous paging during sustained hold), so cross-page swaps work. Edge sensors only mount during drag. **Data model**: collapsed `(col, order)` to a flat `order` sequence; `col` is preserved on each `WorkspaceWidgetState` row as a sort hint when migrating older saved layouts but no longer drives rendering. `swapWidgets(a, b)` exchanges `order` (and rolls `col` along). `moveWidgetByDropTarget` / `reorderWidgets` / column droppables all retired (no callers remain). `WORKSPACE_LAYOUT_VERSION` 34 → 35 so any saved 3-column layouts flush to the new flat-row defaults. **PR #34 invariant preserved**: `onDragEnd` is still the single commit point for layout mutation; `onDragOver` is used only to manage the auto-advance edge timer and never mutates widget order. Files touched: [WorkspacePanel.tsx](src/components/dashboard/WorkspacePanel.tsx) (full rewrite, ~410 lines), [useWorkspaceLayout.ts](src/hooks/useWorkspaceLayout.ts) (simplified to swap-by-order, ~135 lines), [registry.ts](src/domain/workspaces/registry.ts) (LAYOUT_VERSION bump). Verified at 1440 / 768 / 375: desktop 3 widgets all 432×696px, no nav controls (1 page); tablet 2 widgets per page across 2 pages with prev/next + 2 dots; mobile 1 widget per page across 3 pages. Click-Next at tablet translated card 0 from x=25 to x=-694 (off-screen) and card 2 from x=745 to x=25 (visible) — page transition translates correctly. Auto-advance during drag wired but not headless-testable; user should sanity-check by dragging a widget to a row edge in real preview. Lean 5 (Booking interactions) + future widget-add work will exercise the >3-widget code path that the carousel was built for. |
| 2026-05-01 | `73d4f32` + `821df18` | **CODEX SECURITY STABILIZATION + LIVE DEPLOY.** Manual Supabase SQL work in project `ncljfjdcyswoeitsooty` cleared Security Advisor from 6 `Security Definer View` errors to **0 errors**. Codex captured those manual DB fixes in git as `supabase/migrations/20260501090000_harden_security_surface.sql` and `supabase/migrations/20260501093000_lock_remaining_intern_views.sql`, and hardened `vercel.json` with CSP + anti-clickjacking / anti-sniff / referrer / permissions headers. User committed/pushed on `main`; Vercel deployment reached `Ready`; user sanity-checked login, dashboard, leads, clients, bookings, and notifications with no obvious regression. Provenance note for future sessions: **manual dashboard actions happened in Supabase SQL Editor; code/file changes were authored in Codex.** |
| 2026-04-16 | `5be53d9` | Vercel migration prep (removed deployToRoot, added vercel.json) |
| 2026-04-16 | `f3a9442` | Connected to Checkmark Intern Manager Supabase project |
| 2026-04-16 | `2a1aaf3` | Login flow hardening (legacy JWT, transient-error retry, owner row triggers) |
| 2026-04-16 | `8026094` | Demo mode bypass for missing env vars |
| 2026-04-16 | `4eccde5` | Recovery email flow — RecoveryGate, email-based reset |
| 2026-04-16 | `5169163` | Hardened signOut — local clear before server revoke |
| 2026-04-16 | `50b852a` | Auth: PKCE → implicit, deferred async in onAuthStateChange, eager recovery render |
| 2026-04-16 | `97d5ff6` | Inline recovery-hash detection in `src/index.html` (beats supabase-js init race) |
| 2026-04-16 | `887c902` | Clock-out modal "Log Out" button actually signs out |
| 2026-04-17 | `0e6ddc7` | Phase 4 cleanup — removed GH Pages workflow + stale build artifacts |
| 2026-04-17 | `2c0d285` | Stripped placeholder data (forum mocks, TaskContext seeds, Flywheel mockup) |
| 2026-04-17 | `473f04c` | Analytics + Flywheel merged into one page; "Content" nav renamed to "Forum" |
| 2026-04-17 | `b0ec210` | Profile/MyTeam/Forum/Overview rewired to real `intern_users` queries |
| 2026-04-17 | `9a82a12` | `sessions.assigned_to` column added; bookings persisted to Supabase |
| 2026-04-17 | `ba76dfb` | Code-split routes via React.lazy + Suspense in Layout |
| 2026-04-17 | `4ac735c` | **Phase B rename** — 10 `intern_*` tables → `team_*` + compat views; 103 source refs updated; `src/types/database.ts` regenerated |
| 2026-04-17 | (DB only) | Created accounts: Richard Baca (engineer), Matthan Bow (intern) — temp pw `ChangeMe2026!` |
| 2026-04-18 | `c08b7e4` | Overview Piece 1 — removed Admin Shortcuts / Approval Queue / duplicate Today Schedule / Daily Snapshot; renamed "Today Schedule" → "Calendar" |
| 2026-04-18 | `1d88558` | Overview Piece 2 — Tasks widget readable fonts (14px body) + TODAY eyebrow |
| 2026-04-18 | `96d6d10` | Overview Piece 2.5 — smarter 3-pass stage mapping + rotating All-tab sort so colors span flywheel stages |
| 2026-04-18 | `bcfd644` | Overview Piece 3 — Calendar widget: TODAY anchor + Mon–Sun weekday strip with today highlighted in gold |
| 2026-04-18 | `e210c2b` | Overview Piece 4 — Booking widget: TODAY anchor + UPCOMING TODAY count + "+ Book a Session" CTA |
| 2026-04-18 | `07daea6` | Overview Piece 5B — forum recent-activity feed (transient, superseded by 846673b) |
| 2026-04-18 | migration | `chat_channel_reads` table + RLS + `mark_channel_read()` RPC + `get_channel_notifications()` RPC for per-user unread tracking |
| 2026-04-18 | `846673b` | **Overview Piece 5A** — Discord-style Notifications widget: per-channel unread badges, optimistic mark-read on click, realtime refresh on new messages |
| 2026-04-18 | `8347d79` | Overview reorder — Tasks/Notifications on top row, Calendar/Booking on bottom; layout version bumped to 3 so all users pick up the new default |
| 2026-04-18 | `5a18d9d` | **Admin Hub redesign** — 5 widgets (Assign, Flywheel, Team, Notifications, Approvals) in a 2-big-left + 3-right grid; added rowSpan support; Hub.tsx simplified to PageHeader + WorkspacePanel; admins no longer see member widgets on Hub |
| 2026-04-20 | `df1b0a4` | **Overview visual polish** — `MyTasksCard.embedded` prop kills the double "My Tasks" title + double `widget-card` border when mounted inside `DashboardWidgetFrame`; Day/Week toggle collapses onto the stage-pill row. Rebalance row spans (team_tasks 3 → 2, forum_notifications 1 → 2) so both Overview columns terminate at the same Y — zero dead space. Layout version bumped 7 → 8 to flush saved layouts. |
| 2026-04-20 | `28246f7` | **Phase 1 Step 1 — load-perf instrumentation.** Opt-in `perfTrace` module (`src/lib/perfTrace.ts`); wraps auth / overview batch / streak / checklist paths. Console emits a grouped cold-start waterfall when `localStorage.debugPerf = '1'`. No prod overhead. |
| 2026-04-20 | `cf39bb6` | perf: satisfy strict TS in production build (tsconfig happy path). |
| 2026-04-20 | `a6e0a83` | perf: gate the Overview flush on `profile` so the waterfall captures real queries, not just `auth:getSession`. Fixes early-flush bug where `MemberOverviewContext.refetch()` early-returns when `!profile`, flipping `loading` false before queries run. |
| 2026-04-20 | — | **Perf baseline locked.** Production cold-start on `dashboard-v3-dusky.vercel.app` is 1,289ms over 8 checkpoints. Longest single span: `overview:batch` 427ms. Three `auth:fetchProfile:byId` fetches — tied to the `"Multiple GoTrueClient instances"` warning. Full waterfall in `docs/SESSION_CONTEXT.md` → Performance baseline. |
| 2026-04-20 | `c719ef2` (PR #1) | **Phase 1 Step 2A — duplicate Supabase client removed.** Deleted `src/lib/chatSupabase.ts` (second `createClient` with default auth config colliding on the same storage key); consolidated 8 call sites in `Content.tsx` + `adminHubWidgets.tsx` to the main `supabase` client. Preview cold-start drops to 998ms / 7 checkpoints / `overview:batch` collapses 427→185ms. `"Multiple GoTrueClient instances"` warning GONE. One parallel `auth:fetchProfile:byId` remains (was 3×, now 2×). |
| 2026-04-20 | `341d2d1` (PR #2) | **Phase 1 Step 2A' — profile-fetch dedupe.** Added `handledForUserId` ref + `maybeFetchProfile` wrapper in `AuthContext.tsx` so the `getSession().then(fetchProfile)` and `onAuthStateChange(INITIAL_SESSION)` paths no longer both fire. Ref releases on transient errors, sign-out, and `rejectAndSignOut` so retries + sign-in cycles still work. 2×→1× profile fetch. Architectural hygiene win; latency-wise ~stable since the two fetches were parallel. `refreshProfile()` intentionally bypasses the dedupe so admin profile edits still re-read. |
| 2026-04-20 | `85b7df7` (PR #3) | **Phase 1 Step 2B — cron-materialize checklist instances + client fast-path.** 3 Supabase migrations (`enable_pg_cron_and_cron_run_log`, `cron_materialize_checklists_function`, `schedule_materialize_checklists_cron`) + 1 client change in `useChecklist.ts`. pg_cron runs daily at 11:00 UTC (5am MDT / 4am MST) calling `cron_materialize_checklists()` which iterates active members and calls the existing RPC — results logged to `cron_run_log`. Client hits `team_checklist_instances` directly before falling back to the RPC. On cold start the RPC never fires. −~170ms on critical path. |
| 2026-04-20 | `4e666b9` (PR #4) | **fix(perf-trace): gate Overview flush on completed-batch-for-profile-id.** Surfaced the TRUE post-2B waterfall — previous "870ms / 7 checkpoint" reading on PR #2 was an artifact: the flush fired early when `profile` arrived, before the second `refetch` pass started. Added `lastLoadedProfileId` ref set only inside `refetch`'s success path; tightened flush gate to `profile && lastLoadedProfileId.current === profile.id && !loading && !daily.loading`. Mirrors the `handledForUserId` pattern from PR #2. No user-facing behavior change — pure measurement correctness. |
| 2026-04-20 | — | **True cold-start locked: 826ms on PR #4 preview.** 6 clean checkpoints, `checklist:rpc` absent (backend-prepared state holds). Longest single span: `checklist:items` 265ms (items fetch for the 22 daily rows). Remaining ~100ms gap between profile-fetch end (@266ms) and parallel batch start (@372ms) is the React.lazy / provider-mount gap that 2C will naturally shrink. Full day net: **1,289ms → 826ms = −36% / −463ms**. |
| 2026-04-21 | `357562c` (PR #5) | **Phase 1 Step 2C — snapshot RPC.** One `member_overview_snapshot(p_user_id, p_date)` SECURITY DEFINER function replaces four parallel client waves (`overview:batch` + `overview:streak` + `checklist:lookup` + `checklist:items`). Server derives `submission_type` from `team_members.position` (authoritative read path per Codex peer-review); `mustDoConfig.ts` still owns the write path. Indexes added on `deliverable_submissions`, `sessions`, `member_kpis`. `MemberOverviewContext` split into outer provider + inner `MemberOverviewLoaded` that mounts fresh when snapshot arrives; `useChecklist` accepts validated `PreloadedChecklist` with metadata-match check. Preserves pre-2C `kpi_entries` ordering bit-for-bit (flagged latent bug for separate PR). Preview cold-start locked: **470ms with 3 checkpoints** — ONLY `auth:getSession`, `auth:fetchProfile:byId`, `overview:snapshot`. Server-side RPC execution 7.3ms per EXPLAIN ANALYZE. Full Phase 1 Step 2 arc: 1,289ms → 470ms = **−64% / −819ms**. |
| 2026-04-22 | `ec8d3b7` (PR #11) | **Unify member task surface.** AssignGroupModal rewired to new RPC pipeline (fires notifications). MyTasksCard rewrites to read `assigned_tasks` directly via `fetchMemberAssignedTasks` with optimistic complete + realtime. Mock MyTasksContext + CreateTaskModal deleted. AssignedTasksWidget retired, content folded into `team_tasks`. `WORKSPACE_LAYOUT_VERSION` 9→10. Click-to-highlight: notification → `highlight-task` CustomEvent → MyTasksCard scrolls + flashes. |
| 2026-04-22 | `0b09313` (PR #12) | **Scope foundation + three-column Tasks page.** Migration adds `assigned_tasks.scope` ('member'|'studio') + `completed_by` + indexes + `assigned_to` nullable + scope-vs-assignee CHECK. Two new RPCs: `get_team_assigned_tasks`, `get_studio_assigned_tasks`. `/daily` rebuilt around three cards (My · Studio · Team) reading the real data. New `AssignedTaskBoards.tsx` component with graceful fallback when migration unapplied. |
| 2026-04-22 | `9cf9bf5` (PR #13) | **Assign page redesign + session-assign pipeline + preview auto-login.** `/admin/templates` now three sections: Quick Assign widget (inline compose) + Assign-a-Session tile + Templates grid. Session-assign: new `assign_session` RPC, `sessions.assigned_to` update + notification atomic; `assignment_notifications` gains `session_id` (XOR with `batch_id`). `get_assignment_notifications` rewritten LEFT JOIN style to surface both shapes. Notification click routing branches on `session_id`. Preview auto-login: `Login.tsx` reads `VITE_PREVIEW_LOGIN_*` env vars and silently signs in when hostname matches `dashboard-v3-git-*.vercel.app` (production alias hardcoded excluded). |
| 2026-04-22 | `aa53c79` (PR #14) | **Studio scope write-path + scope-aware completion guard.** `assign_custom_task_to_members` gains `p_scope` param (defaults 'member'; 'studio' creates single row, no recipients, no notifications). `complete_assigned_task` branches on scope: member → assignee+admin only; studio → any authenticated team member. `assigned_tasks.recipient_assignment_id` relaxed nullable. QuickAssignWidget gets Members/Studio toggle in header; studio mode hides recipient picker and shows pool-semantics note. `completed_by` now recorded on every completion. |
| 2026-04-22 | `eef4ff3` (PR #15) | **Session polish.** `/sessions` listens for `highlight-session` CustomEvent (shipped in PR #13) — scroll-and-flash on the matching row, mirrors PR #11 MyTasks pattern with a pending-highlight buffer. New `findEngineerConflict` helper + amber warning banner in SessionAssignModal when selected engineer has overlapping booking; non-blocking (studios sometimes double-book on purpose). |
| 2026-04-22 | `c0e6a47` (PR #16) | **User task requests + admin approval queue.** New `task_requests` table with RLS. 5 RPCs (`submit_task_request`, `approve_task_request`, `reject_task_request`, `get_pending_task_requests`, `get_my_task_requests`). Approval atomically materializes an `assigned_tasks` row + fires notification; reject stores optional note. `assignment_notifications` gains `task_request_id`; XOR CHECK relaxed to "exactly one of (batch, session, task_request)". Enum gets 3 new types. MyTasksCard adds "+ Task" button + expandable pending-requests strip. New `PendingTaskRequestsWidget` on admin Hub (span 2). `WORKSPACE_LAYOUT_VERSION` 10→11. |
| 2026-04-23 | `a13dbe4` (PR #17) | **Rich +Task modal.** Bottom-of-list placement (Monday.com "+ Add item" pattern); title + description + flywheel stage picker + due-date; tags carry through to the request → approval → materialized task. Fixes the prior 1-line modal that missed stage tagging. |
| 2026-04-23 | `148257a` · `c07a7ea` (PRs #18–#19) | **Unified admin task modal + Hub placement flip.** Consolidated 3 overlapping admin modals (AssignTaskModal, AssignCustomTaskModal, AssignGroupModal) into one Hub-owned Quick Assign modal with recipient + scope + template triggers. Hub's Assign widget flipped to lead with Quick Assign (high-traffic); Flywheel / Team / Notifications reordered per use frequency. |
| 2026-04-23 | `0054dea` · `6924b41` (PRs #20–#21) | **Assign Trello-style 3-column layout.** `/admin/templates` split into Assign / Approve / Templates columns. Pinned-tile pattern at top of Assign column; canonical Ableton-style filter pills with counts on Templates. Equal-width columns so tiles don't jostle when content changes. |
| 2026-04-23 | `371a547` · `e789f33` (PRs #22–#23) | **Overview 3-column layout + shared CalendarDayCard.** Overview grid flattened to 3 equal columns; Tasks + Booking split from one stacked card into two standalone widgets so drag-reorder treats them independently. New `CalendarDayCard` extracted — same day-view renders inside the Overview Calendar widget AND on the `/calendar` page (chevron day-nav + notes). |
| 2026-04-23 | `7f03e7e` (PR #24) | **Hub 3-column layout.** Hub rebuilt to Quick Assign+Approvals · Flywheel · Notifications+Team. Equal-width columns, uniform widget chrome, mixed row-spans. |
| 2026-04-23 | `f9b4279` · `ac8615d` (PRs #25–#26) | **TaskDetailModal + split click surfaces + notification click routing.** Task row splits into two click targets: checkbox (complete) vs body (open detail modal). New `TaskDetailModal` shows full task with description, scope, flywheel tag, recipients, complete/cancel actions. Notification clicks route by status (`task_request_submitted` → Hub approval; `task_request_approved` → highlight the materialized task). |
| 2026-04-23 | `b594b2b` · `ebd833e` (PRs #27–#28) | **Flywheel widget wired to real task counts + cache fix.** Hub flywheel widget now reads `assigned_tasks` aggregated per stage; dual-opacity bars (opaque = completed, translucent = assigned-open). PR #28 fixed a cache-invalidation miss — approving a tagged request now invalidates `['team-assigned-tasks']` + `['studio-assigned-tasks']` so the flywheel updates without refresh. Completion in MyTasksCard invalidates same keys. |
| 2026-04-24 | `c266640` · `334e55f` (PRs #29–#30) | **WorkspacePanel restored across all 4 pages + per-page scopes.** Overview / Hub / Tasks / Assign all on `WorkspacePanel` with equal-width 3-column grid, drag-reorder via `DashboardWidgetFrame` grip, expand-to-modal via the maximize icon. PR #30 fixed regressions: `TASKS_WIDGET_DEFINITIONS` + `ASSIGN_WIDGET_DEFINITIONS` exported with scope filtering so each page gets the right widgets (e.g. Tasks shows `team_tasks` + `studio_tasks` + `team_board`; Assign shows `admin_assign` + `admin_task_requests` + `admin_templates`). Row-spans restored so Overview column-1 stacks Tasks+Booking under a 2-row Calendar/Notifications. `WORKSPACE_LAYOUT_VERSION` 11→14. |
| 2026-04-24 | `76884fa` (PR #31) | **Hide workspace controls bar site-wide.** All 4 pages pass `showControls={false}`. With the decision to keep all widgets visible + non-removable for now, the "Arrange your …" card added nothing the widget frames don't already do. Drag + expand still work (they live on each widget's frame, not the controls card). |
| 2026-04-24 | `a48b081` (PR #32) | **Column-snap widget grid with smooth drag.** `WorkspacePanel` rewrite from implicit row-major flow to 3 independent `SortableContext` column stacks. Widget state gains `col` (1..3); `order` is now per-column. Grid renders via 3 Tailwind `grid-cols-3` columns with `verticalListSortingStrategy` inside each. Smoothness fixes: `DragOverlay` renders a widget-shaped gold-outlined ghost tracking the cursor; original slot dims to 0.35 opacity with a dashed gold ring as "card goes here" affordance; cross-column moves happen live so target-column siblings glide out of the way; `closestCorners` as collision detection (the dnd-kit multi-container recommendation). `moveWidgetByDropTarget(activeId, overId)` handles both `col-N` droppable ids and widget ids; all destination resolution happens inside `setLayout`'s functional update so rapid-fire onDragOver calls can't race. `WORKSPACE_LAYOUT_VERSION` 14 → 15. |
| 2026-04-24 | `dd105b0` (PR #33) | **Taller Tasks widgets + compact Booking button + `assignee.name` bug fix.** Three focused changes: (1) `/daily` task widgets (My Tasks · Studio Tasks · Team Tasks) bumped from rowSpan 1 (340px) to rowSpan 2 (696px) so long queues show many rows at a glance. (2) `BookingSnapshotWidget` stripped to just TODAY eyebrow + a prominent "+ Book a Session" button — upcoming-today counter + next-session detail removed. New fractional rowSpan 0.5 (~170px) supported by `widgetHeight()` and `WidgetRowSpan` type. Moved from col 1 (under My Tasks) to col 2 (above Calendar) so it acts as a quick-action chip on the schedule. (3) Fixed the `column assignee.name does not exist` error on /daily Team Tasks widget — `get_team_assigned_tasks` RPC referenced `assignee.name` but `intern_users` has no `name` column (correct is `display_name`). New migration `20260424090000_fix_assignee_display_name.sql` applied to prod Supabase. `WORKSPACE_LAYOUT_VERSION` 15 → 16. |
| 2026-04-24 | `f95f3a5` (PR #34) | **Direct cross-column widget swap.** Dragging widget A onto widget B in a different column now performs a DIRECT 1-for-1 swap — A takes B's slot, B takes A's old slot in one atomic move. No cascading push-down / stacking. First attempt committed the swap live via `onDragOver`, which caused a cascading-swap bug (every widget the cursor brushed got swapped with the active one, piling widgets into whichever column the cursor ended in — screenshot showed 3 widgets stacked in col 3 and col 1 empty). Fix: remove `onDragOver` entirely. `onDragEnd` is now the single commit point. During the drag the `DragOverlay` ghost follows the cursor and nothing else moves; on release, exactly one swap/move/reorder happens based on the drop target. New `swapWidgets(aId, bId)` helper on `useWorkspaceLayout`. Same-column reorder still uses `useSortable`'s built-in transform preview + insert-on-drop (the list-reorder feel was already smooth). |
| 2026-04-24 | `46c140a` (PR #35) | **Remove double-boxed chrome on Studio + Team widgets.** Studio Tasks and Team Tasks widgets on /daily were rendering their own `widget-card` wrapper + bold heading INSIDE the outer `DashboardWidgetFrame` — visible nested-card double-box. Stripped the inner Card + CardHeader from `AssignedTaskBoards`. Now renders body-only (show-completed toggle + scrollable task list), matching the MyTasksCard embedded pattern. Both boards are only used as widgets so we always-embed without an `embedded?` branch. |
| 2026-04-24 | `d602c67` (PR #36) | **Flywheel stage pills + right-aligned due date.** Three changes across MyTasksCard + AssignedTaskBoards: (1) Stage filter pills back at the top of My Tasks (Overview + /daily) and Team Tasks (/daily) — All on its own row, 5 stages below (no scroller). (2) Due date moved from the sub-meta line to a right-aligned column on each row ("Today" / "Apr 25" / "—" placeholder). (3) Widget descriptions cleared on team_tasks/studio_tasks/team_board. Header "N open" line + bottom-of-list show-completed text removed. Show-completed becomes an icon-only eye toggle in a sticky footer bar alongside the +Task button. Pending-requests chip moves to the same footer. Helpers added: `taskStage(category)` + `formatDueShort(dueDate)`. |
| 2026-04-24 | `5634e94` (PR #37) | **Restore pending→Submit flow + tighten can_complete.** Two bugs + restored UX pattern. (1) `get_member_assigned_tasks` RPC was missing `can_complete`, `scope`, `assigned_to`, `assigned_to_name` — client defaulted `can_complete: false` so every My Tasks checkbox was disabled. RPC recreated to emit them. (2) `get_team_assigned_tasks.can_complete` allowed `is_team_admin()` override → admins could check off any team-member's task. Tightened to strict `scope='studio' OR assigned_to=caller`. (3) Restored the pending→Submit Completed flow on all three task widgets: clicking a checkbox adds id to local `pendingIds` set (visual flip only, no RPC); SubmitBar at the bottom is greyed when empty, gold when ≥1 pending; clicking Submit commits all queued toggles via parallel RPC calls. Both migrations applied to prod. |
| 2026-04-24 | `da916e8` (PR #38) | **Peer "Request to take" reassignment.** Hover a teammate's task in Team Tasks → gold "Request to take this task" overlay → click fires RPC → assignee gets a notification → opens Approve/Decline modal. Approval atomically moves `assigned_tasks.assigned_to` to the requester. New table `task_reassign_requests` (id, task_id, requester_id, current_assignee_id, status, note, resolved_at, resolver_id). New notification enum values (`task_reassign_requested/_approved/_declined`) + new `task_reassign_request_id` column on `assignment_notifications` with updated XOR check. Four RPCs: `request_task_reassignment`, `approve_task_reassignment`, `decline_task_reassignment`, `get_my_incoming_reassign_requests`. New `TaskReassignRequestModal` component lists pending incoming with Approve/Decline rows + optional decline note. Notification widget routes `task_reassign_*` types to the modal / cache invalidation. |
| 2026-04-24 | `683199c` (PR #39) | **Tier 1 closeout — studio tile + admin hub parity + race safety.** Three loose-end fixes from PRs #37–#38. (1) Studio Task surface — 4th tile on `AdminAssignWidget` opens `AdminTaskCreateModal` with `initialScope="studio"` preset. (2) Admin Hub reassign parity — `AdminNotificationsWidget` mirrors the Forum widget's `task_reassign_request_id` routing. (3) Approve race safety — `approve_task_reassignment` rejects if the task's `assigned_to` no longer matches the request snapshot, auto-cancels the stale request. Migration applied to prod. |
| 2026-04-24 | `c8f617e` (PR #40) | **Edit Tasks widget + admin RPC.** New admin-only "Edit Tasks" library on the Assign page. Two RPCs: `admin_list_all_assigned_tasks` (every live task across the team, ordered by completion → due date → recency) and `admin_update_assigned_task` (partial update via COALESCE + `p_clear_*` flags + `task_edited` notification to assignee). New notification enum value `task_edited` reuses `batch_id` as the polymorphic ref. New widget `AdminEditTasksWidget` (count + CTA) + `AdminEditTasksModal` (search by title, assignee filter, show-completed toggle, click-to-expand inline edit form for title/description/stage/due date). |
| 2026-04-25 | `3fcb2ab` (PR #41) | **Assign-page reorg per user sketch.** First slice of the user-sketched 5-PR redesign. Layout shuffle to: col 1 = Task Requests + Edit Tasks, col 2 = Assign (now 2 tiles only: +Task + +Booking — Studio Task reachable via Task modal's scope toggle, Task Group folded into PR #42's Add-from-template flow), col 3 = Templates. `AssignGroupModal` deleted (template-apply temporarily uses the Templates wizard). +Session tile renamed to +Booking for clearer copy. `WORKSPACE_LAYOUT_VERSION` 17 → 18. Followup PRs queued: #42-#46. |
| 2026-04-25 | `6541f32` (PR #42) | **Row-by-row +Task modal with Add-from-template.** Replaces single-task `AdminTaskCreateModal` on the Assign page with a row-by-row composer. Top-of-modal Members/Studio toggle replaces buried scope dropdown; "+ Add task" appends a blank row; "+ Add from template" opens `AddFromTemplateModal` (pick template card → tick items → "Add N tasks") which appends imports as editable rows. Each row: title / description / flywheel stage / due date / required. Backend: new `assign_custom_tasks_to_members` (plural) RPC accepts a jsonb array of task drafts + recipients + scope; for member scope = ONE batch + N×M tasks + ONE notification per recipient ("3 new tasks") instead of N separate notifications. Studio scope = M shared rows. Hub Quick Assign keeps the simpler single-task modal. |
| 2026-04-25 | `132afc7` (PR #43) | **Edit widget split + Edit Bookings backend/modal.** The `admin_edit_tasks` widget becomes a compact twin-button "Edit" widget at rowSpan 0.5 — Edit Task (PR #40 modal) + Edit Booking (new). Three RPCs: `admin_list_all_sessions` (upcoming first, toggle past), `admin_update_session` (partial update + `session_reassigned` notification when assignee changes), `admin_delete_session` (best-effort cancellation notification before delete). New `AdminEditSessionsModal` with search-by-client, assignee filter, include-past toggle, inline click-to-expand edit form (client / date / start-end / room / engineer / status), delete-with-confirm. Soft amber overlap warning when the edited time collides with another session for the same engineer (non-blocking). User-facing copy "Session" → "Booking" + "All engineers" → "All assignees" (matches Edit Tasks). Col 1 registration order swapped so Task Requests sits on top of Edit per the sketch. WORKSPACE_LAYOUT_VERSION 18 → 20. |
| 2026-04-25 | `b75f7de` (PR #44) | **Assign Log widget + studio task visibility fix.** New widget in col 2 under Assign tile widget: 3-column rows (title / "First L." assignee / "Today" or short "Apr 25"), task + studio + session interleaved by recency. New RPC `admin_recent_assignments` returns one row per (subject, assignee). **Mid-PR fix**: studio tasks created via `assign_custom_task[s]_to_members` had `recipient_assignment_id = NULL`, but `get_studio_assigned_tasks` + `get_team_assigned_tasks` INNER-JOINed the recipient chain to scope by team — silently dropping every studio row. Fix: denormalize `team_id` directly onto `assigned_tasks` (new column + backfill); both insert RPCs set it via `get_my_team_id()`; both fetchers filter by `t.team_id` directly. `admin_recent_assignments` projection switched from `row_to_jsonb(r)` (which fails on anonymous-record subqueries) to explicit `jsonb_build_object`. WORKSPACE_LAYOUT_VERSION 20 → 21. |
| 2026-04-25 | `8c863dc` (PR #45) | **Approval Log widget.** New widget in col 1 between Task Requests (top) and Edit (bottom) per the sketch. Surfaces BOTH approved and declined task_requests, labelled by outcome (green ✓ / rose ✕ + title + requester "First L." + relative time, decline rows show reviewer note italic on a 2nd line). New RPC `admin_recent_approvals` orders by `task_requests.reviewed_at` DESC. Approval/decline mutations on `PendingTaskRequestsWidget` invalidate `['admin-log']` so new entries appear immediately. WORKSPACE_LAYOUT_VERSION 21 → 22. |
| 2026-04-25 | (PR #48, in flight) | **Mark-all-read for notifications.** First Tier 2 slice. Migration `20260426000000_mark_all_read_rpcs.sql` adds two SECURITY DEFINER RPCs: `mark_all_channels_read()` (upserts a `chat_channel_reads` row per channel for `auth.uid()`, returning `{ success, channels_marked }`) + `mark_all_assignment_notifications_read()` (bulk UPDATE on `assignment_notifications` WHERE `recipient_id = auth.uid() AND is_read = false`, returning `{ success, notifications_marked }`). Both reject anonymous callers with SQLSTATE 28000 + `SET search_path = 'public'`. New "Mark all read" button slots into the eyebrow row of `ForumNotificationsWidget` (member Overview) and `AdminNotificationsWidget` (admin Hub), beside the rose unread pill — hidden when `totalUnread === 0`. Click fires both RPCs in parallel via `Promise.all`, optimistically zeros every channel + assignment unread in react-query cache (`['overview-notifications']` + `['overview-assignment-notifications', userId]`), and on error invalidates both keys so server truth flows back. New `markAllAssignmentNotificationsRead` helper in `src/lib/queries/assignments.ts`; `markAllChannelsRead` co-located inline in each widget file (mirrors the existing `markChannelRead` pattern — they're sibling helpers, refactor deferred). |
| 2026-04-25 | `7d9a5a0` (PR #47) | **Assign + Overview default widget orders updated.** Pure visual cleanup, four revs. (rev1) `AdminAssignWidget` rowSpan 1 → 0.5 + `AssignTile` collapsed to twin-button row matching `EditButton`. (rev2) Assign-page placements per user drag: col 1 = Assign Log → Approval Log; col 2 = Assign (rs0.5) → Edit (rs0.5) → Task Requests; col 3 = Templates (rs2). Reordered `ADMIN_WIDGET_REGISTRATIONS` array so within-column order resolves. Hub overview placements unchanged. (rev3) Overview-page placements per user drag: `team_tasks` rs1 → rs2 to match Calendar height; `booking_snapshot` col 2 → col 3 (top, rs0.5 unchanged); `today_calendar` fills col 2 alone (rs2 unchanged); `forum_notifications` rs2 → rs1 so col 3 reads as a balanced stack. Tasks-page placements unchanged. (rev4) Added `1.5` to `WidgetRowSpan` union (heights now: 0.5=170, 1=340, 1.5=518, 2=696, 3=1052); bumped `forum_notifications` Overview rs1 → rs1.5 so col 3 (Booking 170 + gap 16 + Notifications 518 = 704px) sits near-flush with cols 1-2 at rs2 (696px). `WORKSPACE_LAYOUT_VERSION` 24 → 28. |
| 2026-04-25 | `6b23dbb` (PR #46) | **Templates Arrange-by + per-role thumbnails.** Closes out the user-sketched Assign-page redesign. `AdminTemplatesWidget`: existing "Include archived" toggle renamed to "Show archived" per sketch; new Arrange-by selector (A–Z / Newest / Role) sits right of the toggles; Role arrangement groups templates under role-tag dividers (Engineer / Marketing / Intern / Dev / Admin / Ops / extras alphabetically / "No role" last). The big-card grid is replaced with friendly thumbnail tiles in a 2-per-row grid. Each tile is a circular gold icon bubble + template name (line-clamp 2) + task count; the icon glyph is keyed off the template's `role_tag` (Headphones=engineer, Megaphone=marketing, GraduationCap=intern, Code2=dev, Briefcase=admin, Settings=ops, FileText=default). Onboarding templates get a tiny emerald GraduationCap badge at the icon corner. Search / filters / Arrange-by row stays pinned at top while the grid scrolls. Each thumbnail opens the same `TemplatePreviewModal` the cards used. Iterations: rev1 first pass shipped a separate `admin_template_preview` widget below Templates, but the user asked for the thumbnails INSIDE Templates instead — so that widget + `TemplateCard.tsx` were deleted and Templates rowSpan went 2 → 3. rev2 returned rowSpan to 2 (col 3 felt unbalanced vs cols 1-2) and resized tiles bigger / friendlier with per-role icons per the user reference image. WORKSPACE_LAYOUT_VERSION 22 → 24. |
| 2026-04-29 | `f297884` (PR #49) | **Add Member email-setup flow + unified TeamManager.** Owner-only "Add Member" button on the Members admin page opens a modal that creates the auth user + `team_members` row + sends a Supabase password-reset email so the new hire sets their own password (matches the Bridget onboarding pattern). Built on top of the existing `admin-create-member` edge function. Members admin was previously two surfaces (read-only roster + separate TeamManager); merged into one table-styled `TeamManager` so admin Members → Add Member flows in one click. Top-nav Members link repointed at TeamManager. |
| 2026-04-29 | `2fd6603` (PR #48) | **Mark-all-read for notifications.** Migration `20260426000000_mark_all_read_rpcs.sql` adds two SECURITY DEFINER RPCs: `mark_all_channels_read()` (upserts a `chat_channel_reads` row per channel for `auth.uid()`) + `mark_all_assignment_notifications_read()` (bulk UPDATE on `assignment_notifications` WHERE `recipient_id = auth.uid() AND is_read = false`). Both reject anonymous callers + lock `search_path = 'public'`. New "Mark all read" button in `ForumNotificationsWidget` + `AdminNotificationsWidget` eyebrow row fires both RPCs in parallel via `Promise.all`, optimistically zeros every unread in cache, invalidates on error. Hidden when `totalUnread === 0`. First Tier 2 maturity slice merged. |
| 2026-04-29 | `a17d4c9` (PR #51) | **Clients table + admin page + booking-modal client picker.** New `clients` table (name, email, phone, notes, archived). New `/admin/clients` admin page for managing the studio's client list. Booking creation modal now includes a clients dropdown + empty-state hint that links to the admin page. Foundation for Tier 2 EmailJS booking confirmations + reminders (which need a real `clients` row to email). |
| 2026-04-29 | `5a7c2a6` (PR #52, draft) | **New Assign page visual scaffold.** Mockup of the boss-sketched member-centric Assign page: left rail with members list + Templates link + Legacy Assign shortcut, main pane shows "All Tasks for {member}" with row-by-row editor, top-bar pill buttons (Settings for Tasks · Save as Template · Templates dropdown). Visual scaffold only — no real data yet. Later in the same session this was promoted to canonical `/admin/templates` (replacing the widget-grid Assign page) via two follow-up commits in the same PR (`1de2246` wire to real task data, `3ac44ad` make canonical). |
| 2026-04-29 | `a7c223f` (PR #53) | **Kill checkbox re-render flicker + template preview-before-apply.** Bug: clicking one task's checkbox made every other checkbox in the list flash yellow because every parent render created brand-new arrow functions for `onToggle`/`onEdit`, defeating React.memo on TaskRow. Fix: stable callbacks via `useCallback` so memoized rows actually skip re-renders. New template preview-before-apply step — clicking "Apply template" opens a modal listing the template's items with checkboxes; admin reviews, unchecks any they don't want, then applies; replaces the prior "instant-apply" pattern that landed the whole template in one click. |
| 2026-04-29 | `5c3fc06` (PR #54) | **Isolate legacy widget Assign view + add sidebar shortcut.** The old widget-grid Assign page (Task Requests · Approval Log · Edit Tasks · Assign · Assign Log · Templates) moved from `/admin/templates` to `/admin/assign-classic`. `/admin/templates` now exclusively renders the new member-centric Assign page from PR #52. New sidebar entry on the Assign page: "Legacy Assign — old widget-grid view" routes to `/admin/assign-classic` so admins can still reach the widget surface during the transition. |
| 2026-04-29 | `97e4eea` (PR #55) | **Use React Router Link for sidebar nav so legacy works.** The new Assign page sidebar's "Templates" + "Legacy Assign" entries were `<a href>` tags — clicking did a full-page reload, which dropped React Query cache and broke the SPA feel. Switched to React Router's `<Link>` so navigation stays client-side. One-line fix per item. |
| 2026-04-29 | `27799b3` (PR #56) | **Full-page Templates manager.** New page at `/admin/template-library` (471-line `TemplateLibrary.tsx`) extracts the templates UI from the Assign sidebar widget into a dedicated full-page surface — same role-tag filter pills, search, archive toggle, friendly per-role thumbnails as the inline widget, but now with room to breathe. Routes added in `src/app/routes.ts` + `src/features/admin/routes.tsx`. AssignAdmin sidebar's "Templates" link routes here. Onboarding corner badge removed from thumbnails (the legacy widget kept consistent so the visual language doesn't fork). |
| 2026-04-30 | (PR #71, in flight) | **Capture Tier 3 plan in docs (no code).** User delivered a wide-scope list of UI tweaks across login / clock-out modal / theme / widget grid / Calendar / Booking / Forum / Admin Hub / Assign / Tasks. Grouped into 10 Leans (small shippable PRs in dependency order); 8 open questions logged in PROJECT_STATE so the user can answer asynchronously. Tier 3 supersedes the prior Tier 2 EmailJS-first sequence; EmailJS + ExportButtons + flywheel ledger remain queued behind Tier 3. No code changes in this PR — pure docs capture so nothing is lost between sessions. |
| 2026-04-30 | `8c01316` (PR #70) | **Retire `New` + `Required` row tags sitewide.** User: "remove all of the red new tags and required tags... we will implement an urgency mechanic but for now i want it all gone free from distraction." Removed the rose pills + tags from MyTasksCard, AssignedTaskBoards (Studio + Team Board), TaskDetailModal, AdminEditTasksModal, AddFromTemplateModal preview, TemplateAssignFlowModal preview, TemplatePreviewModal, TemplateEditorModal item list. Also dropped the `Priority` rose chip from PendingTaskRequestsWidget. Author-facing toggle labels (AdminTaskCreateModal, TaskRequestModal, MultiTaskCreateModal, TemplateEditorModal item editor) keep their `Required` checkbox — those are active edit controls, not passive display tags. The `isNew` flag itself stays (still drives the gold-tinted row background, a subtler signal of freshness). |
| 2026-04-30 | `b0bab20` (PR #69) | **Task row metadata = role + first-name + last-initial + due-date; retire stage pill row; add `Self`/`Assigned` filter on My Tasks.** User: "remove the sub text on the my tasks... should rather state the role designation ie:[marketing]. We do want the name of the person it's assigned to as well [first name & last name initial] and the due date." Plus: stage pills removed for now (not archived — flywheel-event-ledger PR will bring stages back as real, mapped, graphed KPIs). New helpers in `tasks/shared.tsx`: `formatShortName`, `rolePositionFor` (maps `team_members.position` slugs to lowercase short tags: marketing, engineer, intern, a&r, producer; owner returns null), `isSelfAssigned`, and a new `<SourceFilterRow>` component (All / Assigned / Self). My Tasks now reads as `[role] · First L.` plain text (no badge styling), with a gold `DUE` column header anchoring the right-aligned date. Same metadata applied to `TeamTaskRow` in AssignedTaskBoards. Heuristic for `isSelfAssigned`: `task.batch?.assigned_by === task.assigned_to` (or no batch); task-request approvals currently land in `Assigned` because the admin is the formal assigner — acceptable trade-off for now. |
| 2026-04-30 | `4a69dbb` (PR #68) | **Restore Notifications widget on Overview + Hub with shared sleek panel + Post/Channel quick-actions.** User: "I like this notifications look much now, I want to also re-implement a notifications widget just where it was before in overview, same dimensions as the other two widgets, but with the sleek updates & visual clickable ui of the new notifications interface." Plus: "keep the post bubble at the top of both the overview page's notifications widget & the dashboard admin's notifications widget. That way any of us quickly make a post on the forum from our dashboard's making it useful." (1) **`forum_notifications` placement restored** to `member_overview` col 3 rs2 (flush with My Tasks + Calendar at 696px tall). The always-on top-bar dropdown bell stays — both surfaces now coexist (bell = quick glance, widget = dedicated reading surface). (2) **`AdminNotificationsWidget` refactored** to render the same shared `<NotificationsPanel />` as the bell + member widget. ~270 lines of inline channel/assignment row + RPC helper code retired (sat in `adminHubWidgets.tsx` since PR #48 with its own duplicates of fetch + mark-read + click-routing). (3) **New shared `<NotificationsPostActions />`** ([src/components/notifications/NotificationsPostActions.tsx](src/components/notifications/NotificationsPostActions.tsx)) — owns the Post + Channel quick-action buttons + their two modals (PostToChannelModal + CreateChannelModal, moved out of `adminHubWidgets.tsx`). Renders ABOVE the NotificationsPanel on both widgets so any signed-in user can quick-post a chat message or create a channel from their dashboard. Uses the same `['overview-notifications']` cache key as the panel so the channel-list query dedupes (no extra round trip). `WORKSPACE_LAYOUT_VERSION` 31 → 32. Verified at 1440: Overview Notifications widget rendered at col 3, w=432 h=696 (matches Calendar/My Tasks); Admin Hub heading list shows "Notifications" with Post + Channel buttons present. |
| 2026-04-30 | `4160de5` (PR #67) | **Notifications bell → rightmost slot of the top bar; drop the standalone Sign out button.** User feedback: "the dropdown looks amazing, we just have to move it and the bell to the right side of the header. we don't need the log out link since we have the clock out link that logs you out." Two surgical edits in [Layout.tsx](src/components/Layout.tsx): (1) `<NotificationsBell />` removed from its earlier position between `<SocialLinks />` and the theme toggle, re-rendered as the FINAL element of the right cluster after the avatar button; (2) the dedicated `<button>` with the `LogOut` icon (and the now-unused `LogOut` import) deleted — the `SelfReportModal` already exposes a Log Out path on Clock Out (`onLogout` prop wired to `handleSignOut`). Verified bounding boxes at 1440 viewport: bell `rightEdge=1389`; dropdown still anchors flush right via `getBoundingClientRect`, panel `right=1383 w=360 x=1023`. `handleSignOut` itself stays — only the standalone trigger went. |
| 2026-04-30 | `ccf781c` (PR #66) | **Buttery-smooth notifications dropdown — ease-out-expo open transition, frosted backdrop, layered shadow, snappier rows.** Polish pass on the dropdown that PR #65 introduced. Researched Figma / Linear / Vercel notification panels and translated their patterns into concrete CSS. (1) **Open transition**: `opacity 0 → 1`, `transform: scale(0.96) translateY(-4px) → scale(1) translateY(0)`, 180ms `cubic-bezier(0.16, 1, 0.3, 1)` (ease-out-expo as popularized by Vercel/Linear). Two-frame `mounted/entered` state defers the to-state class for one rAF so the browser actually animates from the from-state instead of starting at the destination. `transformOrigin: top right` so the panel grows out of the bell. (2) **Frosted backdrop + layered shadow**: panel chrome is `bg-surface/95 backdrop-blur-xl border border-white/[0.06] shadow-[0_1px_2px_rgba(0,0,0,0.18),0_8px_24px_rgba(0,0,0,0.32),0_16px_48px_rgba(0,0,0,0.4)]` — three-stop shadow stack reads as a real floating surface, not a flat box. (3) **Snappier row interactions**: row transitions narrowed from `transition-all` to `transition-[background-color,border-color,transform] duration-150 ease-out` so hovers feel responsive (was 200ms+ feeling sluggish), plus `active:scale-[0.995]` for tactile press. (4) **Smooth scroll + autofocus**: `scroll-smooth` on the panel body so mark-all-read re-orders glide; panel autofocuses on open so Escape works without an extra Tab. Verified at the byte level: from-state captured at `requestAnimationFrame` 1, to-state at frame 2, settled rect 360×147 at the same right-anchor. |
| 2026-04-30 | `1dc043c` (PR #65) | **Overview header glow-up — beveled MemberHighlights, standardized gold CTA shadow, IG/TikTok/YouTube strip, Notifications dropdown bell.** Five-part polish per user spec. (1) **MemberHighlights** (`src/components/members/MemberHighlights.tsx`): bubble gap `gap-3` → `gap-5` (12px → 20px); each tile gets a beveled gold pill ring built from a `bg-gradient-to-b from-gold/85 via-gold/55 to-gold/25` outer + `ring-1 ring-gold/35` hairline + `inset_0_1px_0` highlight + `0_2px_6px` glow; hover deepens to full-gold gradient and a brighter glow. (2) **Standardized gold CTA**: unified Book a Session (Overview header), Sessions page Book/Add Client, and Layout Clock In on `h-10 px-4 rounded-2xl bg-gradient-to-b from-gold to-gold-muted shadow-[0_6px_14px_rgba(214,170,55,0.18)]` — replaced the heavy `0_14px_28px_22%` feathering with the lighter shadow already used on the task action buttons. (3) **Social-link strip** (new `src/components/SocialLinks.tsx`): Instagram + TikTok (custom inline SVG since lucide doesn't ship one) + YouTube, hidden below md, gray-on-hover-gold; hrefs are stubs until backend wire-up. (4) **NotificationsPanel** extracted from `ForumNotificationsWidget` into `src/components/notifications/NotificationsPanel.tsx` — the widget body (channels + assignments interleaved, optimistic mark-read, realtime subs, click routing) lives there once and the widget becomes a thin wrapper. (5) **NotificationsBell** (`src/components/notifications/NotificationsBell.tsx`) added to the Layout top bar: bell button with rose unread badge, click opens a 360px portal-anchored dropdown rendering the panel; closes on X / Escape / outside-click but **NOT** on row click (per user spec — "stays down until you x out"). Each notification row gets a category badge: forum (violet MessageSquare), task (gold ClipboardList), booking (sky-blue Calendar) — picked off `session_id` for assignments, "forum" for channels. **Removed `forum_notifications` from `member_overview` placements** so the Overview is now a focused 2-col layout (My Tasks + Calendar). `WORKSPACE_LAYOUT_VERSION` 30 → 31 so saved layouts prune the placement. |
| 2026-04-30 | (PR #64, in flight) | **Retire Clients top-nav, fold into Bookings/Clients toggle on `/sessions`.** User identified the standalone Clients menu (added in PR #51) as drift — clients management belonged with bookings, not as a separate top-nav surface. Deleted: top-nav entry in [Layout.tsx](src/components/Layout.tsx), `APP_ROUTES.admin.clients`, the lazy-loaded `<ClientsAdmin />` route in [features/admin/routes.tsx](src/features/admin/routes.tsx), and the [ClientsAdmin.tsx](src/pages/admin/ClientsAdmin.tsx) file itself. Extracted: a self-contained [ClientsPanel.tsx](src/components/clients/ClientsPanel.tsx) (search + active/archived toggle + table + editor modal — same chrome as before, no PageHeader since the parent owns it) with a `registerAddClient` callback so the parent can wire its page-header "+ Add Client" button to the panel's editor. Refactored [Sessions.tsx](src/pages/Sessions.tsx) with a `view: 'bookings' \| 'clients'` toggle in the page header (URL hash `#clients` for deep-link). Action button swaps between "Book a Session" and "Add Client"; bookings-only category pills (`All / Engineer / Consult / …`) hide on the Clients view. Reference-checked at 1440×900: outer chrome `max-w-6xl mx-auto` x=144 right=1296 w=1152 in BOTH views — identical to Settings/Members/Calendar. |
| 2026-04-30 | (PR #63, in flight) | **Chrome consistency fixes — Members section overflow + Members & Assign sidebar bottom-flush.** Recurring chrome bugs the user flagged multiple times. (1) **Members right-pane border was getting pushed off-screen** when the 8-column member table rendered — CSS Grid's default `min-width: auto` on a `1fr` track expands to fit the widest descendant, ignoring the track size. Added `min-w-0 overflow-hidden` to TeamManager's right-pane `<section>` so the section actually obeys its 1fr track and the rounded border stays at `right=1296px` (flush with the `max-w-6xl` grid) on 1440. (2) **Assign + Members sidebar bottoms didn't line up with main pane bottoms.** Both grids had `items-start`; Assign's aside also had `h-fit sticky top-4`. Switched both grids to `items-stretch` and dropped `h-fit sticky top-4` from the Assign aside, mirroring `/calendar` (`grid-cols-[300px_1fr] gap-3 items-stretch`). Verified bounding boxes: Assign aside.bottom=552 = main.bottom=552; Members aside.bottom=612 = section.bottom=612. **Canonical chrome dimensions captured** in `feedback_chrome_reference_check.md` for future border work — at 1440×900: outer `max-w-6xl mx-auto`; grid `grid-cols-[280px_1fr] gap-6 items-stretch`; right pane `bg-surface rounded-xl border border-border p-6 min-h-[320px]` (+ `min-w-0 overflow-hidden` whenever the right pane holds a wide table); grid x=144 right=1296 w=1152; aside w=280; right pane w=848. **Process change:** verify chrome via `getBoundingClientRect()` numbers vs the canonical reference page BEFORE claiming verified — not just screenshots. User trigger phrase: "reference-check it". |
| 2026-04-30 | (PR #62, in flight) | **Members > Clock Data shift history table.** Closes the loop on PR #50 by populating the Clock Data left-rail slot PR #58 reserved. New SECURITY DEFINER RPC `admin_list_clock_entries(p_member_id uuid default null, p_limit int default 100)` returning `{entry_id, member_id, member_name, clocked_in_at, clocked_out_at, duration_minutes, notes}` per shift. Admin-guarded via `is_team_admin()`, team-scoped, capped at 500 rows. Migration `20260430000000_admin_list_clock_entries.sql` applied to staging — only the standard SECURITY DEFINER advisor warnings (same shape as PR #50's RPCs). New `fetchAdminClockEntries(memberId?, limit?)` helper + `timeClockKeys.adminEntries(memberId)` cache key in [timeClock.ts](src/lib/queries/timeClock.ts). New [ClockDataSection.tsx](src/components/admin/ClockDataSection.tsx) renders inside the existing left-rail layout — `Shift history` h2, "All members" filter `<Select>`, table with member (linked to `/profile/{id}` per the user-entities-clickable rule) · clocked-in · clocked-out (or emerald "ON SHIFT" pulse pill if open) · duration (`Hh Mm` formatted) · notes (line-clamped 2). Empty / loading / error states match the rest of the admin chrome. |
| 2026-04-29 | (PR #61, in flight) | **Overview UI refresh — Booking moves to header button, Notifications full-height, member-avatar highlights, no inline task notes.** Four UI changes: (1) `booking_snapshot` widget removed from `member_overview` placements; the action moved to a "+ Book a Session" primary button in the Overview page-header `actions` slot. The button opens the same canonical `CreateBookingModal` and refetches `MemberOverviewContext` on close (button is wrapped inside the provider so it can call `useMemberOverviewContext`). (2) `forum_notifications` rowSpan 1.5 → 2 so col 3 is the same height as My Tasks (col 1) and Calendar (col 2). (3) New `MemberHighlights` component (`src/components/members/MemberHighlights.tsx`) — Instagram-story-style horizontal row of circular avatars (60px, gold gradient ring, name underneath) at the top of the Overview, between the page header and the workspace grid. Uses `fetchTeamMembers` + filters by `status = 'active'`; each avatar is a React Router `<Link>` to `/profile/{id}` per the user-entities-clickable rule. Falls back to initials when `avatar_url` is null. (4) `MyTasksCard` no longer renders `task.description` inline below the title — the description is reserved for the expanded `TaskDetailModal` (click the row body) so the row stays compact. `WORKSPACE_LAYOUT_VERSION` 29 → 30 so saved Overview layouts shed `booking_snapshot` and pick up the new Notifications rowSpan. |
| 2026-04-29 | (PR #50, in flight) | **Persist Clock In/Out + 'On the Clock' Hub widget.** First Tier 2 slice. New `time_clock_entries` table with one open shift per user enforced by partial unique index + 4 SECURITY DEFINER RPCs (`clock_in()` idempotent, `clock_out(p_notes)`, `get_my_open_clock_entry()`, `admin_currently_clocked_in()`); migration `20260429000000_time_clock_entries.sql` applied to staging, advisors clean. Header Clock button moved from `useState` to `useQuery` + `useMutation` with cache key `['my-open-clock-entry']` invalidating across tabs. SelfReportModal (Close + Log Out paths) now fires `clock_out` alongside its existing behavior. New `AdminClockInWidget` ("On the Clock") on admin Hub col 3 below Team (rowSpan 0.5) with live elapsed counter ticking every minute, refetches every 60s. New `src/lib/queries/timeClock.ts` with the 4 RPC wrappers + cache key factory. `WORKSPACE_LAYOUT_VERSION` 28 → 29 for the new widget id. Followup queued: populate Members > Clock Data pane (PR #58 reserved) with shift history table. |
| 2026-04-29 | (PR #59, in flight) | **Fix Assign right-pane top alignment.** The toolbar (Settings for Tasks · Save as Template · Templates ▾) was rendered as a row above the main card, which pushed the "All Tasks" card top edge below the sidebar's Members card top edge — visually misaligned vs Settings. Folded the toolbar INSIDE the main pane card with a hairline divider below it (now `<main className="rounded-xl border border-border bg-surface p-5">` wraps both), so both the sidebar and main card start at the same Y on the grid. Also: aligned `rounded-2xl` → `rounded-xl` on the sidebar + main card, `gap-4` → `gap-6`, added `items-start` to the grid — exact Settings rhythm. The inner "Title row" lost its own card chrome since it's now nested. |
| 2026-04-29 | (PR #58, in flight) | **Members admin restructured around Settings-style left-rail.** Extracted `SectionNavItem` from AdminSettings into a reusable `AdminSectionNavItem` component at `src/components/admin/AdminSectionNavItem.tsx` (generic over a section-key type). TeamManager now wraps its existing roster (search + filter pills + 8-column member table) as the "Roster" view inside a left-rail layout; new "Clock Data" rail item shows a "coming soon" empty state that PR #50 (Clock In/Out v2) will populate with shift data. Page header simplified to the standard `<h1>Members</h1>` + count badge + Add Member button (no subtitle per PR #60's modern/sleek pass) + `mb-6` rhythm matching Settings/Assign. Stripped the inner `widget-card` chrome from the table (was doubling up against the right-pane card chrome). Foundational for the Tier 2 Clock In/Out v2 PR. |
| 2026-04-29 | `ad76452` (PR #57) | **Add page header to Assign so it aligns with Settings rhythm.** AssignAdmin was jumping straight into its two-column grid with no `<h1>` + subtitle, while every other admin page (Settings) opens with a "Title + subtitle, mb-6" header block. That made the sidebar/main-content cards sit higher on the viewport and read as misaligned. Added `<h1>Assign</h1>` + "Assign tasks to your team members" subtitle, mirroring [AdminSettings.tsx:163-166](src/pages/admin/AdminSettings.tsx:163) exactly. |
| 2026-04-29 | (PR #60, in flight) | **Remove subtitle text from all page headers — modern/sleek pass.** User wants every "title page" to read as just an h1, no descriptive subtext. Touched 15 files: 11 PageHeader callers (Hub · Templates · TeamManager · TemplateLibrary · ClientsAdmin · Sessions · DailyNotes · Dashboard · DailyChecklist · Pipeline · Leads) had their `subtitle="..."` props deleted; 4 inline `<p className="text-text-muted mt-1">` page-header subtitles deleted (AdminSettings · AssignAdmin · Reviews · Schedule). PageHeader component itself was left untouched (subtitle prop still supported). Login page subtitle preserved (helps unauthenticated users orient). |
| 2026-04-22 | `09ec6fc` (PR #10) | **Assignment-system polish merged.** 3 bundled concerns: (1) `FloatingDetailModal` stacking: module-level stack tracks mount order; `z-index = 60 + depth × 10`; Escape handler guards on `stack[-1] === id` so a single keypress closes only the topmost modal. Backdrop opacity steps down with depth so parent peeks through — clear visual hierarchy. Body scroll lock coordinates with stack (only releases when stack empties). (2) Hard-delete templates: new `delete_task_template` RPC (admin guard, returns `{ items_removed, assignments_preserved }` so the UI can show an honest toast); `deleteTaskTemplate` query helper; Delete button in `TemplatePreviewModal` footer with confirm copy that explains past assignments stay intact. Past `assigned_tasks` survive untouched thanks to the FK `ON DELETE SET NULL` shipped in PR #8. `task_template_items` cascade. (3) Batch cancel UI: new `fetchRecentTemplateBatches` query helper reads `task_assignment_batches` directly (admin RLS) with recipient counts + cancelled state; new `RecentAssignmentsSection` component on Assign page renders last 10 batches with per-row Cancel action wired to `cancel_task_assignment_batch` + cache invalidation of `assigned-tasks` so recipients see the batch vanish without refresh. Cancelled batches stay in the list (muted) so admins can see what they've recalled. |
| 2026-04-22 | `d1c046b` (PR #9) | **Assign-page comprehensive redesign merged.** Replaces the legacy `/admin/templates` page (which wrote directly to `report_templates`) with a surface built entirely on the new `task_templates` system via PR #6 + PR #8 RPCs. **New files**: `src/lib/queries/taskTemplates.ts` (13 RPC wrappers + `taskTemplateKeys` factory); `src/components/admin/templates/{TemplateCard,TemplatePreviewModal,TemplateEditorModal,TemplateDuplicateModal,TemplateAssignFlowModal,index}.tsx`; `src/components/members/MemberMultiSelect.tsx` (extracted from Hub's AssignTaskModal so Hub + new Assign wizard share identical recipient UI); 11 new TS types in `src/types/assignments.ts`. **Rewrites**: `src/pages/admin/Templates.tsx` from 1069 → 360 lines (hero + stats + filter bar + card grid + empty state + "New Template" CTA). **Deleted**: `src/components/templates/TemplateAssignModal.tsx`, 100-line `PRESET_TEMPLATES` hardcoded seed list, ~800 lines of legacy page code. Modals: Preview (FloatingDetailModal + item list + Archive/Duplicate/Edit/Assign footer) → Editor (create or edit; per-item inline edit/delete + add-item form) → Duplicate (rename + confirm) → Assign (3-step wizard: recipients → items → confirm with summary). Architectural call (user-confirmed): REPLACE entirely; report_templates admin moves to PublishChecklistModal pathway. **Known issue**: nested modals share `z-[60]`; stacked preview + sub-modal backdrops overlap click targets. Queued for PR #10 fix. |
| 2026-04-22 | `3a4ade7` (PR #8) | **Task-assignment Phase 2 backend merged.** 1 FK-tweak migration (`assigned_tasks.source_template_id` + `source_template_item_id` → `ON DELETE SET NULL` so admins can delete/evolve templates without losing team progress); 1 RPC migration with 6 admin-only SECURITY DEFINER functions + one non-breaking filter on `get_member_assigned_tasks`. New RPCs: `update_task_template`, `update_task_template_item`, `delete_task_template_item`, `duplicate_task_template`, `cancel_task_assignment_batch` (also hides open tasks), `assign_template_preview` (read-only, same item-ownership validation as the real assign RPC). `get_member_assigned_tasks` now filters `ar.status = 'active'` so cancelled batches vanish from the Assigned-To-You widget. All guarded with `is_team_admin()`, cross-template injection rejected with SQLSTATE 22023. No frontend changes — this PR exists to ship the schema admins need before the comprehensive Assign-page UI lands. |
| 2026-04-22 | `8821785` (PR #7) | **Task-assignment MVP frontend merged.** Widget visibility model formalized: `accessVisibility` (`personal` / `shared` / `admin` / `role_scoped`) + `dataScope` (`self` / `team` / `target_user` / `global`) added to `WorkspaceWidgetRegistration`; disjoint `MemberScope` / `AdminScope` types preserve admin/member invariant while enabling multi-page placement within a side. `WorkspaceWidgetRegistration` swaps `defaultSpan`/`defaultRowSpan` → `defaultPlacements: WidgetPlacement[]` so a widget can declare placements on multiple pages. Added MemberWidgetId `assigned_tasks`; `team_tasks` + `assigned_tasks` both claim `member_overview` + `member_tasks` placements. New `AssignedTasksWidget` (~200 lines) in `memberOverviewWidgets.tsx`: `useQuery` + `completeAssignedTask` optimistic toggle + `postgres_changes` subscription on `assigned_tasks`. Extended `ForumNotificationsWidget` + `AdminNotificationsWidget` with a second `useQuery` for assignment notifications, a parallel realtime subscription on `assignment_notifications` filtered by `recipient_id`, and an "ASSIGNMENTS" section below the channels list with optimistic mark-read. Added 4th "Custom Task" tile to `AdminAssignWidget`; new inline `AssignCustomTaskModal` with multi-recipient select + title/description/category/due/required form, calls `assign_custom_task_to_members` RPC. `src/types/assignments.ts` + 5 new query helpers in `src/lib/queries/assignments.ts`. `DailyChecklist.tsx` replaced with a widget-grid shell for the new `member_tasks` scope (pulls `TASKS_WIDGET_DEFINITIONS` filtered by placement from `widgetRegistry`). `WORKSPACE_LAYOUT_VERSION` 8 → 9. Mock `TeamTasksCard` + `StudioTasksCard` retired (neither was DB-backed). |
| 2026-04-21 | `951be5c` (PR #6) | **Task-assignment Phase 1 backend merged.** 2 migrations: (1) 5 enums + 6 tables (`task_templates`, `task_template_items`, `task_assignment_batches`, `assignment_recipients`, `assigned_tasks`, `assignment_notifications`) + 7 hot-path indexes + RLS via existing `is_team_admin()`; (2) 11 SECURITY DEFINER RPCs covering template library CRUD + 3 atomic assignment actions (custom / full / partial) + member read+complete + notifications. Additive — zero changes to `report_templates`, `task_assignments`, or `team_checklist_*`; 2C cold-path untouched. Server-side `get_member_assigned_tasks`: 3.2ms. Seed data from smoke tests kept (see Known seed data). Frontend UI pending Codex handoff PR. |

---

## Active

### Phase 2 kickoff — 2026-05-07

**Phase 1 is live and must stay stable while Phase 2 is built.**

Current production status:

- Live URL: `https://dashboard-v3-dusky.vercel.app`
- Production Settings shows **Google Calendar Sync**
- Connected account: `checkmarkaudio@gmail.com`
- Calendar target: `primary`
- Sync mode today: `Checkmark -> Google -> Apple`

### Phase 1 risks while Phase 2 is not live yet

- **Dual-entry drift:** if staff create or edit bookings directly in Apple Calendar, those changes will not come back into Checkmark.
- **Refresh-lag confusion:** Apple Calendar can briefly lag behind Google, which can make a correctly-synced booking look missing for a short time.
- **Manual duplicate risk:** if someone recreates a missing-looking event in Apple by hand, Checkmark can later push the real event too, causing duplicate calendar entries.
- **Human-source-of-truth confusion:** staff may assume the connected Apple view is authoritative and forget that Checkmark still owns the official booking state.
- **Outbound sync regressions during unrelated booking work:** any deployment that touches booking create/edit/cancel flows, session mapping, or the Google sync helper can temporarily affect outbound sync if not tested.

### Safe Phase 2 rollout order

1. **Add inbound detection separately** from the current outbound path.
2. **Apply inbound edits only for already-linked events** (`google_event_id` exists).
3. **Allow only a restricted inbound field set first** (time/date/title/notes/location).
4. **Handle external cancellation second**, mapping deletes/cancels to Checkmark cancellation, not hard-delete.
5. **Leave unmatched Apple/Google-created events out of auto-import at first**; surface them for admin review instead.

### Guardrail

Working on Phase 2 should **not** interrupt Phase 1 if we keep it additive:

- leave the existing outbound sync contract intact
- add inbound logic behind the existing connection, not instead of it
- avoid changing the current Google connection model or calendar target until Phase 2 inbound is proven
- keep the team rule in force: **If it is not in Checkmark, it is not booked**

### Tier 3 — Interface tweaks + page-by-page fixes (2026-04-30)

User-delivered list of UI tweaks across login / clock-out modal / theme / widget grid / Calendar / Booking / Forum / Admin Hub / Assign / Tasks. Tier 3 supersedes the prior "Tier 2 EmailJS-first" sequence; EmailJS + ExportButtons + flywheel ledger remain queued behind Tier 3.

**Status (2026-05-08 audit):** 8 of 10 leans shipped. Forum (Leans 8 + 9) is the only Tier 3 work remaining. After it lands, Tier 3 closes and the deferred queue (Analytics + flywheel ledger, EmailJS, ExportButtons, Clock In/Out v2) takes priority.

| # | Lean | Status | Where it shipped |
|--|--|--|--|
| 1 | Critical fixes — login form back · clock-out modal de-jam · "+ Book a Session" button | ✅ shipped | PR #72 (#162 added preview-login lockdown) |
| 2 | Theme & surface polish — light-mode tone-down · subtle body gradient | ✅ shipped | PR #79 (multiple revisions) |
| 3 | Widget grid containment — single-row carousel, no vertical drop | ✅ shipped | `WorkspacePanel.tsx` single-row carousel + `useWorkspaceLayout` flat order |
| 4 | Calendar polish — click-to-detail · persistent notes · brighter notes + ♪ bullets | ✅ shipped | PRs #157–159 |
| 5 | Booking interactions — click-to-edit · status hover popover · recurring fixes | ✅ shipped | PRs #157–160 |
| 6 | Tasks tweaks — undo request-to-take · Studio Tasks room dividers | ✅ shipped | `cancelTaskReassignment` + Studio sections in `AssignedTaskBoards.tsx` |
| 7 | Admin Hub rearrange — drop flywheel + quick assign · MemberHighlights up top · stretched widgets | ✅ shipped | PR #78 (registry comments mark dropped placements) |
| 8 | Forum polish — longer · own-messages right-aligned · presence bubbles · Troubleshooting → corner button | ⚪ pending | — |
| 9 | Forum media uploads — upload button · image/video preview | ⚪ pending | — (can reuse `member-media` Storage pattern from Tier 1 super-PR) |
| 10 | Assign page revamp — bulk Select/Select-all/Delete · default-on Show Completed · Legacy → Assign Log | ✅ shipped | bulk select + `Select all` in `AssignAdmin.tsx` |

**Auth + Profile leans (separate sequence on top of Tier 3):**

| # | Lean | Status | Where it shipped |
|--|--|--|--|
| auth-1 | Onboarding — Add Member reveals temp password post-create | ✅ shipped | PR (Lean 1 auth) |
| auth-2 | Login polish + 3-layer preview-login lockdown | ✅ shipped | PR #162 |
| auth-3 | Member self-serve change password | ✅ shipped | PR #163 |
| auth-4–7 | Profile schema + Storage bucket + ProfileEditor + site-wide MemberAvatar | ✅ shipped | PR #164 (super-PR) |
| auth-T1 | Wider Discord-style profile (role pills · live status · stats sidebar) | ✅ shipped | PR #165 |

**Deferred (post-Tier-3):** Analytics revamp + flywheel event ledger · EmailJS booking confirmations / 24h reminders / 1hr review-asks · reusable `<ExportButtons />` · Clock In/Out v2 (clock-out modal + per-member shift table).

**Locked answers (user, 2026-04-30):**

1. **Login page** — User: "I don't remember when it disappeared. I manually disabled it for website previews because I had to keep logging in, perhaps that got entangled? It was working though for the live url up until maybe yesterday or the day before." → **Lean 1 investigates** the auto-login env-var leak (likely `VITE_PREVIEW_LOGIN_EMAIL` / `_PASSWORD` ended up in the production Vercel scope). Production must require login; previews stay auto-login per `feedback_preview_no_login_required.md`.
2. **Widget grid** — **(a) widgets locked to their current row, only horizontal swap allowed.** No vertical drag. No empty slots below.
3. **Studio Tasks rooms** — **single widget with section dividers** (visual `[Control room] ─────────`, `[Studio A] ─────────`, `[Lobby] ─────────`, `[Studio B] ─────────`). Backend still adds a `room` column on `assigned_tasks` so tasks can be tagged + grouped; UI renders as one widget with grouped rows under each section header.
4. **Booking status hover** — User: "study Figma + Monday on github for a way to do this seamlessly, easy hover-over and satisfying." Lean 5 = research the pattern (Figma/Monday/Linear-quality hover-popover with smooth transitions), then ship; if `cancel_session` / `confirm_session` / `reschedule_session` RPCs don't exist they get added in the same PR.
5. **"Show completed" default-on (Assign page)** — **local-date.** Tasks completed today stay visible on Assign page until midnight local; after that they roll off. Add a **task-completion timestamp on the detail modal** so the day is tracked. Older completed tasks land on a new **"Completed Tasks" log page in the Members admin left rail** (new section alongside Roster + Clock Data). User: "Add this feature now or in the best Lean order if it takes you off the UI flow." → folded into **Lean 10** (Assign revamp) since the data plumbing is the same as Show-Completed.
6. **Gradient scope** — **body level, very subtle.** Just for satisfying vibe. NOT on cards.
7. **Forum presence** — **online when logged in / site open.** Site-based heartbeat. Richer states ("active 5m ago" etc.) deferred.
8. **Bulk-delete on Assign page** — **top-level bar at the top of the main pane**, scoped to the **currently-viewed member**. Not across all members at once.

**Refined Lean scopes (post-answers):**

- **Lean 6 (Tasks):** Studio Tasks gets a single widget with grouped section headers (`Control room` / `Studio A` / `Lobby` / `Studio B`); add `room` column on `assigned_tasks` (migration + advisor check); group rows by `room` in the UI.
- **Lean 10 (Assign revamp) expanded:** also adds the per-member **"Completed Tasks" log page** to TeamManager's left-rail (new section alongside Roster + Clock Data). Drives the same `completed_at` timestamp the Show-Completed default-on uses on the Assign page.
- **Lean 1 investigation note:** likely culprit is the preview-auto-login env vars bleeding into production. Audit: (a) Vercel env scope (must be **Preview** only, not **Production**), (b) `Login.tsx` hostname guard (must hard-exclude `dashboard-v3-dusky.vercel.app` aka the prod alias), (c) any code changes since 2026-04-28 that touched the login flow.

**Raw user list (preserved verbatim, source of truth for the Leans above):**

> **Important interface tweaks:**
> **Overall interface stuff:**
> 1) Light mode is blinding, please use grey, gold, and black, make use of toning brightness down on boxes slightly to reduce eye strain.
> 2) Widget movement is still funky because there is still a grid detected underneath the current widgets we have, allowing our widgets to slip way down the page. We should only be able to move the widgets left right and center on one row across the website right now.
> 3) Give the website a nice mild gradient using checkmark's colors black gold white and maybe a pinch of purple like the forum page's purple, just to give it a little life.
> 4) Overall login page at the front is gone, we need that back.
> 5) The modal that pops up upon clock out appears stuck, jammed at the top of the website and its first half of it is not visible being cut off — we need to bring it back down.
>
> **Tasks:**
> 2) We need an undo request for requesting to take task feature. Really easy you just click to undo in case you miss-clicked or made a mistake.
> 3) Studio Tasks — can you make a modest "Control room" / "Studio A" / "Lobby" / "Studio B" dividers within the task space in studio tasks widget? I will want to add studio tasks by the room they take place in.
>
> **Calendar:**
> 1) After a booking is made, you should be able to click on the title of the booking and view the booking details, same goes for the side daily calendar view.
> 2) When you leave notes at the calendar, the notes close if you leave the page — make the notes stay opened unless you log out or close them yourself manually.
> 3) Make the note text brighter so you can actually see the notes; make the bullet points of each note little music notes just like this one ♪.
>
> **Booking:**
> 1) "+Book a session" is still looking a little too right-floating, like it's floating away — we need to bring the borders in or something so it is lower down in the header and easy to see.
> 2) Clicking on a current booking should open modal of the booking information & the option to edit.
> 3) The status — you should be able to hover over the "pending" status and have a popup letting you click either Cancel, Confirm, or Reschedule. Hover over a "confirmed" status → Cancel or Reschedule. "Cancelled" status → Reschedule only.
>
> **Forum:**
> 1) Make forum longer lengthwise.
> 2) Let the senders see their own messages as appearing on the right side of the forum while they are logged into their own account, like text messages do, to differentiate.
> 3) Make the members list actually have little bubbles that light up when you are online and turn off when you are offline.
> 4) We need an upload media symbol to easily attach files in the forum.
> 5) If a video or picture is sent, ensure that a visual preview is seen of it so people can see what they will be clicking on.
> 6) We want to make troubleshooting its own modal that you press a button in the corner of the website. Bottom line we don't need it here.
>
> **Dashboard (Admin Hub):**
> 1) Remove flywheel widget.
> 2) Put team members at the top header like how we have it in overview.
> 3) Extend down the notifications widget just like the one in overview, same dimensions.
> 4) Remove quick assign widget.
> 5) Make Task Requests widget longer to match same dimensions as the notifications widget.
> 6) Put daily calendar widget in the middle where KPIs used to be, just like the one in overview.
>
> **Assign page:**
> 1) We need a Select / Select all / Delete function at the top of the page. Checkboxes show up when using this feature, for easy removal of tasks.
> 2) We need an easy-to-see show/hide completed button. Default should be on (showing today's completed). Convey somehow that the default-seen completed checked tasks are from today only.
> 3) Get rid of Legacy Assign, but in the same location on the left assign nav bar we can instead call it the **Assign Log**. Use the data from the assigned & approval log widgets found within it, and turn them into pages like our bookings and clients pages — toggles under the booking-menu pattern.
>
> **Analytics** needs total revamping and flywheel integration — we can do this when we are ready for that big step.

---

## Phase 1 (legacy)

**Phase 1 — Stabilize the foundation** (in progress): Load-perf work from the Codex 4-phase roadmap.

- ✅ **Step 1 — instrumentation + prod baseline** (2026-04-20). 1,289ms cold start, waterfall in `SESSION_CONTEXT.md`.
- ✅ **Step 2A — Duplicate Supabase client** (2026-04-20, `c719ef2` / PR #1). Deleted `src/lib/chatSupabase.ts`; consolidated 8 call sites to the main `supabase`. Preview cold-start 998ms (↓22%), `overview:batch` 427→185ms, `"Multiple GoTrueClient instances"` warning GONE.
- ✅ **Step 2A' — Profile-fetch dedupe** (2026-04-20, `341d2d1` / PR #2). `handledForUserId` ref + `maybeFetchProfile` wrapper in `AuthContext.tsx`. 2×→1× profile fetch.
- ✅ **Step 2B — cron-materialize + client fast-path** (2026-04-20, `85b7df7` / PR #3). pg_cron at 11:00 UTC, `cron_materialize_checklists()`, `cron_run_log`. Client short-circuits the RPC when instance exists. `checklist:rpc` no longer fires on cold start. −~170ms.
- ✅ **fix(perf-trace) flush-gate race** (2026-04-20, `4e666b9` / PR #4). `lastLoadedProfileId` ref; flush now fires only after a real batch completes for the current profile. True cold start revealed: **826ms with 6 honest checkpoints**, longest `checklist:items` 265ms.
- ✅ **Step 2C — Snapshot RPC** (2026-04-21, `357562c` / PR #5). `member_overview_snapshot(p_user_id, p_date)` — one RPC replaces the four-wave waterfall. Codex peer-review refinements incorporated (server-derived `submission_type`, `PreloadedChecklist` metadata match-check, atomic sub-queries in one transaction). **Preview cold-start locked: 470ms / 3 checkpoints / 7.3ms server-side.** Full Phase 1 Step 2 arc: **1,289ms → 470ms = −64%**, landed well inside the 500–700ms target with margin.

**Phase 1 Step 2 CLOSED.** Next structural lever is hosting (Cloudflare migration) once the site has 1–2 stable days on prod post-2C. Timing remains Claude's judgment call per `feedback_cloudflare_migration_timing.md`.

**Phase 1 — Assignment System** (in progress): First-class admin workflow for assigning custom tasks / full templates / partial templates to one or many members, with notifications.

- ✅ **Phase 1 backend — schema + RPCs** (2026-04-21, `951be5c` / PR #6, merged). 6 tables + 5 enums + 7 indexes + RLS via `is_team_admin()`; 11 SECURITY DEFINER RPCs (4 library CRUD, 3 atomic assign actions, 2 member read+complete, 2 notifications). Codex peer-review refinements incorporated (`assignment_recipients` rename to avoid collision, `PreloadedChecklist`-style metadata validation surface, single-transaction atomicity, enums over text). Additive — does NOT touch `report_templates`, `task_assignments`, or `team_checklist_*`. 2C cold-path untouched.
- ✅ **Phase 1 frontend MVP** (2026-04-22, `8821785` / PR #7, merged). Custom-task assign only. New `AssignedTasksWidget` on Overview + Tasks (multi-page placement via `defaultPlacements` in registry). `AssignTaskModal` admin surface behind the Hub Assign widget's "Task" tile. Notifications widget extended with Assignments section + realtime. Widget-visibility model formalized (`accessVisibility` + `dataScope`). `/daily` restructured into a widget grid; mock Team/Studio cards retired. Post-merge: 4th "Custom Task" tile consolidated back into the existing "Task" tile (commit `46d3339` before merge) so the Hub stays 3 tiles as per the original design.
- ✅ **Phase 2 backend prep** (2026-04-22, `3a4ade7` / PR #8, merged). 6 admin-only RPCs for template editing + batch cancel + assignment preview. `ON DELETE SET NULL` FK tweak + `ar.status = 'active'` filter. Ships the schema the comprehensive Assign-page UI now calls.
- ✅ **Assign-page redesign** (2026-04-22, `d1c046b` / PR #9). Full `/admin/templates` rewrite targeting `task_templates`. Card grid + filter + 4 modals + extracted `MemberMultiSelect`.
- ✅ **Phase 1 polish** (PR #10) — modal stacking + hard-delete templates + batch cancel UI.
- ✅ **Unified MyTasks + scope model** (PRs #11, #12, #14) — retired Assigned-To-You widget, three-column Tasks page, studio scope end-to-end (column + RPCs + UI toggle + completion guard).
- ✅ **Assign page reform** (PR #13) — Quick Assign inline compose + Assign-a-Session tile + Templates section; session-assign pipeline via new `assign_session` RPC; preview auto-login.
- ✅ **Session polish** (PR #15) — click-to-flash on `/sessions`, engineer conflict warning on reassign.
- ✅ **User task requests + admin approval queue** (PR #16) — self-serve "+ Task" button for members; Hub approval widget for admins.
- **Phase 3+ (deferred)**: assignment history UI, `mark_all_read`, fold assigned-tasks summary into `member_overview_snapshot`, drag-to-reorder template items, unify `report_templates` into `task_templates`, template RPCs accept `p_scope` (studio templates), notification click routing for `task_request_*` types.
- **Cloudflare migration (queued)**: timing "once stable on prod" — after this six-PR session settles for a day or two, migrate Vercel → Cloudflare Pages as a single-concern PR per `feedback_cloudflare_migration_timing.md`.

**UI design refresh — scoped pilot** (paused): Apply the v1.0 design system PDF to the live app on three pages first. See `~/.../My Drive/Checkmark Audio — Design System · Print.pdf` for the visual spec.

**Locked constraints:**
- ✅ Keep top nav (do not refactor to sidebar even though PDF shows sidebar)
- ✅ Keep current menu names (Overview, Tasks, Calendar, Booking, Forum + admin: Hub, Assign, Members, Analytics, Settings)
- ✅ Keep current header as-is
- ✅ Do not modify Settings dropdown content

**Progress:**
- ✅ **Overview (member) — complete.** Column-snap grid. Col 1: My Tasks. Col 2: Booking button (rs 0.5) above Calendar (rs 2). Col 3: Notifications (rs 2).
- ✅ **Admin Hub — complete.** Column-snap grid: Col 1 Quick Assign + Task Requests · Col 2 Flywheel (rs 2) · Col 3 Notifications + Team. Flywheel reads real `assigned_tasks` per stage (dual-opacity done/open bars). Reassign-request notifications open the same Approve/Decline modal as the member-side widget.
- ✅ **Tasks (`/daily`) — complete.** Column-snap grid: My Tasks · Studio Tasks · Team Tasks — each at rowSpan 2 (~696px). Stage filter pills + right-aligned due dates + pending→Submit Completed flow. Hover a peer's row → "Request to take this task" overlay → assignee approves or declines.
- 🟡 **Assign (`/admin/templates`) — second redesign in progress (PRs #52–#57 merged).** The widget-grid Assign page from PRs #41–#46 (Task Requests · Approval Log · Edit · Assign · Assign Log · Templates in three columns) was preserved at `/admin/assign-classic` but moved out of the canonical Assign route per the user's new boss-sketched direction: a member-centric task editor where you pick a member from a left rail and edit their full task list on the right (much more like Monday.com's row-by-row board than a widget grid). Current state on `/admin/templates`:
  - **Left rail**: Members list (active members only, click selects) + sticky bottom links: Templates (routes to the new full-page Templates manager at `/admin/template-library` per PR #56) + Legacy Assign (routes to `/admin/assign-classic` per PR #54).
  - **Main pane**: Top bar pill buttons (Settings for Tasks · Save as Template · Templates dropdown). Body shows "All Tasks for {selected member}" with row-by-row editor + + Add Task button + counters (N tasks · N complete). Apply-template flow opens a preview-before-apply modal (PR #53) so admin reviews items before they land.
  - **Page header (PR #57)**: `<h1>Assign</h1>` + "Assign tasks to your team members" subtitle so the page aligns with Settings rhythm.
  - **Performance fix (PR #53)**: stable `useCallback` refs so React.memo on `TaskRow` actually skips re-renders. Fixes the "all checkboxes flash yellow when one task toggled" bug.
  - **Templates page (PR #56)**: friendly per-role thumbnail grid + search + role-tag pills + Onboarding/Show-archived toggles + Arrange-by selector (A–Z / Newest / Role). Same pattern as the inline widget on `/admin/assign-classic`, but full-page.
  - **Still on the table**: the boss-sketched "Save as Template" + "Settings for Tasks" top-bar buttons are visual stubs in PR #52's scaffold; both need real implementations next.

All 4 refreshed pages share the same grammar: column-snap `WorkspacePanel` + `DashboardWidgetFrame` (drag grip + expand). Controls bar hidden. Drag model (PR #34): during drag only the `DragOverlay` ghost moves; on drop, cross-column = direct 1-for-1 swap, same-column = sortable insert+shift, empty column = append to that column.

**Out of scope for now:** all other pages. User wants to validate the design language on the highest-impact surfaces first.

---

## Known state / seed data on prod

Rows created by smoke-tests that live on prod Supabase and will be visible to users when the assignment-system UI ships:

- **Template**: `Marketing Onboarding` (4 items, onboarding=true, role_tag='marketing')
  - Items: Set up profile photo / Read brand guide / Connect to content calendar / Shadow two client sessions
- **Assignments**:
  - Full `Marketing Onboarding` → Gavin Hammond (4 tasks)
  - Custom task `Call vendor re: Q2 campaign` → Gavin + Matthan Bow (2 tasks total)
  - Partial `Marketing Onboarding` (items 1+2) → Matthan Bow (2 tasks)
- **State**: Gavin completed "Set up profile photo" + marked one notification read; all else unread/incomplete.

**Why kept**: useful seed data for Codex's frontend PR to render against.

**How to recover if wiped**: `docs/seed_data/task_assignment_phase_1_seed.sql` — run as admin via Supabase SQL editor. Recreates equivalent rows (fresh UUIDs).

**How to clean when ready**: admin UI will gain a "Cancel batch" / "Delete template" surface in Phase 2. Until then, direct SQL: `DELETE FROM task_assignment_batches WHERE title IN ('Marketing Onboarding', 'Marketing Onboarding (selected)', 'Call vendor re: Q2 campaign')` — cascades to recipients + tasks + notifications.

---

## Deferred (not yet built)

### QuickAdd pattern + member task suggestions
**Status:** Designed, not built. On hold until UI design refresh lands.

Phased plan (decisions locked):
- Phase 1: `<QuickAddButton>` + `<QuickAddModal>` reusable components, wire to chat channels first
- Phase 2: Roll out to projects, leads, sessions, positions; simplify TeamManager add flow
- Phase 3: Member-side "Suggest a task" UI hooking into existing `task_edit_requests` schema
- Phase 4: Animations, optimistic UI, undo toasts, empty-state CTAs

Locked design choices:
- **Hybrid suggestions**: members can add to OWN checklist instantly; shared-template additions need admin approval
- **Inline `+ Add X` buttons** per section (no FAB)

Existing infrastructure to leverage:
- `task_edit_requests` table with `change_type` enum (`'add'` / `'rename'` / `'delete'`) and `status` enum
- `approve_task_edit_request()` RPC
- `ApprovalsPanel.tsx` admin UI (already shows pending requests + approve/reject)

### Approach A — column rename
- `intern_id` → `member_id` in 9 tables
- 4 function names containing "intern" (cosmetic)
- 20 indexes whose names still contain "intern" (cosmetic)
- Compat view drop (whenever; free to leave forever)
- See `docs/intern-rename-resume/` for detailed plan

### Other
- **Theme DB persistence** — currently localStorage; coupled with profile pictures (both touch user table)
- **Profile pictures + banners** — Supabase Storage upload; column on `team_members`
- **React Query** everywhere — currently barely used; would make re-navigation instant
- **DnD widget reordering** — replace up/down arrows with `@dnd-kit/core`
- **"View as member" admin toggle** — admins QA member experience without a second account
- **GitHub Pages workflow disable** — Phase 4 manual step on user side (Settings → Pages → Source: None)
- **`CreateBookingModal` real-data write path** — currently inserts to mock `TaskContext`
- **Real Forum/chat send-receive** — channels seeded but no UI to send messages
- **Onboarding flow** — wizard for new team members vs. current "owner manually creates row + sends reset email"
- **Flywheel event ledger** (Phase 2 of original blueprint) — immutable event table; analytics derives from event history. **This is the highest-leverage future work** for tracking quality/productivity.
- **Domain features**: real Performance Reviews, Lead Tracking, Education Students, Projects, KPI Dashboard

---

## Done

- ✅ **Vercel migration** — replaces GitHub Pages; preview URLs; no more hard-refreshes
- ✅ **Auth hardening** — recovery, sign-out, owner protection, transient-error retry, lock-disable, implicit flow, inline hash detection
- ✅ **Owner lockdown** — 3 layers (DB triggers + code constant + email-first resolver); `checkmarkaudio@gmail.com` cannot be demoted or deleted
- ✅ **Account Access UI** — owner-only role toggle + direct setup-link generation
- ✅ **Setup-link onboarding** — admin-generated recovery links copied/sent directly; temp passwords remain fallback only
- ✅ **Placeholder data cleanup** — forum mocks deleted, TaskContext seeds emptied, Flywheel mockup gracefully empty
- ✅ **Analytics merged with Flywheel** — one page, all charts; old `/admin/flywheel` route redirects
- ✅ **Forum nav rename** — "Content" → "Forum" (also: 5-stage flywheel restored on member Overview)
- ✅ **Theme toggle** — light/dark/system with sun/moon header button
- ✅ **Sessions + Calendar real data** — migrated off mock `TaskContext`
- ✅ **Member Overview real data** — daily checklist, today's sessions, KPI trend, must-do progress
- ✅ **Admin shared overview context** — single fetch, all admin widgets share state
- ✅ **Responsive widget grid** — `auto-fit + minmax(320px, 1fr)` + container queries on widget internals
- ✅ **Code splitting** — per-route React.lazy, separate chunks per page (BarChart 308KB only loads on Analytics)
- ✅ **Schema rename Phase B** — 10 `intern_*` → `team_*` with compat views; atomic migration; 0 data risk
- ✅ **Two new accounts** — Richard Baca (engineer/intern role), Matthan Bow (intern/intern role)
- ✅ **Overview refresh (4 widgets)** — Calendar + Tasks + Booking + Notifications, each with a TODAY eyebrow; readable fonts; flywheel stage rotation on the All-tab; Discord-style unread tracking backed by `chat_channel_reads` + `get_channel_notifications()` RPC with realtime + optimistic mark-read on click.
- ✅ **Admin Hub (5 widgets)** — Assign / Notifications / Team / Flywheel / Approvals, mixed-row-span grid. Assign widget includes `AssignTaskModal` + `AssignGroupModal` (new inline modals that write directly to `team_checklist_items` and `task_assignments`). Notifications widget adds admin post + create-channel flows. Flywheel aggregates team KPIs. Approvals inlines approve/reject RPC calls.

---

## Recovery instructions

If a Claude session dies mid-task:

1. **Read this file FIRST** for project context
2. **Read `docs/intern-rename-resume/`** if rename work was active
3. `git log --oneline -10` to see recent activity
4. `git status` to see uncommitted changes
5. Check `~/.claude/projects/-Users-bridges-GITHUB-Dashboard-V3/memory/MEMORY.md` for any private context notes
6. Ask the user what they were working on if anything is unclear

**The auto-memory system at `~/.claude/projects/.../memory/` survives across sessions. The repo's `docs/` folder also survives. Both are equally valuable for recovery.**

---

## How to update this file

After any meaningful commit or decision:

1. **Update the snapshot** (latest commit hash + active line)
2. **Append to the timeline** — date, commit, one-line summary
3. **Move items between Active / Deferred / Done** as state changes
4. **Keep total length under 400 lines** — archive old completed work into `docs/PROJECT_STATE_ARCHIVE.md` if needed

Don't update for every push. Update for meaningful milestones (a feature shipped, a deferral decision, a config change that affects future work).
